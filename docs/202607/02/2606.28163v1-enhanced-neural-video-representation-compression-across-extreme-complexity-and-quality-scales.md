---
title: Enhanced Neural Video Representation Compression across Extreme Complexity and Quality Scales
title_zh: 跨越极端复杂度和质量尺度的增强型神经视频表示压缩
authors: "Ho Man Kwan, Tianhao Peng, Fan Zhang, Mike Nilsson, Andrew Gower, David Bull"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28163v1"
tags: ["query:compress"]
score: 8.0
evidence: 隐式神经表示用于视频压缩，可迁移至图像压缩
tldr: 隐式神经表示在视频压缩中具潜力，但现有方法难以平衡复杂度与可扩展性。本文提出NVRC++，采用轻量INR与多个高分辨率特征网格，配合高效优化框架和先进熵模型，实现四种复杂度等级，各覆盖宽比特率和质量范围，支持实时解码。相比SOTA INR编解码器NVRC，解码速度提升高达7.6倍，性能相当。该方法为不同部署场景提供了统一高效的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28163v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1227, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 687}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1251, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 676}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 387}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28163v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 450}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 556, \"height\": 303}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 646, \"height\": 304}]"
motivation: 现有神经视频编码器在复杂度和可扩展性间权衡不足，缺乏统一架构以适应不同比特率质量等级，限制实际部署。
method: 提出NVRC++，基于轻量INR与多分辨率特征网格，结合高效优化和先进熵模型，实现长视频序列的灵活压缩与实时解码。
result: 在四种复杂度等级下，NVRC++解码速度比SOTA INR编解码器NVRC快7.6倍，同时保持相当的性能。
conclusion: NVRC++实现了跨极端复杂度和质量尺度的统一视频压缩，兼顾效率与性能，适用于多样化部署需求。
---

## 摘要
隐式神经表示（INR）最近成为一种有前景的视频压缩方法，在提供快速解码的同时实现了有竞争力的率失真性能。然而，现有的神经视频编解码器难以平衡复杂度和可扩展性。轻量级模型在扩展到不同比特率/质量级别时通常会出现压缩性能下降，而高性能模型则表现出有限的可扩展性，因为其模型复杂度通常随质量增加。缺乏能够在广泛比特率范围内保持一致复杂度的统一架构严重限制了其多样化的实际部署。为解决这些挑战，我们引入了NVRC++，一种基于INR的新型视频编解码器，它利用带有多个高分辨率特征网格的轻量级INR，在任意给定复杂度水平下提供高可扩展性。与之配套的是一个优化框架，能够对长视频序列在高分辨率网格上进行高效过拟合，从而利用时空冗余而不会带来过高的计算或内存开销。此外，我们设计了一种先进的熵模型用于高效压缩高维网格参数。因此，NVRC++提供了四个复杂度级别（从7kMACs/像素到360kMACs/像素），每个级别覆盖广泛的比特率和质量范围，同时支持实时解码。实验结果表明，与最先进的基于INR的视频编解码器NVRC相比，NVRC++提供了更快的解码速度（最高达7.6倍），同时提供了可比的性能。

## Abstract
Implicit neural representations (INRs) have recently emerged as a promising approach to video compression, delivering competitive rate-distortion performance alongside rapid decoding. However, existing neural video codecs struggle to balance complexity and scalability. Lightweight models often suffer from degraded compression performance when scaled to different bitrate/quality levels, whereas high-performance models exhibit limited scalability, as their model complexity typically increases with quality. This lack of a unified architecture capable of maintaining consistent complexity across a wide range of bitrates severely limits their diverse real-world deployment. To address these challenges, we introduce NVRC++, a novel INR-based video codec that utilizes a lightweight INR with multiple high-resolution feature grids, providing high scalability at any given complexity level. This is paired with an optimization framework that enables efficient overfitting on high-resolution grids for long video sequences, thereby exploiting spatio-temporal redundancies without prohibitive computational or memory overhead. Additionally, an advanced entropy model is designed for efficiently compressing the high-dimensional grid parameters. As a result, NVRC++ provides four complexity levels (from 7kMACs/pixel to 360kMACs/pixel), each spanning wide bitrate and quality ranges while supporting real-time decoding. The experimental results show that NVRC++ offers a much faster decoding speed (up to 7.6x) compared to the SOTA INR-based video codec, NVRC, while delivering comparable performance.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义

- **研究动机**：隐式神经表示（INR）在视频压缩中展现出竞争力，但现有方法在复杂度与可扩展性之间存在严重不平衡。轻量级INR编解码器在扩展比特率/质量时性能下降严重；高性能模型（如NVRC）则复杂度随质量增加而提升，缺乏统一架构实现跨比特率的一致复杂度，难以应用于实际多样化部署场景。
- **核心问题**：如何设计一个既能保持极低复杂度（轻量级）又能覆盖极端质量范围（高比特率下高质量）的INR视频编解码器，且支持实时解码。
- **整体含义**：提出NVRC++框架，通过多高分辨率特征网格、分层编码结构、多维度网格熵模型和增强型轻量INR（HiNeRV++），首次实现了四种复杂度等级（7 kMACs/pixel 至 360 kMACs/pixel）下固定的解码复杂度，同时覆盖宽广的比特率和质量范围，使INR视频编码向实际应用迈出重要一步。

## 2. 方法论

### 核心思想
- 利用轻量级神经网络配合**多个高分辨率特征网格**来解耦模型复杂度与重建质量，使单一模型配置能覆盖宽比特率区间。
- 引入**参数内编码结构（in-parameter coding structure）**，将特征网格参数按时间切片组织，采用类分层B帧结构建模依赖，实现高效压缩。
- 提出**多维度网格熵模型**，结合尺度先验、时间先验和空间先验来压缩高维网格参数，提升压缩效率。
- 设计改进的轻量INR架构**HiNeRV++**，支持多参考输入和像素重排输出层，加速解码。

### 关键技术细节
- **多分辨率特征网格**：提取插值特征与相邻原始网格切片作为多参考输入，提高编码质量。
- **参数内编码结构**：将每个分辨率的网格沿时间轴分成切片，按K级分层B帧顺序编码，每个切片依赖更高层的参考切片，而非全部其他切片。
- **优化框架**：
  - 使用无条件量化（不依赖先前帧），消除特征传播链，允许随机采样，显著降低训练内存需求（最大模型含1亿+参数，可在24GB GPU上编码600帧1080p视频）。
  - 解耦率与失真计算：率估计采用随机采样并缩放期望值，失真计算跳过熵模型。
- **特征网格掩码**（Feature Grid Masking）：对高分辨率网格在训练初期使用类似Dropout的随机掩码，迫使模型先依赖低分辨率网格，随着训练逐步退火，平衡高低比特率性能。
- **多维度网格熵模型**：
  - **尺度先验**：低分辨率网格作为高分辨率网格的条件。
  - **时间先验**：已编码的相同尺度切片作为当前切片的条件。
  - **空间先验**：采用多步掩码（1-4步）利用已编码的同一切片内特征作为条件。
  - 量化采用步长δ_l，分布参数（均值、尺度）由上述先验估计。
- **HiNeRV++**：
  - 特征网格分辨率远高于HiNeRV，且在前三个网络阶段均使用不同特征网格编码。
  - 移除最后阶段的复杂块，仅用Pixel Shuffle层作为输出层，降低计算量。
  - 采用多参考输入（插值特征+相邻原始网格切片）增强输入条件。

## 3. 实验设计

### 数据集
- **UVG**：7个全高清（1920×1080）序列，帧数300~600帧。
- **MCL-JCV**：30个全高清序列，帧数120~150帧。

### Benchmark方法
- **传统编解码器**：x265 (veryslow)、HM-18.0 (randomaccess)、VTM-20.0 (randomaccess)。
- **自编码器类神经编解码器**：DCVC-FM、DCVC-RT（均采用单帧内设置）。
- **INR类编解码器**：HiNeRV、NVRC、C3。

### 评价指标
- PSNR（RGB，BT.601色彩转换）和MS-SSIM，对x265计算Bjøntegaard Delta rate (BD-rate)。
- 解码复杂度：kMACs/pixel和FPS（帧/秒）。

## 4. 资源与算力

- 文中**未明确说明**训练所使用的GPU型号、数量或训练总时长。
- 仅提及“最大模型可在24GB显存的消费级GPU上编码600帧1080p视频”，且训练策略遵循HiNeRV和NVRC（未详述）。推理速度在NVIDIA RTX 4090上测量（根据表格注释）。
- 因此，无法量化训练所需的算力资源。

## 5. 实验数量与充分性

- **实验组数**：包含四个复杂度变体（S1-S4）在2个数据集上的完整对比，每个变体至少5个比特率点。对比了7种基准方法（包含传统、自编码器、INR三类）。
- **消融实验**：
  - 对高分辨率网格、特征掩码、空间/时间/尺度先验、多参考输入（2D vs 3D stem）分别进行了完整消融（表2）。
  - 对比了HiNeRV++与HiNeRV的性能与收敛速度（表3）。
  - 对比了NVRC++与NVRC的可扩展性（图4右）。
- **客观性**：基准方法结果均来自原论文或开源实现和提供的checkpoints，评估指标标准，公平。
- **充分性**：覆盖了性能、复杂度、消融、可视化比较，实验设计较为充分，但缺少对训练稳定性、不同视频内容类型的详细分析。

## 6. 主要结论与发现

- NVRC++在四种复杂度等级下均实现了固定的解码复杂度，且性能随复杂度提升而接近或超越SOTA INR编解码器NVRC。
- S4/S3与NVRC性能相当，但解码速度最高快7.6倍。
- S3优于VTM和DCVC-RT；S2优于HM且媲美DCVC-RT低比特率性能；S1优于x265和C3，与HiNeRV相当。
- HiNeRV++相比HiNeRV：收敛速度更快，300 epoch时PSNR高2.09dB，解码速度大幅提升（162.9 vs 35.5 FPS）。
- 多维度网格熵模型、参数内编码结构、特征掩码等每个组件均有显著贡献（消融实验均产生退化）。

## 7. 优点

- **高可扩展性**：固定复杂度配置覆盖宽比特率范围，解决了现有INR编解码器的核心缺陷。
- **解码速度与性能的优异平衡**：在相近性能下，解码速度优于NVRC和其他INR方法，达到实时级别（73.4 FPS at S3）。
- **创新性技术组合**：参数内编码结构、多维度熵模型、特征掩码等设计精巧，有效应对高分辨率网格带来的存储和优化挑战。
- **轻量级与高性能的统一**：展示了通过将表示负担从神经网络层转移到高分辨率特征网格，可以实现更低计算复杂度且更高重建质量。
- **广泛的基准对比**：与多种传统和神经编解码器对比，验证了方法的有效性。

## 8. 不足与局限

- **训练资源未明确**：未报告训练所需的GPU型号、数量和时长，难以评估训练成本。
- **熵解码时间分离**：FPS计算中未包含熵解码步骤（与NVRC一致），实际端到端延迟可能更高。
- **仅测试全高清**：未在更高分辨率（如4K）或更低分辨率上评估可扩展性。
- **内容类型单一**：UVG和MCL-JCV包含自然场景，但对屏幕内容、动画等类型未测试。
- **超参数敏感性**：特征掩码的退火调度、网格分辨率选择等超参数可能依赖人工调整，未分析鲁棒性。
- **消融实验仅在S2上**：未在其他复杂度等级上验证组件有效性是否一致。
- **应用限制**：INR方法仍需逐视频过拟合训练（编码速度慢），尽管解码快，但整体压缩速度仍远低于传统编解码器。

（完）
