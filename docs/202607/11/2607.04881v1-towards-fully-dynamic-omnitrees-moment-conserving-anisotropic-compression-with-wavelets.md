---
title: "Towards Fully Dynamic Omnitrees: Moment-Conserving Anisotropic Compression With Wavelets"
title_zh: 面向全动态全向树：基于小波的矩守恒各向异性压缩
authors: "Theresa Pollinger, Masado Ishii, Jens Domke"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04881v1"
tags: ["query:compress"]
score: 9.0
evidence: 基于小波的各向异性压缩（omnitree），与图像压缩中的变换编码相关
tldr: omnitree结合八叉树和k-d树的优势，高效编码各向异性细化。本文扩展其操作，实现从细化到粗化的完全动态压缩，并自然集成小波以保持矩守恒。通过小波系数指导粗化与向下分割，在3D形状和云密度数据上，存储节省高达28倍，有损压缩比OpenVDB高5倍且视觉质量几乎无损。该工作展示了omnitree在高效存储和处理各向异性数据方面的潜力，为图形学等领域提供新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04881v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1132, \"height\": 908, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 621, \"height\": 961, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1243, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 379, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1217, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1203, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1101, \"height\": 1194, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 926, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04881v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 923, \"height\": 1073, \"label\": \"Figure\"}]"
motivation: 将omnitree从仅支持细化扩展到完全动态压缩，实现各向异性数据的高效自适应粗化，提升存储效率。
method: 定义omnitree上的粗化和向下分割操作，利用小波系数作为局部细化优先级，指导自适应压缩过程。
result: 与OpenVDB相比，对象存储节省高达28倍，云数据集有损压缩约5倍，视觉质量损失可忽略。
conclusion: omnitree在小波引导下实现高效各向异性压缩，在图形学和相关领域有广泛应用前景。
---

## 摘要
最近，全向树作为一种灵活的空间划分树被引入，它改进了八叉树和k-d树的优点：全向树对各向异性细化的高效编码对于具有各向异性特征和高维度的应用特别有意义。这些应用包括但不限于计算机图形学、数据库、机器学习和物理模拟。本文定义了全向树编码上的新操作，将其能力从现有的细化扩展到包括粗化，从而实现完全自适应的压缩。它展示了全向树与小波的自然集成，通过设计守恒了存储函数的矩。对于全向树，小波系数可以解释为局部细化优先级，可用于指导自适应过程。我们推导了由小波系数指导的粗化和向下分裂算法，并展示了它们在一个包含3D形状的大型数据集以及云的连续值密度场上的应用。与OpenVDB（计算机图形学中广泛使用的稀疏体数据数据结构）的比较表明，即使对于中等各向异性的三维数据，全向树也具有实际优势。与OpenVDB相比，对象存储空间最多可减少28倍，且渐近节省超出理论预期。使用有损压缩，云数据集相对于OpenVDB可压缩约5倍，而视觉质量损失可忽略不计。这展示了全向树在高效存储和处理方面的潜力，并激励了对其在各个领域应用的进一步研究。

## Abstract
Recently, omnitrees were introduced as a flexible space partitioning tree that improves upon the benefits of both octrees and k-d trees: Omnitrees' efficient encoding of anisotropic refinements holds particular interest for applications with anisotropic features and high dimensionality. These include, but are not limited to, computer graphics, databases, machine learning, and physics simulations. The present paper defines new operations on the omnitree encoding that extend its capabilities from the existing refinement to also include coarsening and therefore fully adaptive compression. It demonstrates natural integration of omnitrees with wavelets, which conserves moments of the stored function by design. For omnitrees, the wavelet coefficients can be interpreted as local refinement priorities, which can be used to guide the adaptation process. We derive algorithms for coarsening and downsplit that are guided by wavelet coefficients, and show their application to a large dataset of 3D shapes, as well as the continuous-valued density field of a cloud. The comparison to OpenVDB, a widely-used data structure for sparse volumetric data in computer graphics, enables a demonstration of the practical benefits of omnitrees even for moderately anisotropic three-dimensional data. Compared to OpenVDB, objects can be stored using up to 28x less space, and asymptotically show savings that exceed theoretical expectations. Using lossy compression, the cloud dataset can be compressed by $\approx5\times$ compared to OpenVDB, with negligible loss of visual quality. This demonstrates the potential of omnitrees for efficient storage and processing, and motivates further research into their applications in various domains.

---

## 论文详细总结（自动生成）

# 论文中文总结：面向全动态全向树：基于小波的矩守恒各向异性压缩

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有空间划分树（八叉树、k-d树）在面对各向异性数据时效率不高——八叉树每次必分裂所有维度，k-d树每次只分裂一维，两者均无法灵活地在不同层级按需选择分裂的子集。全向树（omnitree）虽然已通过高效编码各向异性细化解决了部分问题，但此前仅支持细化（refinement），不支持粗化（coarsening），无法实现完全自适应的压缩。
- **研究意义**：该工作首次定义全向树上的粗化和向下分裂（downsplit）操作，使其能够从单一方向的细化扩展到**完全动态的自适应压缩**；同时将小波系数自然地整合到全向树框架中，利用小波系数作为局部细化优先级指导压缩过程，实现**矩守恒**（质量守恒），为计算机图形学、数据库、机器学习、物理模拟等领域提供一种高效存储和处理各向异性数据的新方案。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用全向树灵活的二进制分裂标签（每维度1表示需要分裂，0表示不分裂），通过**小波变换**将节点上的函数值转换为小波系数；小波系数的大小反映了该维度上的变化强度，因此可以用于判断哪些维度可以粗化（系数足够小则丢弃）；同时引入**向下分裂**操作，将多维分裂拆成级联的低维分裂，从而暴露更多粗化机会。
- **关键技术细节**：
    1. **粗化（Coarsening）**：对内部节点施加负标记（在指定维度上mj=-1），通过标记迁移算法（类似细化中的标记扫除）修改分裂标签，实现孩子节点融合。粗化完成后，小波系数中与被粗化维度相关的细节系数被置零，剩余系数构成新节点的小波系数。
    2. **向下分裂（Downsplit）**：将一个具有≥2个分裂维度的节点，分解为两层结构：父节点保留一个子集维度S2，新生成的中层孩子节点负责另一个子集S1。变换过程中叶子盒的空间位置不变，仅改变线性化顺序；小波系数按张量积形式重新分配（式14-15）。
    3. **小波引导的压缩算法**：
        - 先对所有节点执行正向Haar小波变换（双层嵌套循环，复杂度O(2^d N)）。
        - **纯粗化压缩**：自底向上扫描，若某节点的所有孩子是叶子且该节点在维度C上的所有细节系数绝对值均≤阈值ε，则进行粗化（C可等于全部分裂维度，也可仅为部分）。
        - **交替粗化-向下分裂压缩**：当纯粗化无法继续时，进入循环：(1) 对每个多维节点，选择|wv^{{j}}|最小的维度j*进行向下分裂；(2) 对新的叶子父节点应用粗化；(3) 归一化恢复规范树。重复直至无粗化发生。
    4. **误差界**：粗化丢弃的细节系数带来的L1误差上界为(2^d-1)·ε·|Ω|·树高（式17）。
- **公式说明**：Haar小波变换矩阵为Hadamard矩阵的Kronecker积（式11）；小波系数与函数值的相互转换通过局部的H和R矩阵实现（式10）。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
    1. **Thingi10K**（4,166个可打印3D模型，水密、无自交、实体，顶点数≤10,000）：将模型体素化为二进制密度函数（物体内为1，外为0），分辨率从ℓ=2到ℓ=7逐步细化。
    2. **Disney云数据集**（WDAS Cloud Dataset）：选取1/16下采样版本，密度值为浮点数，原始活动域(126,86,154)，零填充至(128,128,256)对应初始分辨率(7,7,8)。
- **基准（Baseline）**：OpenVDB（广泛使用的稀疏体数据层次结构），使用其默认的BoolGrid（二进制）和浮点网格存储，并调用prune()进行层次压缩。
- **对比方法**：
    - OpenVDB（原始存储）
    - Omnitree纯粗化（plain coarsening）
    - Omnitree向下分裂+粗化（downsplit coarsening）
    - 另外比较了不同文件存储方式：原始二进制、blosc2压缩后的树描述符+原始数据系数、blosc2同时压缩树描述符和数据系数。

## 4. 资源与算力

论文中**未明确说明**使用的GPU型号、数量、训练时长等算力信息。实验仅涉及CPU上的树操作和小波变换（算法复杂度为线性），未提及深度学习相关算力消耗。因此资源与算力部分在文中属于未报告状态。

## 5. 实验数量与充分性

- **实验数量**：
    - Thingi10K：对4,166个模型进行了7个分辨率水平（ℓ=2~7）的测试，每个模型记录OpenVDB、plain omnitree、downsplit omnitree三种存储的系数数量和文件大小。统计了均值、中位数、四分之一/四分之三分位数、极值。
    - Disney云：对20个阈值（ε=10^(-6)到1）进行了有损压缩实验，报告了三种误差范数（L1、L2、L∞）的相对误差、剩余系数数量、文件大小（含原始和blosc2压缩）。
- **充分性评估**：
    - 非常充分：大规模数据集（4,166个模型）覆盖了各种形状的3D对象，统计图表清晰展示了中位数、均值、分位数，具有较强的统计意义。
    - 消融实验：比较了“纯粗化” vs “向下分裂+粗化”，以及“原始存储” vs “blosc2压缩”，验证了向下分裂的边际收益和blosc2压缩的叠加效果。
    - 公平性：OpenVDB使用了其默认的prune和blosc2压缩，omnitree也使用了相同的blosc2选项，节点数统计均为实际存储的叶节点/系数数量，比较方式直接。
    - 不足：未进行真实应用程序（如渲染、模拟）的运行时间对比（作者已说明运行时比较需要高性能实现，超出本文范围）；未对更高阶小波（如CDF、Alpert）进行实验。

## 6. 论文的主要结论与发现

1. **系数数量**：对于Thingi10K，在ℓ=7时，downsplit omnitree的中位数系数仅5,555，而OpenVDB为198,959，plain omnitree为21,990；中位数比值分别为97.21%和89.00%的减少。
2. **文件大小**：在ℓ=7时，downsplit omnitree（blosc2压缩）的中位数文件大小为3,222字节，OpenVDB为88,988字节，节省约27.6倍；最大节省达28倍。
3. **渐近增长率**：向下分裂配合粗化后，近似速率r_a(7)=0.51，明显低于四叉树理论值0.60和OpenVDB的0.63，说明渐近节省超越理论预期。
4. **有损压缩**：Disney云在阈值ε=0.1时，视觉质量几乎无下降，downsplit omnitree文件大小比OpenVDB减少约4.9倍。
5. **矩守恒**：在所有有损压缩实验中，质量（一阶矩）守恒在10^(-8)以内。
6. 全向树在任何单个对象上从未比OpenVDB需要更多的系数；对于扁平立方体等高度各向异性对象，比值可达15,472倍。

## 7. 优点

- **方法创新**：首次将粗化和向下分裂操作引入全向树，结合小波系数实现各向异性自适应压缩，突破了八叉树和k-d树的限制。
- **理论严密**：给出了粗化、向下分裂的数学定义和小波系数重分配公式；推导了误差上界，并证明粗化可无损压缩零细节系数的节点。
- **实验扎实**：使用超4,000个形状和大型连续云数据，统计手段充分（中位数、分位数、均值），对比OpenVDB这一工业标准，结果具有说服力。
- **实用价值**：在损失可忽略的情况下，存储节省可达10倍以上；且方案可扩展到更高阶小波和多维数据（4-6维），具有广泛的应用前景。

## 8. 不足与局限

- **运行时性能未评估**：论文未提供算法执行时间对比，仅分析了存储效率；高精度逐级小波变换和树重构的开销可能在实际应用中影响可用性（作者已承认需高性能实现）。
- **启发式策略简单**：向下分裂的维度选择仅基于一维细节系数绝对值最小，缺乏更优的全局优化（如动态规划），可能遗漏更好的分裂-粗化组合。
- **仅测试Haar小波**：最低阶小波的逼近阶有限，未试验更高阶（CDF、Alpert多小波）以提升压缩率或守恒阶数。
- **数据集局限**：仅测试了轴对齐的各向异性（3D形状和云），未涉及旋转各向异性或高维（4D+）数据，实验场景尚不够多样。
- **文件格式未优化**：omnitree的存储格式仍为实验性（numpy二进制+描述符blosc2），未设计针对性的编码方案以进一步压缩描述符（占比较高）。
- **与NeuralVDB比较缺失**：文中提到NeuralVDB可实现10-100倍压缩，但未与之直接对比；仅做了与OpenVDB的基线比较，后者不含神经网络压缩。

（完）
