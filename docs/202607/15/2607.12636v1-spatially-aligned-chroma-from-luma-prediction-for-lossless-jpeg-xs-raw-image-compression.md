---
title: Spatially-Aligned Chroma from Luma Prediction for Lossless JPEG XS Raw Image Compression
title_zh: 用于无损JPEG XS原始图像压缩的空间对齐的色度亮度预测
authors: "Taizo Suzuki, Soma Yokota, Masaki Onuki"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12636v1"
tags: ["query:compress"]
score: 10.0
evidence: 提出CfL增强的Star-Tetrix变换用于JPEG XS原始图像压缩，直接涉及变换编码
tldr: JPEG XS无损原始图像压缩中，直接应用色度亮度预测（CfL）因缺乏空间对齐导致性能不稳定。本文提出CfL增强的Star-Tetrix变换（CfL-STT），通过线性插值使亮度样本与色度采样网格对齐，抑制高频传感器噪声并保持跨通道相关性。实验表明，CfL-STT在多个原始图像数据集上一致提升编码效率，且保持精确可逆性，为原始图像压缩提供了高效的颜色去空间方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12636v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 307, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12636v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1750, \"height\": 219, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12636v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 226, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12636v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 847, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12636v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 451, \"label\": \"Table\"}]"
motivation: 直接CfL预测因空间不对齐导致原始图像压缩性能不稳定甚至下降，需针对CFA采样特性改进。
method: 提出CfL-STT，通过线性插值对齐亮度样本并集成至Star-Tetrix变换中预测色度分量。
result: CfL-STT在无损压缩中一致提升编码效率，优于直接CfL和原STT，且保持精确可逆性。
conclusion: 空间对齐策略有效利用跨通道相关性并抑制噪声，为JPEG XS原始图像压缩提供更优颜色空间变换。
---

## 摘要
本研究提出了一种增强的Chroma from Luma (CfL) Star-Tetrix变换（STT），称为CfL-STT，用于改善JPEG XS中的原始图像压缩。所提出的CfL-STT将CfL预测集成到STT中，以从CFA采样的原始图像的亮度分量预测色度分量。与为全彩色图像设计的传统CfL预测不同，所提出的方法采用通过沿水平和垂直方向进行线性插值获得的空间对齐亮度样本，以匹配色度采样网格。这种空间对齐抑制了高频传感器噪声，同时保留了跨通道相关性，从而产生了更去相关的Y-Delta-Du-Dv色彩空间。该方法已在JPEG XS参考软件中实现，并在原始图像数据集上进行了评估。实验结果表明，直接应用CfL预测会产生图像依赖的性能，并且由于缺乏空间对齐可能降低编码效率，而所提出的CfL-STT在无损原始图像压缩中始终提高编码效率，同时保持精确的可逆性。

## Abstract
This study proposes a Chroma from Luma (CfL)-enhanced Star-Tetrix transform (STT), referred to as CfL-STT, for improving raw image compression in JPEG XS. The proposed CfL-STT integrates CfL prediction into the STT to predict chroma components from the luma component in CFA-sampled raw images. Unlike conventional CfL prediction designed for full-color images, the proposed method employs spatially aligned luma samples obtained via linear interpolation along the horizontal and vertical directions to match the chroma sampling grid. This spatial alignment suppresses high-frequency sensor noise while preserving cross-channel correlation, resulting in a more decorrelated Y-Delta-Du-Dv color space. The proposed method was implemented in the JPEG XS reference software and evaluated on raw image datasets. Experimental results demonstrate that a direct application of CfL prediction yields image-dependent performance and may degrade coding efficiency due to the lack of spatial alignment, whereas the proposed CfL-STT consistently improves coding efficiency in lossless raw image compression while preserving exact reversibility.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在JPEG XS 2nd Edition中，原始图像压缩（CFA采样，如Bayer模式）采用Star-Tetrix变换（STT）进行颜色空间去相关。然而，AV1、JPEG XL等标准中使用的Chroma from Luma（CfL）预测虽能进一步降低色度冗余，但传统CfL假定亮度与色度样本共位，不适用于CFA原始图像，因为原始图像中色度采样位置与亮度采样位置存在空间错位，且传感器噪声非均匀。直接应用CfL会导致预测精度下降、编码性能不稳定甚至恶化。
- **整体含义**：为了在保持无损可逆性的前提下提升JPEG XS原始图像压缩效率，需要针对CFA采样结构设计空间对齐的CfL预测，将其集成到STT中，实现更高效的颜色去空间变换。

### 2. 论文提出的方法论

- **核心思想**：在STT输出的Y∆UV基础上，利用线性插值将亮度样本与色度采样网格对齐，然后对色度分量U和V进行线性回归预测，得到残差Du和Dv，最终将Y∆UV变换为Y∆DuDv，实现进一步去相关。
- **关键技术细节**：
  - **STT回顾**：STT通过四步提升实现：①根据相邻G样本双线性插值得到U、V；②用U、V加权更新G得到Y1、Y2（含白平衡）；③Y1、Y2间进行空间预测得到∆；④更新Y得到最终Y。所有操作整数可逆。
  - **空间对齐的CfL预测**：定义对齐亮度信号Yu = (Y_left + Y_right)/2，Yv = (Y_top + Y_bottom)/2，使亮度样本与色度采样网格对齐（U位于水平相邻Y之间，V位于垂直相邻Y之间）。然后对每个色度分量C ∈ {U, V}，使用最小二乘法估计回归参数α*_C、β*_C，预测êC* = α*_C Y_C + β*_C，残差D*_C = C - floor(êC*)。
  - **与直接CfL区别**：直接CfL使用每个宏像素内最近邻亮度样本（相当于Yu=Y_left, Yv=Y_bottom），缺乏空间对齐，导致性能波动。
- **算法流程**：编码端先执行STT得到Y、∆、U、V → 计算空间对齐亮度Yu、Yv → 对U、V分别计算回归参数 → 生成残差D*u、D*v → 将Y、∆、D*u、D*v及参数（作为副信息）送入熵编码。解码端反向操作恢复U、V。

### 3. 实验设计

- **数据集**：两个原始图像数据集：
  - MIT-Adobe FiveK数据集：10张DNG图像（编号#0500~#5000）。
  - iPhone SE3拍摄数据集：10张DNG图像（m0001~m0010）。
  - 位深：12或14比特每样本。
- **Benchmark/baseline**：JPEG XS参考软件中标准STT（基线）。对比方法：直接CfL-STT（无空间对齐）和所提对齐CfL-STT。
- **评价指标**：无损压缩比特率（bpp，相对于原始CFA图像）。副信息（四个帧级回归参数）包含在内。
- **附加实验**：模拟编码端线组级回归（每16个分量行估计一次参数），评估局部系数估计下的性能（开销约10⁻³ bpp）。

### 4. 资源与算力

- **文中未明确提及GPU型号、数量或训练时长**。论文强调所提方法仅涉及线性插值、简单累加及最小二乘计算，计算开销低，无需深度模型训练。因此未报告算力资源。

### 5. 实验数量与充分性

- **实验数量**：共20张测试图像（每数据集10张），对比三种方法（STT、直接CfL-STT、对齐CfL-STT），另有一组线组级模拟实验。未进行消融实验（如对齐策略选择、不同插值方式等未实验），也未进行有损压缩实验。
- **充分性与客观性**：
  - **优点**：覆盖了不同场景（MIT-Adobe FiveK和iPhone SE3），基线对比明确，展示了统计结果（平均bpp）以及单张图像结果（表I），并提供了伪彩色可视化（图4）佐证去相关效果。
  - **不足**：图像数量较少（共20张），仅测试无损模式，未涉及有损压缩或量化影响。未与最新学习类方法对比（但论文指出学习类方法针对语义压缩不适用）。线组级实验仅做了简单模拟，未实现完整码流级集成。总体而言实验设计合理但覆盖不够全面，结论有一定的局限性。

### 6. 论文的主要结论与发现

- 直接应用传统CfL预测（无空间对齐）在原始图像压缩中产生图像依赖行为，部分图像编码效率甚至低于基线STT。
- 所提空间对齐CfL-STT在所有测试图像上一致优于基线STT，两个数据集的平均bpp分别从7.00降至6.94（降0.06 bpp）和从7.29降至7.16（降0.13 bpp）。
- 空间对齐通过线性插值引入隐式低通滤波，抑制高频传感器噪声，同时保持跨通道相关性，使Y∆DuDv表示更去相关。
- 线组级模拟表明，局部系数估计下仍能保持编码增益，且副信息开销可忽略。
- 所提方法完全可逆，满足无损压缩要求。

### 7. 优点

- **方法创新性强**：针对CFA原始图像特性，将空间对齐概念引入CfL预测，解决了传统跨通道预测在原始域失效的问题。
- **理论合理**：通过分析亮度与色度采样网格的几何关系，提出简单的线性插值对齐，既降低计算复杂度又抑制噪声，有效提升去相关效果。
- **实用性高**：完全基于整数提升结构，与JPEG XS低延迟、低复杂度的设计目标兼容；仅需少量额外计算（两抽头插值、标量预测），解码端无需参数估计，适合硬件实现。
- **实验充分验证优势**：在所有测试图像上一致优于基线，而非个别优势；可视化进一步证实残差分布更集中。

### 8. 不足与局限

- **实验覆盖有限**：仅使用20张图像，且均为Bayer CFA，未覆盖其他CFA模式（如X-Trans、Quad Bayer）或其他原始格式；未测试有损压缩场景，无法评估在量化噪声下的表现。
- **缺乏与近期方法的深入对比**：未与H.266/VVC中的跨分量线性模型预测、基于学习的原始图像压缩方法（如CAMRA、深度编码器）进行对比，尽管论文指出它们目标不同，但至少可比较无损压缩性能。
- **空间对齐策略未优化**：仅使用简单的双线性插值，未探索更精细的对齐方法（如方向自适应插值、基于梯度的插值），也未分析不同插值的影响。
- **线组级模拟不完整**：仅模拟了参数每16行估计一次，未实现实际码流级封装，未考虑JPEG XS速率分配器的交互。
- **潜在偏差风险**：测试图像的选择可能偏向于易于压缩的场景，且位深仅有12/14比特，未覆盖更低位深或更高位深图像。

（完）
