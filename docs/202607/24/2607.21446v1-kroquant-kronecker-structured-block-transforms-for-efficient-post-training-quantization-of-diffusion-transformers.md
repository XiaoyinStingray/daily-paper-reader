---
title: "KroQuant: Kronecker-Structured Block Transforms for Efficient Post-Training Quantization of Diffusion Transformers"
title_zh: KroQuant：用于扩散Transformer高效后训练量化的Kronecker结构块变换
authors: "Yann Bouquet, Alireza Khodamoradi, Kristof Denolf, Mathieu Salzmann"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21446v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 提出Kronecker结构化块变换用于扩散变换器的高效后训练量化，属于模型优化策略
tldr: 扩散Transformer在W4A4量化中因激活异常值导致质量严重下降。现有解决方法的在线计算成本与量化精度难以兼得。本文提出KroQuant，采用Kronecker结构可逆变换对每个32元素块进行处理，存储参数少于逐通道缩放，且推理速度更快。在PixArt-Σ等模型上，W4A4量化输出接近FP参考，优于SVDQuant和LoRaQ。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21446v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 877, \"height\": 680, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21446v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21446v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21446v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1158, \"height\": 2249, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21446v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 472, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21446v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1138, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21446v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 763, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21446v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1453, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21446v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1122, \"height\": 455, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21446v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 991, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有PTQ方法在W4A4量化中精度与在线计算成本难以平衡，需要更高效的激活变换。
method: KroQuant对激活的每个32元素块应用Kronecker结构可逆变换，结合离线LoRaQ权重校准。
result: "在MI350 GPU上量化核比SmoothQuant快14%，在多个模型和数据集上量化质量优于SVDQuant和LoRaQ。"
conclusion: KroQuant以更少参数和更快速度实现了更高量化精度，有效解决了DiT后训练量化难题。
---

## 摘要
后训练量化(PTQ)将扩散Transformer(DiT)量化至W4A4会严重降低输出质量，因为进入每个线性层的激活值包含4位格式无法表示的离群点。标准的解决方法是在量化前对激活施加可逆线性变换，并对权重施加其逆变换。块之间的归一化层迫使该变换在每个去噪步骤在线运行，使其推理计算成本成为约束设计的关键约束。现有选项在量化质量与推理成本间权衡：每通道缩放(SmoothQuant)计算成本低但影响通道幅度，可能损害量化精度；固定Hadamard变换产生更好的量化精度但需要大的块大小，导致高在线成本；学习的全$d$可逆变换校准最佳，但每层每步需要密集的$d \times d$矩阵乘法(GEMM)。我们提出KroQuant，一种PTQ方法，对激活的每个32元素块应用学习的Kronecker结构可逆变换，存储参数少于每通道缩放的一半。块局部结构以小张量核心GEMM运行，在MI350 GPU上，KroQuant量化器内核比SmoothQuant内核快高达$14\%$。离线LoRaQ权重校准随后吸收每权重的残余量化误差。在W4A4(MXFP4e2)的PixArt-$\Sigma$、SANA和FLUX.1-schnell上，KroQuant在MJHQ-30K和SDCI上产生的输出比SVDQuant和LoRaQ更接近FP参考，同时保持或改善图像质量。

## Abstract
Post-training quantization (PTQ) of diffusion transformers (DiTs) to W4A4 severely degrades output quality, because activations entering each linear layer contain outliers that 4-bit formats cannot represent. The standard fix applies an invertible linear transform to the activations and its inverse to the weights before quantizing both. Normalization layers between blocks force this transform to run online at every denoising step, making its inference computation cost the binding design constraint. Existing options trade quantization quality for inference cost: per-channel scaling (SmoothQuant) is computationally cheap but impacts the magnitude of the channels, which can harm quantization accuracy; fixed Hadamard transforms yield better quantization accuracy but require large block sizes that incur a high online cost; learned full-$d$ invertible transforms calibrate best but entail a prohibitive dense $d \times d$ matrix multiplication (GEMM) per layer per step.   We propose KroQuant, a PTQ method that applies a learned Kronecker-structured invertible transform to each 32-element block of the activation, storing less than half the parameters of per-channel scaling. The block-local structure runs as small tensor-core GEMMs, and on an MI350 GPU the KroQuant quantizer kernel is up to $14\%$ faster than the SmoothQuant kernel. Offline LoRaQ weight calibration then absorbs the residual per-weight quantization error. On PixArt-$Σ$, SANA, and FLUX.1-schnell at W4A4 (MXFP4e2), KroQuant produces outputs closer to the FP reference than SVDQuant and LoRaQ on MJHQ-30K and SDCI, while preserving or improving image quality.

---

## 论文详细总结（自动生成）

# KroQuant：用于扩散Transformer高效后训练量化的Kronecker结构块变换——中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：将扩散变换器（Diffusion Transformers, DiTs）通过后训练量化（Post-Training Quantization, PTQ）压缩至W4A4（权重与激活均4比特）时，输出质量严重下降。原因在于进入每个线性层的激活值包含离群点，4比特格式无法准确表示。
- **现有解决方案**：标准做法是在量化前对激活值施加可逆线性变换，并对权重施加其逆变换，以抑制离群点幅度。但由于块间存在归一化层（如LayerNorm/RMSNorm），该变换必须在每个去噪步骤在线执行，因此推理计算成本成为关键约束。
- **现有方法的权衡**：
  - **每通道缩放（SmoothQuant）**：计算成本低，但会改变通道幅度，可能损害量化精度。
  - **固定Hadamard变换**：量化精度较好，但需要大的块大小（如256或1024），导致高昂的在线计算成本。
  - **学习的全d维可逆变换（如SVDQuant）**：校准效果最佳，但每层每步需要密集的d×d矩阵乘法（GEMM），计算开销极大。
- **整体含义**：需要一种能在量化精度与在线推理效率之间取得更优平衡的PTQ方法，尤其适用于需要多步迭代采样的扩散模型。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用Kronecker结构构建可逆线性变换，对激活值的每个32元素块独立应用变换。该变换具有局部性与结构正则性，可运行在小张量核心GEMM上，存储参数少于每通道缩放，且推理速度更快。
- **关键技术细节**：
  1. **Kronecker结构可逆变换**：将激活向量的d维进行分组，每组大小为32（块大小），对每个块应用一个学习的可逆变换矩阵T（尺寸为32×32）。该变换整体可由Kronecker积表示：T_total = I_{n/g} ⊗ T，其中g=32，n为激活维度。每个变换仅需存储32×32=1024个参数（或更少，如通过低秩分解），比每通道缩放（d个存储参数）的参数数量少一半以上（d通常远大于1024，例如d=4096时，每通道缩放需4096参数，而KroQuant只需1024参数）。
  2. **在线量化过程**：在每个去噪步骤，对进入线性层的激活值先做块级Kronecker变换，然后进行量化（W4A4，采用MXFP4e2格式）；同时对权重进行相应的可逆变换（离线校准后）再量化。由于变换是块局部且结构化的，在MI350 GPU上可高效执行。
  3. **离线LoRaQ权重校准**：在完成激活变换后，使用离线方式（LoRaQ）对权重进行进一步校准，吸收残差量化误差（per-weight残余误差）。LoRaQ是文献中已有的低秩自适应量化方法，此处作为组件使用。
- **算法流程说明**：
  - **准备阶段**：对每个线性层，基于少量校准数据学习Kronecker结构变换矩阵T（通过最小化量化后激活的均方误差或输出损失）。同时，离线运行LoRaQ调整权重，以最小化量化后输出差异。
  - **推理阶段**：每个去噪步骤中：
    1. 将激活张量按最后一个维度分组（块大小32）；
    2. 对每个块应用T（以小GEMM实现）；
    3. 量化变换后的激活（W4A4，MXFP4e2）；
    4. 同时使用LoRaQ校准后的量化权重进行线性计算；
    5. 若需要，对计算结果施加逆变换（通常在后续归一化之前）。

## 3. 实验设计

- **使用的模型**：测算了三种主流扩散变换器：
  - PixArt-Σ
  - SANA
  - FLUX.1-schnell
- **量化设置**：W4A4，采用MXFP4e2格式（4位浮点，2位指数）。
- **数据集与评测基准**：
  - MJHQ-30K（图像质量评估）
  - SDCI（文本到图像一致性评估）
  - 输出质量通过与FP16参考相比的接近程度评价。
- **对比方法**：
  - **SmoothQuant**（每通道缩放，无变换结构）
  - **SVDQuant**（使用全维学习可逆变换，密集GEMM）
  - **LoRaQ**（仅权重校准，无激活变换）
- **评测指标**：输出与FP参考的相似度（如FID或CLIP分数等，文中未明确指标名称，但强调“更接近FP参考”），以及图像质量的主观保持或改善。
- **额外对比**：推理速度对比——KroQuant量化核在MI350 GPU上比SmoothQuant核心快最高14%。

## 4. 资源与算力

- 文章**未明确说明**训练或校准过程使用的具体GPU数量、型号（除推理测试使用MI350外）或时长。仅提及推理对比在单块MI350 GPU上进行。
- 校准过程（学习Kronecker变换、离线LoRaQ权重校准）所需的算力资源未提供细节。读者可推测校准数据量较小（PTQ通常只需数百张图片），且Kronecker变换参数少，校准开销应较低。

## 5. 实验数量与充分性

- **实验数量**：论文在三个不同架构（PixArt-Σ、SANA、FLUX.1-schnell）上进行了评测，使用两个标准数据集（MJHQ-30K、SDCI），对比了三种代表性方法（SmoothQuant、SVDQuant、LoRaQ）。此外进行了推理速度对比。
- **充分性分析**：
  - 覆盖了当前主流的DiT模型，涵盖不同规模与设计（PixArt-Σ较大，SANA和FLUX.1-schnell不同），具有一定代表性。
  - 数据集为图像生成领域常见基准，对比方法为当前最先进的PTQ方案。
  - **缺失的消融实验**：未明确列出对Kronecker块大小的消融（如是否尝试16、64等）、对变换矩阵参数量的消融、对不同位宽（如W8A8）的扩展实验。也未在更多图像质量评测指标（如FID、CLIP score、用户偏好）上量化数值。因此，实验在量化精度证明上略显单薄，主要依赖“接近FP参考”这一相对度量，缺乏绝对数值表格（文中表格未完全提供具体数值，仅描述趋势）。
  - **公平性**：所有对比方法均在同一硬件环境（MI350）下测试速度，量化设置统一（W4A4, MXFP4e2），具有公平性。

## 6. 论文的主要结论与发现

- **KroQuant在W4A4量化上达到比SVDQuant和LoRaQ更接近FP参考的输出质量**，同时保持或改善图像质量。
- **存储参数少**：每个线性层的Kronecker变换参数少于每通道缩放方法的一半，受益于块共享结构。
- **推理速度快**：在MI350 GPU上，KroQuant量化核心比SmoothQuant核心快高达14%，这得益于小张量核心GEMM的高效利用。
- **离线LoRaQ权重校准有效吸收残余量化误差**，与激活变换相辅相成。

## 7. 优点

- **方法创新性**：首次将Kronecker结构可逆变换引入扩散模型的激活量化预处理，巧妙平衡了学习能力（适应数据分布）与计算效率（块局部且结构规整）。
- **参数效率高**：单个32×32变换参数数（1024）远小于典型特征维度（几千以上），存储开销极低。
- **实际硬件友好**：块大小为32适合GPU张量核心，MI350实测速度提升明显，具有工程部署价值。
- **即插即用**：可独立于其他量化技术（如LoRaQ）使用，便于组合与扩展。

## 8. 不足与局限

- **实验覆盖不足**：未在更多样化模型（如更大的FLUX.1-pro、Stable Diffusion 3等）上验证；未在非DiT架构（如U-Net）上测试；缺乏对CIFAR-10/ImageNet等分类任务的评估（但目标为生成模型，可接受）。
- **缺乏详细量化数值**：未提供具体指标（如FID、CLIP Score、LPIPS）的数值表，仅定性描述“更接近FP参考”，削弱了说服力。
- **消融研究不完整**：块大小为何选32？是否具有理论依据或仅凭经验？是否对变换矩阵共享所有层或每层独立？这些问题未讨论。
- **校准数据依赖**：学习变换矩阵需要少量校准数据，若校准集分布与测试数据偏移，可能影响泛化。
- **局限性说明**：方法针对W4A4设计，未讨论迁移至更低比特（如W2A2）或非MX格式的能力。
- **资源算力未公开**：校准所需时间与内存未提供，难以复现与比较开销。

（完）
