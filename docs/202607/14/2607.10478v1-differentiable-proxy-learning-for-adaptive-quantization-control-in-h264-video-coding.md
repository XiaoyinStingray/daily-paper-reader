---
title: Differentiable Proxy Learning for Adaptive Quantization Control in H.264 Video Coding
title_zh: 可微代理学习用于H.264视频编码中的自适应量化控制
authors: "Qihan Xu, Ivan V. Bajić"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10478v1"
tags: ["query:compress"]
score: 9.0
evidence: 通过可微代理实现H.264编码中的自适应量化控制，直接涉及图像/视频压缩中的量化策略
tldr: "H.264视频编码中量化参数优化因编解码器不可微而困难。本文提出一种可微分代理学习方法，基于可变率学习压缩模型和软索引机制，使代理对QP可微，并训练其近似H.264的率失真行为。实验表明代理准确，用于自适应量化框架在语义分割和MS-SSIM上分别取得17.12%和15.30%的BD-rate降低。该方法为视频编码优化提供了新途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10478v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 915, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1601, \"height\": 490, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 714, \"height\": 333, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 697, \"height\": 1054, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1871, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 785, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 684, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 689, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1415, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10478v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 645, \"height\": 639, \"label\": \"Figure\"}]"
motivation: H.264编码器不可微，导致优化量化参数以适配特定目标（如感知质量或机器视觉）极为困难。
method: 基于可变率学习压缩模型构建可微分代理，通过软索引机制使其对QP可微，并训练代理逼近H.264的率失真行为。
result: "代理准确模拟H.264全局QP和空间QP的率失真，在语义分割和MS-SSIM上分别实现17.12%和15.30%的BD-rate降低。"
conclusion: 可微分代理学习有效实现自适应量化控制，显著改善率-任务权衡，拓展了视频编码优化方法。
---

## 摘要
H.264因其相对简单、高效以及软件和硬件实现的广泛可用性，在过去二十年中一直是最广泛使用的视频编码格式。然而，由于标准视频编解码器的非可微性质，针对特定目标（如感知质量或机器视觉任务）优化编解码器参数（如量化参数QP）具有挑战性。尽管最近已使用可微代理来围绕标准编解码器实现基于梯度的优化，但很少明确表征其对目标编解码器的保真度。本文提出了一种针对H.264帧内编解码器的可微代理学习方法，以实现自适应量化控制。基于可变速率学习压缩模型，所提出的代理通过软索引机制使其相对于编解码器QP可微。然后训练该代理以近似H.264在两种量化设置下的率失真行为：全局QP（每张图像使用一个QP）和空间QP（在宏块级别分配QP）。使用冻结的训练代理，我们开发了基于代理的自适应量化（AQ）框架，用于感知优化和机器视觉任务。实验结果表明，所提出的代理紧密近似了H.264帧内编解码器的率失真行为。由此产生的基于代理的AQ框架在固定QP的H.264基线基础上持续改善了率-任务权衡，实现了语义分割的BD率降低高达17.12%，MS-SSIM降低高达15.30%。

## Abstract
H.264 has been the most widely used video coding format for the past two decades due to its relative simplicity, efficiency, and wide availability of software and hardware implementations. However, optimizing codec parameters such as the quantization parameter (QP) for specific objectives (e.g., perceptual quality or machine vision tasks) is challenging due to the non-differentiable nature of standard video codecs. While differentiable proxies have recently been used to enable gradient-based optimization around standard codecs, their fidelity to the target codec is rarely explicitly characterized. In this paper, we propose a differentiable proxy learning method for H.264 intra codec to enable adaptive quantization control. Built upon a variable-rate learned compression model, the proposed proxy is made differentiable with respect to codec QP through a soft-indexing mechanism. It is then trained to approximate the rate-distortion behavior of H.264 under two quantization settings: global-QP, which uses one QP per image, and spatial-QP, which assigns QPs at the macroblock level. Using the frozen trained proxy, we develop a proxy-based adaptive quantization (AQ) framework for both perceptual optimization and machine vision tasks. Experimental results demonstrate that the proposed proxies closely approximate the rate-distortion behavior of H.264 intra codec. The resulting proxy-based AQ framework consistently improves rate-task trade-offs over fixed-QP H.264 baselines, achieving BD-rate reduction of up to 17.12% for semantic segmentation and 15.30% for MS-SSIM.

---

## 论文详细总结（自动生成）

# 论文详细总结：可微代理学习用于H.264视频编码中的自适应量化控制

## 1. 论文的核心问题与整体含义

H.264作为应用最广泛的视频编码标准，其量化参数（QP）的优化对于提升感知质量或机器视觉任务性能至关重要。然而，标准编解码器是非可微的，导致无法通过梯度下降直接优化QP分配，因而难以实现自适应量化（AQ）。以往的解决方案（如可微代理）多将代理作为辅助模块用于预处理或后处理优化，较少关注代理本身对目标编解码器的保真度。本文首次系统性地研究了可微代理的保真度，并提出了一个基于可微代理的端到端AQ框架，在全局QP和空间QP两种设置下，实现了对H.264帧内编解码器的率失真行为的高保真近似，并成功用于感知优化和机器视觉任务（目标检测、语义分割），显著改善了率-任务权衡。

## 2. 方法论

### 核心思想
构建一个基于可变速率学习压缩模型（DCVC-FM帧内模型）的可微代理，通过软索引机制使代理的输出对编解码器QP可微，然后训练代理逼近真实H.264编解码器的重构图像和比特率。训练完成后，冻结代理，用它提供可微的率失真信号，训练一个AQ网络，该网络预测QP配置（全局标量或空间QP图），进而优化下游任务损失与率的加权和。

### 关键技术细节
1. **可微代理结构**：基于DCVC-FM，但引入软索引机制替代离散的索引选择。对于宏块位置$(u,v)$，控制参数$q_{u,v}$，软分配权重为：
   $$
   w_{u,v,i} = \frac{\exp(-(q_{u,v}-i)^2/\tau)}{\sum_j \exp(-(q_{u,v}-j)^2/\tau)},
   $$
   最终缩放因子 $s_{u,v} = \sum_i w_{u,v,i} s_i$，实现连续可微。
2. **QP映射**：由于代理控制参数与编解码器QP数值不同，在BPP-PSNR空间上建立映射，基于Kodak数据集统计得到。
3. **代理训练目标**：
   $$
   \mathcal{L}_p = D_R(R_c, R_p) + \lambda D_X(\hat{X}_c, \hat{X}_p),
   $$
   其中$D_R$为绝对差值，$D_X$为MSE，$\lambda$由$q$计算得到（含可调乘数$\alpha$）。
4. **全局QP代理**：预定义锚点QP集合 $\{35,40,45,51\}$，训练时随机采样。
5. **空间QP代理**：预定义五组均匀分布（如$\mathcal{U}[20,30], \dots, \mathcal{U}[40,51]$），对每个宏块独立采样QP，代理训练时$\lambda$取所有宏块的均值$\bar{\lambda}$。
6. **AQ网络设计**：
   - 全局AQ：轻量CNN（3个步长卷积 + 全局平均池化 + 全连接），输出单个QP。
   - 空间AQ：全卷积编码器，输出宏块级QP logits，并用$\beta$依赖的调制因子 $s(\beta)=1+\alpha\tanh(w\beta+b)$ 进行调节，最后通过sigmoid缩放至有效QP范围。

## 3. 实验设计

### 数据集与场景
- **代理训练**：Vimeo-90k数据集（仅第一帧），使用JM 19.0生成YUV420格式的压缩帧，转换YUV444输入代理。
- **代理评估**：Kodak数据集（24张图像）。
- **AQ训练与测试**：
  - 机器视觉任务：COCO（目标检测，YOLOv5 S/M/L）；Pascal VOC（语义分割，DeepLabv3 ResNet-50）。
  - 感知优化：Vimeo-90k（训练空间AQ），Kodak（测试MS-SSIM）。

### Benchmark与对比方法
- 基线均为固定QP的H.264帧内编码（JM 19.0）。
- 对比自身变体：全局QP AQ vs 空间QP AQ（在语义分割任务中比较）。
- 未与其他AQ方法（如未使用代理的方法）进行外部对比，主要聚焦于展示代理的有效性以及相比固定QP的改进。

## 4. 资源与算力

文中仅提到代理训练使用PyTorch、Adam优化器、学习率$1\times10^{-5}$、batch size 4，在单块NVIDIA 2080 Ti GPU上训练。未明确说明训练时长、AQ网络训练资源及总体GPU小时数。整体算力消耗属于中等水平。

## 5. 实验数量与充分性

- **代理保真度评估**：
  - 全局QP代理：测试了QP范围[28,51]内的率失真曲线（连续点），计算BD-rate。
  - 空间QP代理：测试五组锚点分布。
  - 视觉对比：图5展示了两个低/高码率的例子。
- **AQ评估**：
  - 全局QP AQ：目标检测（3种模型尺寸，各6个β值），感知MS-SSIM（1个β扫描）。
  - 空间QP AQ：语义分割（1个β扫描），感知MS-SSIM（1个β扫描）。
  - 每个实验提供率-任务曲线和BD-rate数值。
- **消融实验**：未明确设置消融实验（如代理保真度对AQ性能的影响），但通过对比全局QP与空间QP的结果间接说明了更细粒度控制的优势。
- **公平性**：所有率-任务曲线均在同一码率范围内比较，使用BD-rate评价，较为客观。但缺少与现有其他AQ方法（如基于传统优化的快速AQ）的对比，充分性略有不足。

## 6. 主要结论与发现

1. 所提可微代理能高保真近似H.264帧内编解码器的率失真行为，全局QP代理BD-rate（PSNR）为-4.75%，空间QP代理因复杂性更高，保真度略低（PSNR BD-rate -15.01%）。
2. 基于代理的AQ框架优于固定QP基线：
   - 目标检测（YOLOv5）：BD-rate降低8.19%~9.50%。
   - 语义分割：全局QP AQ降低11.99%，空间QP AQ降低17.12%。
   - 感知MS-SSIM：全局QP AQ降低3.75%，空间QP AQ降低15.30%。
3. 空间QP控制比全局QP控制带来额外增益，证明更细粒度分配的潜力。
4. 高保真代理虽好，但即使代理不够精确（如空间QP代理保真度较低），仍能实现有效的AQ优化，说明代理误差具有良好的鲁棒性。

## 7. 优点

- **系统性保真度分析**：首次明确评估可微代理对目标编解码器的率失真和视觉保真度，并揭示其与AQ性能的关系。
- **创新的软索引机制**：将离散QP映射到连续可微空间，无需改变代理架构。
- **双设置支持**：同时覆盖全局和空间QP，提供了灵活的自适应粒度。
- **任务全面**：涵盖机器视觉（检测、分割）和感知优化，验证了通用性。
- **结果显著**：在语义分割上达到17.12% BD-rate降低，效果突出。

## 8. 不足与局限

- **实验覆盖不足**：
  - 未与现有AQ方法（如x264的VAQ、JM内置AQ）进行直接比较，基准仅为固定QP。
  - 只在帧内编码上验证，未延拓到帧间编码（作者也提到是未来方向）。
  - 机器视觉任务仅测试了YOLOv5和DeepLabv3，未涵盖更多架构（如Transformer）。
- **保真度与AQ性能的定量关系未深入**：缺乏消融实验考察代理保真度变化（如控制τ、α）对AQ最终性能的影响。
- **训练资源信息不完整**：缺少训练时长、AQ网络训练时间等细节，不利于复现。
- **泛化性风险**：代理训练和AQ训练数据集较小（Vimeo-90k），在大规模多样化视频上的鲁棒性存疑。
- **空间QP生成方式简化**：仅用均匀分布覆盖，未模拟真实内容自适应的QP分布，可能限制代理在真实场景中的泛化。

（完）
