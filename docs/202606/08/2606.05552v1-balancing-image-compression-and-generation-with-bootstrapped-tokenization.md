---
title: Balancing Image Compression and Generation with Bootstrapped Tokenization
title_zh: 通过自引导分词实现图像压缩与生成的平衡
authors: "Haozhe Chi, Jinghan Li, Hao Jiang, Wu Sheng, Yi Ma, Jing Wang, Yadong Mu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05552v1"
tags: ["query:compress"]
score: 8.0
evidence: 通过自引导令牌化实现图像压缩与生成的平衡
tldr: "标准图像分词在每个token中混合不同粒度信息，导致token间冗余并增加生成器训练难度。本文提出SelfBootTok，通过自引导学习将信息分解为全局和局部token组，令生成器仅依赖全局token预测局部细节，计算量减少约40%。该方法仅用64个token即实现gFID 1.56的新纪录，同时提升重建与生成质量。该范式通过自监督扩展有效平衡压缩与生成，为图像分词提供了高效新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1019, \"height\": 1016}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1367, \"height\": 693}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1386, \"height\": 660}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1021, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 671, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 679, \"height\": 413}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05552v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1285, \"height\": 988}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 653, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 374}]"
motivation: 标准图像分词混合粒度导致冗余，生成器训练困难。
method: SelfBootTok将信息分解为全局与局部token，通过自引导学习令生成器仅从全局token预测局部细节。
result: "计算量减少40%，64个token时gFID达1.56，重建与生成质量更优。"
conclusion: 自引导范式平衡了压缩与生成，可扩展至更多数据与参数。
---

## 摘要
尽管图像分词取得了进展，但标准方法通过在每个词元中混合所有粒度来编码冗余信息，因此词元之间仍然存在冗余。不同粒度信息的混合也使生成器的训练复杂化。本文引入了SelfBootTok，该方法通过将信息清晰地分解为全局和局部词元组来解决这一问题。通过自引导学习，模型仅从全局词元预测局部细节，将视觉细节的负担从生成器转移到分词器。因此，我们的生成器效率更高，仅需全局词元即可减少约40%的计算量，同时提供更优的重建和生成。此外，这种范式可优雅地扩展：通过利用更多数据或参数来自监督局部表示学习，SelfBootTok仅使用64个词元就实现了新的最先进gFID评分1.56。

## Abstract
Despite progress in image tokenization, standard methods encode redundant information by mixing all granularities within each token, thus redundancy persists between tokens. The mix of information of different granularity also complicates the training of generators. This paper introduces SelfBootTok, a method that resolves this by cleanly decomposing information into global and local token groups. Through self-bootstrapped learning, the model predicts local details exclusively from global tokens, shifting the burden of visual details from the generator to the tokenizer. Consequently, our generator is far more efficient, requiring only global tokens and reducing computation by approximately 40%, while delivering superior reconstruction and generation. Moreover, this paradigm scales elegantly: by leveraging more data or parameters to self-supervise local representation learning, SelfBootTok achieves a new state-of-the-art gFID score of 1.56 using only 64 tokens.

---

## 论文详细总结（自动生成）

# 基于 SelfBootTok 论文的详细总结

## 1. 论文的核心问题与整体含义

- **研究动机**：现有图像分词方法（如 VQ-VAE、TiTok、SoftVQ）在每个 token 中混合了不同粒度（全局语义与局部细节）的信息，导致 token 间存在冗余，且生成器需要同时学习所有粒度的信息，训练复杂度高。
- **核心问题**：如何更高效地分解图像信息，让生成器只关注高层语义，而分词器负责恢复局部细节，从而平衡压缩比、生成质量与计算效率。
- **整体含义**：提出 SelfBootTok，通过自引导（self-bootstrapped）学习将视觉细节负担从生成器转移到分词器，实现仅用 64 个 token 达到 SOTA 生成效果（gFID 1.56），同时计算量减少约 40%。

## 2. 方法论

### 核心思想
- **全局-局部分解**：将图像 token 分为两组——全局 token（zG，捕获高层语义）和局部 token（zL1 和 zL2，捕获不同粒度的细节）。
- **自引导学习**：生成器只输出全局 token，局部 token 由分词器中的局部 aligner 从全局 token 自监督预测（通过 MLP 和因果 Transformer）。
- **对齐与优化**：使用软向量量化（Soft VQ）、多码本（global, local1D, local2D）以及最优传输（optimal transport）损失将 2D VAE 潜空间对齐到 1D token 序列。

### 关键技术细节
1. **编码与量化**：
   - 输入图像经 ViT 编码器得到 zG。
   - 通过自引导预测得到 zL1（MLP）和 zL2（因果 Transformer）。
   - 对每个 token 组使用独立的软量化码本，公式：
     \[
     q_\phi(Z|I) = \text{Softmax}(-\frac{1}{\tau}\|\hat{Z}-C\|^2), \quad Z = q_\phi(Z|I)C
     \]
   - 总损失：\( L = L_{\text{recon}} + \lambda_1 L_{\text{percep}} + \lambda_2 L_{\text{adv}} + \lambda_3 L_{\text{align}} + \lambda_4 L_{\text{KL}} \)
   - 对齐损失包含全局对齐（与 DINOv2 特征）、局部对齐（与 SigLIP2 或 I-JEPA）以及最优传输损失。

2. **最优传输对齐**：
   - 将 2D VAE 潜在图（尺寸 H×W）与 1D token 序列对齐，求解 Sinkhorn 正则化运输问题，损失为运输代价 ⟨P*, C⟩。

3. **自引导预测**：
   - 局部 1D tokens：\( z_{L1} = \text{MLP}(z_G) \)
   - 局部 2D tokens：自回归 \( p(z_{L2}|z_G) = \prod p(t_i^{\text{local}}|z_G, t_{<i}^{\text{local}}) \)，使用因果 Transformer。

4. **解码**：
   - 融合全局与局部 tokens（软残差融合或拼接），注入 decoder 的 cross-attention 层，重建图像。

5. **缩放策略**：
   - 两阶段训练：第一阶段用小局部 aligner 训练整体；第二阶段冻结编码器/全局部分，只缩放局部 2D aligner 并微调，同时生成器可仅训练一次。

## 3. 实验设计

- **数据集**：仅使用 ImageNet-1K，分辨率 256×256。
- **Benchmark**：比较了 2D tokenizer（VAE, SD-VAE, VAR-Tok, VQGAN, VA-VAE）、自回归 tokenizer（MaskGIT, LlamaGen, RQ-VAE, MAGE, MAR, ViTok）以及 1D tokenizer（TiTok, FlexTok, OneDpiece, HieraTok, GigaTok, MAETok, SoftVQ 等）。
- **对比指标**：rFID（重建 FID）、gFID（生成 FID，含/不含 CFG）、PSNR、SSIM。
- **生成模型**：使用 SiT 作为下游生成器（其余方法使用对应生成器）。

## 4. 资源与算力

- **硬件信息**：论文未明确说明使用的 GPU 型号和数量。
- **训练成本**：
  - 基准配置：173M 参数（ViT-base backbone），局部 aligner 从 10M 到 600M 缩放。
  - 两阶段训练可节省约 40% 计算量（从 4.9B 参数/60K GFLOPs 降至 2.95B/36.6K GFLOPs），训练时间从约 28 天降至约 13 天（对应 600M 局部 aligner 缩放实验）。
  - 生成器（SiT）训练 800K 迭代，仅需一次，后续缩放不需重新训练。

## 5. 实验数量与充分性

- **主要实验结果**：表 1 全面对比了多种 tokenizer 和生成模型。
- **消融实验**：
  - 表 2：三种局部 1D aligner（I-JEPA, DINOv2, SigLIP）对重建的影响。
  - 表 3：逐步添加全局、局部 1D/2D、因果解码、局部 codebook 的消融，验证设计有效性。
  - 图 5：渐进式加入局部信息对重建指标的影响。
  - 图 6：局部 2D aligner 从 10M 到 600M 缩放的重建/生成性能提升。
  - 融合策略对比（软残差 vs 拼接）在正文中提及但未给出表格，仅说明拼接重建好但生成差。
- **公平性**：
  - 与 SOTA 方法在同一数据集、同一指标下比较，且在 token 数量相当或更少的情况下取得更好结果。
  - 生成器统一使用 SiT，避免生成器差异带来的偏差。
- **充分性**：消融覆盖了各个模块，但缺乏跨数据集的泛化实验（如 COCO、FFHQ），且未见在文本到图像等任务上的评估。

## 6. 论文的主要结论与发现

1. **性能 SOTA**：SelfBootTok 在仅 64 token 下，gFID 达到 1.56（含 CFG），优于所有其他 1D tokenizer（SoftVQ 1.78，TiTok 2.48 等），与最佳 2D tokenizer 相当。
2. **计算高效**：生成器只需处理全局 token（64 个），计算量减少约 40%；缩放时生成器只训练一次，总训练时间减少约 54%。
3. **优雅可扩展**：局部 aligner 参数从 10M 到 600M 缩放时，重建和生成质量持续提升，且无需重训练生成器。
4. **全局-局部分解有效**：逐步加入局部 1D 和 2D token 可显著改善重建，且自引导预测设计使得生成器更简单。

## 7. 优点

- **创新性**：首次明确将 token 分解为全局/局部两组，并通过自引导预测局部细节，将生成器负担转移到分词器。
- **高效性**：生成器仅需 64 个全局 token，计算量小；缩放时只需扩展局部对齐器，节省大量资源。
- **并行训练**：分词器缩放与生成器可同时训练，互不干扰。
- **理论启发**：最优传输对齐 2D→1D 的设计具有通用性。

## 8. 不足与局限

- **数据集单一**：仅在 ImageNet 上评估，未验证在自然图像、文本到图像或其他领域（如医学、遥感）的泛化能力。
- **预印本状态**：论文为 2026 年 6 月预印本，未经过正式同行评审，部分实现细节（如码本大小、τ 值、训练超参数）可能未完全披露。
- **局部对齐器的选择**：局部 1D aligner 使用 I-JEPA 最佳，但对齐器来源不同可能导致公平性问题（是否与 SoftVQ 等使用相同 encoder）。
- **生成器依赖**：仅使用 SiT 作为生成器，未测试其他生成模型（如 DiT、MaskGIT 等）的兼容性。
- **理论分析不足**：未提供关于冗余减少的理论证明或信息论分析，更多是实验验证。
- **实际部署成本**：虽训练效率高，但最终模型可能因局部 aligner 较大而推理速度受影响（600M 参数）。

（完）
