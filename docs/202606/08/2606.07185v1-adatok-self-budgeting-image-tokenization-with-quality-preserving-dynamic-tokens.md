---
title: "AdaTok: Self-Budgeting Image Tokenization with Quality-Preserving Dynamic Tokens"
title_zh: "AdaTok: 具有质量保持动态令牌的自预算图像令牌化"
authors: "Xiaocheng Lu, Yuxi Chen, Jie Zhang, Jian Liu, Jingcai Guo, Fangqi Zhu, Tao Han, Song Guo"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07185v1"
tags: ["query:compress"]
score: 8.0
evidence: 用于图像压缩的自预算图像令牌化，动态分配令牌数
tldr: 当前图像tokenizer对所有图像使用固定数量token，但视觉复杂度差异大，导致简单图像浪费token、复杂图像不足。本文提出AdaTok，一种自预算离散1D tokenizer，通过优先表示学习与自适应token分配协同设计，使tokenizer自身决定每个图像所需token数。在ImageNet-1K上，自适应模式平均仅用118个token即达rFID1.50，且生成吞吐量提升2.1倍。该工作首次将token数量作为内容条件输出，而非固定超参数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07185v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07185v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1310, \"height\": 580, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07185v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 612, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07185v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 603, \"height\": 538, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07185v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07185v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07185v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 823, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07185v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 688, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07185v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 544, \"height\": 241, \"label\": \"Table\"}]"
motivation: 固定token预算无法适应图像复杂度差异，现有弹性方法需外部指定预算，缺乏自适应性。
method: 提出AdaTok，结合优先表示学习（嵌套尾部掩码和多头LoRA解码器）与自适应分配（轻量GRPO策略），并引入动态帕累托权重平衡保真度与效率。
result: AdaTok-Adaptive平均118个token达到rFID1.50，比固定256token节省一半以上，自回归生成吞吐量提升2.1倍。
conclusion: 证明视觉token数可作为内容条件输出学习，实现质量保持的动态预算分配。
---

## 摘要
图像令牌化方法，从2D网格到最近的1D序列，通常使用相同固定数量的令牌编码每个图像。然而视觉复杂度高度异质，因此统一预算在简单输入上过度分配，而在复杂输入上分配不足。现有的弹性令牌化方法允许可变长度重建，但通常将令牌长度视为部署时的操作点、搜索目标或外部预测，而非令牌化器自身的输出。在这项工作中，我们探讨离散视觉令牌化器能否一次性地自我预算。我们的核心发现是，可操作的弹性需要表示与分配的协同设计：前缀必须在不同预算下保持可解码性，并且令牌化器必须学习每个图像需要的前缀。我们提出了AdaTok，一种自预算的离散1D令牌化器。AdaTok结合了优先表示学习（通过嵌套尾部掩码对令牌排序，并通过多头LoRA解码器头解决依赖于预算的语义偏移）和自适应令牌分配（通过候选预算上的轻量级确定性分组GRPO策略）。动态Pareto加权在策略训练中平衡保真度和效率，无需手动权衡扫描。在ImageNet-1K上，AdaTok-Full在256个令牌时达到rFID 1.31，而AdaTok-Adaptive平均仅使用约118个令牌即达到rFID 1.50，在可比预算下优于离散1D基线。在自回归图像生成中，较短的适应表示相对于固定的256令牌解码实现了约2.1倍的吞吐量，这表明视觉令牌数量可以作为内容条件输出学习，而不是设置为固定超参数。

## Abstract
Image tokenizers, from 2D grids to recent 1D sequences, typically encode every image with the same fixed number of tokens. Yet visual complexity is highly heterogeneous, so a uniform budget overspends on simple inputs and underserves complex ones. Existing elastic tokenizers expose variable-length reconstructions, but often leave token length as a deployment-time operating point, a search target, or an external prediction rather than an output of the tokenizer itself. In this work, we ask whether a discrete visual tokenizer can budget itself in one pass. Our central finding is that actionable elasticity requires a representation--allocation co-design: prefixes must remain decodable across budgets, and the tokenizer must learn which prefix each image needs. We propose AdaTok, a self-budgeting discrete 1D tokenizer. AdaTok combines Prioritized Representation Learning, which orders tokens with nested tail masking and resolves budget-dependent semantic shift through Multi-Head LoRA decoder heads, with Adaptive Token Allocation, which trains a lightweight deterministic-group GRPO policy over candidate budgets. Dynamic Pareto Weighting balances fidelity and efficiency during policy training without manual trade-off sweeps. On ImageNet-1K, AdaTok-Full reaches rFID 1.31 at 256 tokens, while AdaTok-Adaptive attains rFID 1.50 using only ~118 tokens on average, outperforming discrete 1D baselines at comparable budgets. In autoregressive image generation, the shorter adaptive representation yields ~2.1x throughput over a fixed 256-token decode, suggesting that visual token count can be learned as a content-conditioned output rather than set as a fixed hyperparameter.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的图像令牌化器（如VQ-VAE、VQGAN、TiTok等）对所有图像使用**固定数量的离散令牌**，但图像的视觉复杂度差异巨大（例如简单背景vs密集街景）。固定预算导致简单图像浪费令牌、复杂图像质量不足，且自回归生成中序列长度与延迟成正比。
- **研究动机**：虽然已有弹性令牌化器（FlexTok、One-D-Piece）支持可变长度重建，但**令牌长度仍由外部指定**（如部署时的操作点、搜索目标或外部预测器），而非令牌化器自身的输出。论文希望让离散令牌化器**自主地、一次性地决定每幅图像所需的令牌数**，实现“自预算”。
- **整体含义**：将令牌数量转化为**内容条件的可学习输出**，从而在保持重建质量的前提下，显著降低计算开销（如生成吞吐量提升2.1倍），推动视觉令牌化向更高效、更智能的方向发展。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：弹性令牌化需要**表示学习与分配策略的协同设计**。AdaTok包含两个耦合模块：
  1. **优先表示学习（PRL）**：构建有序、可跨预算解码的1D令牌序列。
  2. **自适应令牌分配（ATA）**：轻量策略网络一次性地为每个图像选择最优令牌数。

- **关键技术细节**：
  - **PRL模块**：
    - **嵌套尾部掩码（Nested Tail Masking, NTM）**：训练时随机采样预算 \( l \in \{32,64,...,256\} \)，掩码掉第 \( l+1 \) 到 \( N \) 个令牌，迫使编码器将最信息密集的语义打包到前缀。
    - **多头LoRA（Multi-Head LoRA, MH-LoRA）**：为每个预算桶分配一个秩为16的LoRA头（共8个桶，仅增加~1.1%参数）。解码时，使用对应预算的LoRA头 \( \phi_k \) 调制共享基解码器 \( \Theta \)，解决同一前缀在不同预算下的语义偏移问题（公式1）。
    - **优化目标**：\( \mathcal{L}_{\text{PRL}} = \mathbb{E}_{l\in L} [\mathcal{L}_{\text{recon}} + \lambda_p \mathcal{L}_P + \lambda_g \mathcal{L}_G + \mathcal{L}_{\text{commit}}] \)（公式2），联合优化所有预算下的重建损失。

  - **ATA模块**：
    - **确定性分组GRPO**：将候选预算集 \( L \) 作为确定性组，对每个预算计算保真度奖励 \( r_{\text{mse}}(l) = - \text{MSE} \) 和效率奖励 \( r_{\text{len}}(l) = -l/l_{\max} \)。通过组内标准化（公式3）得到相对优势 \( A \)，避免采样方差，无需值网络。
    - **动态Pareto加权（DPW）**：根据每个目标（MSE、长度）的梯度影响动态调整权重（公式5），避免手动扫描 \( \lambda \)。
    - **训练管线**：采用两阶段训练（先训PRL至收敛，再冻结并训ATA），避免非平稳奖励和码本崩溃。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：ImageNet-1K，分辨率 256×256。重建指标在50K验证集上计算；生成指标（gFID）基于50K类条件样本，遵循ADM协议。
- **基准（Benchmark）**：对比重建质量（rFID、PSNR、SSIM、LPIPS）和生成质量（gFID），以及平均令牌数和吞吐量。
- **对比方法**：
  - **固定预算离散**：VQGAN、MaskGIT、LlamaGen、ImageFolder、VAR、TiTok-S/B/L。
  - **连续/自适应**：CAT、Semanticist（连续向量，位深度不同）。
  - **弹性离散**：FlexTok、ALIT、One-D-Piece。
  - **消融内部**：无NTM、无MH-LoRA、启发式分配、固定权重GRPO等。

### 4. 资源与算力

- **硬件**：8× NVIDIA H100 GPU。
- **训练时间**：
  - Stage 1（PRL）：200 epoch，AdamW优化器（lr 1e-4, weight decay 0.05, batch size 256）。
  - Stage 2（ATA）：10K步（约1小时，占总成本~3%），冻结令牌化器，训练轻量MLP策略。
- **参数规模**：AdaTok整体97M参数，MH-LoRA仅增加~1.1%（约1.05M参数），ATA策略网络330K参数。

### 5. 实验数量与充分性

- **实验组数**：论文提供了**5张表格**（Table 1–5）和**4张图表**（Figure 1–4），涵盖：
  - 整体重建与生成对比（Table 1）
  - 消融研究：PRL组件（Table 2 (a-c)）、ATA策略学习范式（Table 2 (d-f)、Table 4）
  - 匹配预算重建（Table 3）
  - AR生成吞吐量分析（Table 5）
  - 优化轨迹（Figure 4）、各预算rFID曲线（Figure 3）、帕累托前沿（Figure 1(b)）
- **充分性与客观性**：
  - 消融实验系统：从NTM单独影响、MH-LoRA恢复、到启发式vs GRPO vs GRPO+DPW，逻辑链条完整。
  - 对比方法覆盖主流固定与弹性离散令牌化器，但连续自适应方法（CAT、Semanticist）因位深度不同而被明确指出不可直接比较，态度客观。
  - AR生成实验采用“自比较”（关注相对∆gFID），避免与全训练基线直接对比，设计合理。
  - 缺乏对视频或多模态场景的测试，且未报告码本利用率之外的更多鲁棒性分析（如对抗样本）。
- **总体评价**：实验设计较为充分，消融支撑了各模块必要性，但应用广度有限。

### 6. 论文的主要结论与发现

1. **AdaTok实现了自预算离散令牌化**：平均仅118个令牌即达到rFID 1.50，显著优于相同或更高预算的离散弹性基线（如One-D-Piece 256令牌rFID 1.55）。
2. **MH-LoRA解决了同前缀的语义偏移**：单独加入NTM导致rFID恶化（2.04 vs 1.71），加上MH-LoRA后反超（1.63），说明预算专用调制器是关键。
3. **确定性分组GRPO+DPW优于其他策略**：相比PPO（过度分配令牌）、REINFORCE（方差大导致太短）和固定权重GRPO，DPW获得更高的超体积（1342）和更优的长度-质量权衡。
4. **AR生成吞吐量提升显著**：118令牌平均下，生成吞吐量约为固定256令牌的2.1倍；128令牌时甚至比256令牌的gFID更好（∆ = -0.34），说明“少即是多”在特定训练步数下成立。
5. **令牌序列具有由粗到细的信息层次**：频谱分析显示低频能量占比随预算增长单调下降，验证NTM实现了信息排序。

### 7. 优点：方法或实验设计上的亮点

- **方法论亮点**：
  - **自预算**：首次将令牌数量作为内容条件的可学习输出，而非外部超参数，具有实际部署价值。
  - **表示-分配协同设计**：NTM + MH-LoRA使得前缀跨预算可解码，DPW + GRPO实现单次分配，无需搜索或外部预测器。
  - **轻量高效**：两阶段训练仅增加~3%训练开销，MH-LoRA增加1.1%参数，ATA推理为单次前馈。
- **实验设计亮点**：
  - 消融实验逻辑严密：从NTM单独有害到MH-LoRA修复，再到策略提升，每步结论清晰。
  - 公平对比：明确区分离散与连续令牌的位深度差异，并仅在离散域内进行主要排名。
  - AR生成采用自比较（∆gFID），避免与全训练基线混淆，突出自身弹性带来的效果。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 仅使用ImageNet-1K一个数据集，未在更多领域（如医学、遥感、多分辨率）验证；未评估视频或多模态任务。
  - 生成任务仅作为兼容性验证（MaskGIT需要固定长度），未训练可变长度生成器（论文提到未来工作需视觉EOS机制）。
- **偏差风险**：
  - 两阶段训练依赖第一阶段PRL的收敛质量，若PRL未完全优化，ATA策略可能不优。
  - 预算桶离散化为8个，未探索更细粒度或连续预算。
- **应用限制**：
  - 离散编码的rFID整体高于连续编码（如CAT rFID 0.46），但后者每像素位宽更高，表1中未进行严格的bpp对比。
  - 推理时需为每个预算桶配置LoRA头（8个），虽小但增加存储开销（~1.1%参数）。
  - AR生成时若需自适应选择长度，必须依赖视觉EOS（尚未实现），论文中117令牌平均结果仅用于分析，实际生成仍需固定预算。

（完）
