---
title: Connections Between Pairs of Filters Improve the Accuracy of Convolutional Neural Networks
title_zh: 滤波器对之间的连接提高卷积神经网络的准确性
authors: "Kathleen Anderson, Philipp Grüning, Erhardt Barth"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13736v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 通过滤波器对连接提高CNN准确性，属于计算机视觉架构改进技术
tldr: 传统卷积神经网络依赖逐点激活函数构建层级结构，存在信息交互受限的缺点。本文提出在卷积层的每对滤波器之间引入可学习的连接函数，替代固定的乘法或最小值操作。通过参数化连接方式，网络能针对不同层自适应调整连接策略，从而提升特征表达能力。实验证明，该设计在图像分类任务上显著提高了准确率，为CNN架构创新提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13736v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1180, \"height\": 1199, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1802, \"height\": 996, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 373, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 879, \"height\": 558, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 822, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13736v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 726, \"height\": 547, \"label\": \"Figure\"}]"
motivation: 解决传统CNN中逐点非线性导致滤波器间信息交互不充分的问题。
method: 在卷积层中，对每对滤波器的输出应用一个可学习的参数化连接函数，替代固定操作。
result: 在多个图像分类基准上，可学习连接函数相较于固定连接（如乘法）带来稳定的准确率提升。
conclusion: 滤波器间的可学习连接能自适应调节信息融合方式，是增强CNN性能的有效手段。
---

## 摘要
尽管研究人员不断为卷积神经网络寻找新的改进网络结构，但大多数新发明的架构仍然依赖于堆叠卷积块并用逐点激活函数分隔的传统模式。然而，纯粹基于逐点非线性的网络存在缺陷。一种替代方案是在网络的两个滤波器之间引入成对连接。典型的连接函数使用乘法或最小值操作来实现逻辑与连接。在本文中，我们更进一步，证明了卷积神经网络可以从更一般的连接中获益，这些连接包含可学习的参数。借助这些参数，网络能够在不同网络层实现不同的连接，并更好地使连接函数适应手头的任务。

## Abstract
While researchers continue to find new and improved network structures for CNNs, most of the newly invented architectures still rely on the traditional pattern of stacking convolutional blocks and separating them with pointwise activation functions. However, there are drawbacks to a network purely building on pointwise nonlinearities. One alternative is to introduce a pairwise connection between two filters of a network. Typical connection functions use multiplications or the minimum operation to realize logical AND connections. In this paper, we go one step further by demonstrating that CNNs can benefit from more general connections, which include parameters that are learned. With such parameters, the network is able to implement different connections in different network layers and better adapt the connection function to the task at hand.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统卷积神经网络（CNN）依赖逐点非线性激活函数（如 ReLU）来分离卷积层，这种结构限制了滤波器之间的信息交互，导致网络对复杂特征（如角点、交叉点）的检测能力不足，且对对抗攻击的鲁棒性较差。
- **研究动机**：已有工作（如特征乘积网络 FP-nets、最小网络 Min-nets）通过引入成对滤波器之间的“与”（AND）连接（乘法或最小值操作）来检测二维结构（如角落、结），但固定的连接函数无法适应不同网络层的需求。作者希望设计更通用的、可学习的连接函数，使网络能够根据任务和层位置自适应调整连接方式。
- **整体含义**：打破传统“卷积+逐点非线性”的固定模式，证明滤波器对之间的参数化连接能够提升 CNN 的准确率，为架构设计提供新方向。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在 CNN 中插入“连接块”（connected block），该块包含两个并行的深度可分离卷积，其输出通过一个**可学习的参数化连接函数**进行融合，而非固定的乘法或最小值。
- **关键技术细节**：
  - **连接块结构**（见图 1）：
    - 输入经 1×1 卷积、批量归一化（BN）、ReLU 后，分成两路。
    - 每路进行 3×3 深度可分离卷积 + 实例归一化（IN）。
    - 两路输出通过**连接函数** f(x, y) 合并，再经 1×1 卷积 + BN，最后与输入残差连接。
  - **连接函数的演变**：
    - 基线函数：
      - 简单连接：f(x, y) = (x + y)⁺ （ReLU后的加法）
      - AND 连接（Min 块）：f(x, y) = min(x⁺, y⁺)
    - 新提出的函数：
      - XOR 连接（固定参数 l=0.5）：f(x, y) = max(x, y) − 0.5·(x + y)⁺
      - **可学习的 XOR 连接**（核心贡献）：
        \[
        f(x, y) = \max(x, y) - l \cdot (x + y)^+
        \]
        其中 l 是每个连接块独立学习的标量参数，取值范围 [0, 1]。
        - 当 l=0 时，f = max(x, y)（类似“或”门）；
        - 当 l=0.5 时，实现 XOR 效果；
        - 当 l=1 时，接近“与”门但考虑符号差异。
  - **网络架构**：
    - 主干为简化的金字塔残差网络，包含三个栈（stack），每个栈由 N 个块组成（实验中 N=3）。
    - 连接块放置在**每个栈的第一个位置**（即第 1、4、7 块），其余为基本块（无连接）。
    - 总参数量约 217k。

#### 3. 实验设计：数据集、基准与对比方法

- **数据集**：CIFAR-10（60k 张 32×32 彩色图像，10 类，50k 训练 / 10k 验证）。
- **基准**：
  - **无连接块的基线网络**（纯基本块架构）。
  - **简单连接块**（加法融合）。
  - **AND 连接块**（最小值融合，即 Min-net 风格）。
- **对比方法**：实验中共评估了约 10 种不同的连接函数（见附录），包括 ReLU Min、Gauss Min、ReLU Min minus Absolute、Simple、ReLU XOR、ReLU Sum、Min minus Absolute、ReLU Min、XOR、XOR learned 等。主要对比焦点为：固定 XOR、学习 XOR 与上述基线。
- **训练设置**：
  - 优化器：SGD，momentum=0.9，weight decay=0.0001。
  - 学习率：初始 0.1，在第 100 和 150 个 epoch 乘以 0.1。
  - 总 epoch：200。
  - 重复次数：每个实验用 5 个不同随机种子（对学习 XOR 参数单独进行了 25 次额外重复，共 30 次）。

#### 4. 资源与算力

- **未明确说明**：论文未提及使用的 GPU 型号、数量或具体训练时长，仅提到使用 PyTorch 框架（推测）。仅提供了参数数量（217k）和训练 epoch 数。在资源描述上存在不足。

#### 5. 实验数量与充分性

- **实验数量**：
  - 主实验：对比了约 10 种不同连接函数在 CIFAR-10 上的准确率（图 6）。
  - 额外实验：对学习 XOR 参数 l 的分布进行了 30 次重复训练（图 7）。
  - 未在其他数据集（如 CIFAR-100、ImageNet）上验证，也未进行跨架构（如 ResNet、MobileNet）的集成测试。
- **充分性与公平性**：
  - **优点**：控制参数量相同（217k），使用相同架构仅替换连接函数，对比公平；每个条件重复 5 次，统计均值与盒形图。
  - **不足**：仅在 CIFAR-10 一个数据集上评估，泛化能力未验证；未与更广泛的最新方法（如 Volterra 网络、Π-net）进行直接比较；未提供消融实验（如不同位置放置连接块、不同 N 值的影响）。因此实验充分性有限，但结论在 CIFAR-10 上可靠。

#### 6. 论文的主要结论与发现

- **结论**：成对滤波器之间的连接函数**不限于 AND 逻辑**，其他函数（如加法、XOR）同样能提升准确率；**可学习的参数化 XOR 函数**在所有对比中表现最优。
- **具体发现**：
  - 学习 XOR 连接块（均值 92.4%）比无连接块基线（91.8%）高 0.6%，比简单连接高 0.4%。
  - 在 217k 参数下，该准确率接近 MobileNetV2（1.06M 参数）的 92.4%。
  - 学习参数 l 的分布具有规律性（图 7）：第三个栈（最深）的 l 始终很低（≈0，即 OR 型），前两个栈的 l 一高一低（一个接近 1，一个接近 0），表明网络自动选择在不同层使用不同类型的连接。

#### 7. 优点

- **方法创新性**：将可学习参数引入滤波器间连接函数，突破固定 AND/OR 限制，使网络能自适应调整行为。
- **轻量高效**：仅增加少量参数（每个连接块一个标量 l），总模型参数量极小（217k），却带来显著提升。
- **可解释性**：学习到的 l 值揭示了网络不同层的偏好（浅层倾向 AND 型滤波，深层倾向 OR 型保留信息），有助于理解 CNN 内部机制。
- **实验设计规范**：控制变量法，对比多种连接函数，统计多次重复结果。

#### 8. 不足与局限

- **实验覆盖不足**：仅在 CIFAR-10 上验证，未在更大数据集（如 ImageNet）、其他任务（如目标检测、语义分割）上测试，泛化性存疑。
- **偏差风险**：网络架构固定（两栈三块、特定位置放置），未探索其他配置（如更多栈、不同放置策略），可能对结果产生 bias。
- **计算资源未报告**：缺乏训练时间、GPU 型号等细节，可复现性受限。
- **对比不全面**：未与最新紧凑网络（如 ShuffleNet、EfficientNet-Lite）或含有其他非线性交互的方法（如自注意力）比较。
- **理论分析较弱**：连接函数为何有效仅从“2D 特征检测”角度解释，缺乏严格数学推导。
- **应用限制**：双路并行结构增加了推理计算量（虽然参数少，但深度可分离卷积数量翻倍），不适用于严格受限的移动端场景。

（完）
