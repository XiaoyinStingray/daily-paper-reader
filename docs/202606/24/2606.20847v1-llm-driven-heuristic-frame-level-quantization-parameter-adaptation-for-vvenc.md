---
title: LLM-Driven Heuristic Frame-Level Quantization Parameter Adaptation for VVenC
title_zh: 基于LLM驱动的VVenC帧级量化参数自适应启发式算法
authors: "Liqiang He, Yingwen Zhang, Riyu Lu, Meng Wang, Shiqi Wang"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20847v1"
tags: ["query:compress"]
score: 8.0
evidence: 使用大语言模型和率失真优化启发式进行量化参数自适应，直接适用于图像压缩量化策略
tldr: 帧级量化参数分配是视频编码难题，固定QP方案内容无关，经典RDO乘数设置不准。本文利用LLM构建闭环进化框架，自动设计RDO启发式，通过VVenC编码评估候选函数。实验表明，进化启发式在率失真上优于固定QP和拉格朗日基线。LLM自主发现一种自适应启发式，通过熵项惩罚QP波动，为RDO算法设计提供新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20847v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1692, \"height\": 1258, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20847v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 1291, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20847v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1887, \"height\": 782, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20847v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 696, \"height\": 435, \"label\": \"Table\"}]"
motivation: 固定QP忽略内容特性，经典RDO因乘数不准导致次优分配，需自动设计自适应启发式。
method: 构建闭环进化框架，LLM迭代提出RDO启发式代码，通过VVenC编码直接评估各候选函数的QP选择效果。
result: 多个测试集上，LLM进化启发式在率失真性能上超越固定QP方案和拉格朗日基线。
conclusion: LLM自主发现自适应启发式，利用熵惩罚QP波动，为率失真优化算法设计提供新思路。
---

## 摘要
最优帧级量化参数（QP）分配仍然是现代视频编码器中的一个持久挑战。实际系统中广泛采用的固定QP方案本质上是与内容无关的，而经典的拉格朗日率失真优化（RDO）方法常因不精确的乘数设置而效果不佳。本文探索了使用大型语言模型（LLM）来自动设计用于帧级QP自适应的RDO启发式算法。我们构建了一个闭环进化框架，其中LLM迭代地提出RDO启发式算法作为带有可执行代码的算法思想，这些候选方案通过弗劳恩霍夫通用视频编码器（VVenC）直接编码进行评估，每个启发式算法作为一个评分函数，基于过去帧和当前候选对象的编码统计来比较不同的QP选择。多个测试集上的实验结果表明，进化得到的启发式算法在固定QP方案和拉格朗日基线之上均实现了有希望的率失真改进。进一步分析揭示，LLM能自主发现一种自适应启发式算法，通过基于熵的项来惩罚QP波动，为RDO算法的设计提供了新的见解。

## Abstract
Optimal frame-level quantization parameter (QP) allocation remains a persistent challenge in modern video encoders. The fixed-QP scheme widely adopted in practical systems is inherently content-agnostic, while classical Lagrangian rate-distortion optimization (RDO) methods often suffer from inaccurate multiplier settings. In this paper, we explore the use of large language models (LLMs) to automatically design RDO heuristics for frame-level QP adaptation. We construct a closed-loop evolutionary framework in which the LLM iteratively proposes RDO heuristics as algorithmic ideas with executable code, and these candidates are evaluated directly through encoding with the Fraunhofer Versatile Video Encoder (VVenC), where each heuristic acts as a scoring function that compares different QP choices based on the encoding statistics of past frames and current candidates. Experimental results across multiple test sets show that the evolved heuristic achieves promising rate-distortion improvements over both the fixed-QP scheme and the Lagrangian baseline. Further analysis reveals that the LLM can autonomously discover an adaptive heuristic that penalizes QP fluctuations via entropy-based terms, providing new insights into the design of RDO algorithms

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在VVenC编码器中，帧级量化参数（QP）分配直接影响率失真性能。固定QP方案（如基于时间层预设偏移）是内容无关的，无法适应不同视频内容的纹理复杂度、运动幅度等特性。经典的拉格朗日率失真优化（RDO）方法虽然理论上可自适应，但实际中拉格朗日乘子λ难以准确设定，且缺乏对帧间依赖关系的显式建模，往往导致性能不如甚至劣于固定QP方案。
- **动机**：如何自动设计一个内容自适应、且无需手动调参的RDO启发式算法，以提升帧级QP分配的效率。
- **整体含义**：首次探索利用大型语言模型（LLM）的代码生成与推理能力，通过闭环进化框架自动发现有效的RDO启发式，为视频编码优化提供全新思路。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：构建LLM驱动的闭环进化框架，将RDO启发式定义为“可执行代码+算法思想”，通过迭代生成、编码评估、种群管理，自动进化出优于固定QP和传统拉格朗日方法的帧级QP决策函数。
- **关键技术细节**：
  - **RDO启发式定义**：每个候选QP对应的编码统计集合 \( S_t \) 包含历史帧（0~t）的比特率、失真、QP值、帧类型、参考层信息等。LLM生成的启发式函数 \( H(S_t) \) 返回候选QP的得分，选择得分最高的QP。
  - **三大模块**：
    1. **LLM启发式生成**：使用DeepSeek模型，通过“探索（exploration）”和“修改（modification）”两种提示策略，基于历史最优启发式的代码和思想，生成新的候选。
    2. **启发式评估**：将候选启发式嵌入VVenC多遍编码流程，对每个候选QP编码当前帧（复用历史决策），收集统计 \( S_t \)，计算启发式得分并选择QP。完成全序列编码后，计算与固定QP方案的BD-rate作为适应度。
    3. **启发式管理**：维护固定容量K=4的种群，保留最优启发式；新启发式若优于最差则插入，并基于适应度采样父代用于下一轮生成。
  - **迭代过程**：10次迭代，每次生成4个候选（2次探索+2次修改），共40次评估。最终选择最佳启发式用于测试。

### 3. 实验设计

- **数据集**：
  - **训练集**：SJTU 4K数据集中的5个序列（Wood, TrafficAndBuilding, TrafficFlow, TallBuildings, ResidentialBuilding），下采样至1080p。
  - **测试集**：CTC Class B（5个序列）、UVG、MCL-JCV。
- **编码配置**：VVenC 1.7.0，faster preset，随机接入（RA）配置，帧长65，Intra Period=32。QP值：22, 27, 32, 37；候选QP集为基QP±1,±2。
- **对比方法**：
  - 固定QP方案（VVenC默认层级偏移）。
  - 拉格朗日RDO基线（基于文献[27][28]的扩展，使用固定λ）。
- **评价指标**：BD-rate（YUV-PSNR，Y:U:V权重6:1:1），负值表示比特节省。

### 4. 资源与算力

- 文中未明确说明使用的GPU型号、数量、训练时长。
- 仅提及使用 **DeepSeek** 作为LLM引擎，VVenC编码在CPU上运行（通过多遍编码实现）。
- 实验算力消耗较大（每次迭代需对4个候选×5个训练序列×4个QP分别编码），但具体硬件规格缺失。

### 5. 实验数量与充分性

- **实验数量**：在训练集上进化10代共40个候选；在测试集上报告了3个测试集共12个序列的BD-rate结果。每个QP独立优化启发式，但最终报告为整体平均。
- **充分性评价**：
  - **充分**：对比了固定QP和拉格朗日基线，覆盖了CTC Class B、UVG、MCL-JCV等标准测试集；展示了单个QP=32启发式的详细分析（含代码和公式）。
  - **不足**：未进行消融实验（如不同提示策略、不同LLM、不同进化代数的影响）；未与其他先进QP自适应方法（如基于学习的、λ自适应方法）对比；仅使用PSNR，未考虑主观质量指标；拉格朗日基线实现方式（固定λ）可能并不公平（其BD-rate为正值，说明该基线不适合QP决策）。
- **客观性**：BD-rate计算标准，但拉格朗日基线结果较差且未调优，可能导致对比倾向。

### 6. 主要结论与发现

- **性能结果**：LLM进化启发式在CTC Class B平均BD-rate节省-1.73%，UVG -1.00%，MCL-JCV -1.06%，均优于固定QP方案和拉格朗日基线（拉格朗日基线反而平均增加约2.5%~3%）。
- **可解释启发式示例**（QP=32）：LLM自动发现了以下评分函数：
  \[
  H(S_t) = \frac{PSNR_w}{(1 + H_{QP}) \cdot \log(\sum R_i + 1)}
  \]
  其中PSNR_w为加权平均PSNR，\( H_{QP} \) 为QP分布的熵。该启发式以“每对数比特的PSNR增益”为导向，并通过熵项惩罚频繁QP切换，实现动态比特分配，避免了固定λ的局限。
- **Q P分配行为**：进化启发式在保持层级结构的同时展现出灵活性，允许某些低层帧的QP与高层帧相同，更好地适应局部内容。

### 7. 优点

- **方法创新**：首次将LLM应用于视频编码RDO启发式自动设计，开创了新的优化范式。
- **模块化框架**：启发式生成、评估、管理分离，便于扩展至其他编码参数（如CTU级QP、λ调优等）。
- **可解释性**：LLM生成的启发式具有清晰的数学形式（如熵惩罚项），揭示了有效的设计原则，优于黑箱优化。
- **实用可行**：基于多遍编码的评估方式避免了修改编码器核心代码，易于复现。

### 8. 不足与局限

- **实验覆盖有限**：仅针对1080p分辨率、RA配置、VVenC编码器；未测试低分辨率、低延迟配置或其他编码器（x265、HM等）。
- **训练集规模小**：仅5个训练序列，可能限制泛化性；未对训练集与测试集内容相似性进行分析。
- **计算资源消耗大**：每代需多次编码（4候选×5序列×4QP），且未报告LLM推理和编码时间成本。
- **对比基线不充分**：拉格朗日基线使用固定λ，可能不是最优实现；未与基于学习的方法（如RL、进化算法）或自适应λ方法对比。
- **缺少消融研究**：未单独验证“探索vs修改”策略、种群大小、迭代次数等因素的影响。
- **评估指标单一**：仅使用PSNR-based BD-rate，未考虑主观质量（SSIM、VMAF）或编码时间。
- **LLM依赖**：框架效果高度依赖LLM生成质量，文中仅使用DeepSeek，未探索其他模型（如GPT-4、Claude）。

（完）
