---
title: "UI-LIC: A Unified Framework for Evaluating Learned Image Compression Models"
title_zh: UI-LIC：一种评估学习型图像压缩模型的统一框架
authors: "Nicholas J. Nolen, Luc Trudeau, Andrew C. Freeman"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23545v1"
tags: ["query:compress"]
score: 9.0
evidence: 学习型图像压缩模型统一评估框架，直接关联所有图像压缩需求
tldr: 学习图像压缩（LIC）评估因异构软件栈和不同训练条件而复杂。UI-LIC开源框架集成6个高性能LIC模型，提供统一训练、推理和分析控制器，GUI支持与传统视频帧内编码器在相同比特率下比较PSNR、SSIM等指标，并可交互式分析质量热图。该框架降低了研究门槛，便于公平比较和主观分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23545v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23545v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1744, \"height\": 818, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23545v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 1026, \"label\": \"Table\"}]"
motivation: 解决LIC评估因异构软件栈、训练条件和评估方法不同导致的复杂性和不可比较问题。
method: 集成6个高性能LIC模型，构建统一控制器进行训练、推理和分析，提供GUI和可配置质量热图覆盖。
result: 支持在相同比特率下与传统编码器比较PSNR、SSIM、VMAF、LPIPS，并生成交互式热图。
conclusion: UI-LIC开源框架简化LIC评估流程，提升可重复性，促进公平比较与后续研究。
---

## 摘要
学习型图像压缩（LIC）系统的评估和比较因异构的软件栈、不同的训练条件和多样的评估方法而变得复杂。为了解决这些挑战，我们提出了UI-LIC，一个用于评估LIC模型的开源软件框架。我们集成了六个高性能LIC模型，并提供了一个集中控制器，用于使用共享配置参数进行训练、推理和分析。我们的图形用户界面程序提供了一个简化的界面来评估这些模型以及传统的视频帧内编码器，平衡压缩比特率并计算PSNR、SSIM、VMAF和LPIPS等质量指标。最后，我们提供了一个交互式图像分析器，带有可配置的质量热图叠加。我们的框架降低了进一步LIC研究的门槛，通过单一设置命令解锁比较指标和主观分析。该开源软件在MIT许可下发布，可在github.com/BaylorMultimediaLab/UI-LIC获取。

## Abstract
The evaluation and comparison of Learned Image Compression (LIC) systems is complicated by heterogeneous software stacks, varying training conditions, and divergent evaluation methodologies. To address these challenges, we introduce UI-LIC, an open-source software framework for evaluating LIC models. We integrate six high-performance LIC models, and provide a centralized controller for performing training, inference, and analysis with shared configuration parameters. Our GUI program offers a streamlined interface to evaluate these models alongside traditional video intra-frame encoders, equalizing the compressed bitrates and calculating quality metrics such as PSNR, SSIM, VMAF, and LPIPS. Finally, we provide an interactive image analyzer with configurable quality heatmap overlays. Our framework lowers barriers to further LIC research, unlocking comparative metrics and subjective analysis with a single setup command. The open-source software is released under the MIT license and is available at github.com/BaylorMultimediaLab/UI-LIC.

---

## 论文详细总结（自动生成）

# UI-LIC：一种评估学习型图像压缩模型的统一框架——论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：学习型图像压缩（LIC）模型的评估和比较面临严重困难，原因包括：
  - 异构的软件栈、不同的训练条件、评估方法不统一。
  - 训练数据、优化目标、模型架构、检查点选择、评估实现等多种因素均会影响最终率失真性能。
  - 生成式LIC模型（如StableCodec）可能产生“幻觉”伪影（视觉上合理但语义上与原图不同），传统客观指标不能完全反映。
  - 不同模型作为独立仓库发布，依赖环境、配置、脚本各异，集成对比需要大量工程工作。
- **动机**：需要一个统一、开放、可复现的评估框架，降低研究门槛，促进LIC领域的公平比较和主观分析。
- **含义**：UI-LIC框架旨在解决上述问题，提供训练、评估、可视化的一站式工具，支持与传统编码器的对比，并开源（MIT许可）。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：模块化、解耦的系统，将模型训练、推理、可视化分离，通过统一接口管理多个LIC模型。
- **关键技术细节**：
  - **软件架构**：
    - Python 3.10实现，每个LIC模型维持其原始包版本（通过Conda环境隔离，避免冲突）。
    - 提供快速启动脚本，自动创建各模型虚拟环境并下载官方预训练权重（包括VMAF的Docker容器）。
    - **参数解析接口**：基接口统一构建训练/测试命令、验证参数、在不同Conda环境中执行。
    - **训练与测试接口**：各LIC模型继承自基接口，通过别名映射将模型特有变量（如epoch数、学习率、数据集路径）统一到全局参数。
    - **任务配置**：JSON配置文件定义模型、数据集、作业及参数，用户可输入全局参数并覆盖每个模型的默认值。
    - **任务调度器**：加载接口和参数文件，检查必需参数，允许用户选择执行哪类作业。
  - **集成模型**：
    - LIC模型：ELIC、StableCodec、TCM、HPCM、DCVC-RT intra、RwkvCompress（共6个）。
    - 传统编码器（通过FFmpeg）：AVC/H.264（x264和NVENC）、HEVC/H.265（x265和NVENC）、AV1（SVTAV1和NVENC）。
  - **对参考代码的修改**（关键调整）：
    - DCVC-RT：开发专用帧内训练/推理脚本。
    - StableCodec：标准化测试使用ImageFolder而非H5Database；将epoch转换为step以便传入StableCodec原生训练。
    - ELIC和TCM：开发评估脚本简化比特流和重建表示的保存。
    - 所有模型：替换硬编码路径，增加输入验证和错误处理。
  - **评估流程**：
    - 支持客观指标：PSNR（加权和YUV分量）、SSIM。
    - 感知指标：VMAF、LPIPS。
    - 效率指标：bpp、推理延迟。
    - LPIPS分解：将5个特征层（从低层高频到高层语义）保存为单独图像，支持交互式误差分析。
    - 误差图生成：MSE、块级SSIM、归一化图像梯度。
    - **比特率均衡**：对于不原生支持可变比特率的LIC模型（需要多个权重集），传统编码器通过QP搜索匹配LIC的压缩比特率，实现公平对比。
  - **交互式评估工具（GUI）**：
    - 基于Tkinter的图形界面（图2）。
    - 用户选择数据集，启用LIC模型和传统编码器执行编码。
    - 支持加载已有重建图像（用于未集成的新编码器）。
    - 可视化：侧边对比、滑动遮罩、误差图叠加（包括LPIPS各层独立开关）。
    - 报告界面：显示定量指标表格和数据集平均结果。

## 3. 实验设计
- **数据集**：仅使用了Kodak数据集（True Color Kodak Images，24幅图像作为测试集）。
- **Benchmark**：对比了6个LIC模型（HPCM、LIC-TCM、RwkvCompress、StableCodec、DCVC-RT、ELIC）与3个传统编码器（AV1、AVC、HEVC），在相同比特率下比较PSNR、SSIM、LPIPS、VMAF。
- **关键实验**：论文中仅展示了表1的结果，即各模型在Kodak上的平均性能（bpp由比特率均衡决定）。例如：
  - HPCM在1.25 bpp下PSNR 42.23，优于AV1等传统编码器，但LPIPS和VMAF不如AV1。
  - DCVC-RT在0.47 bpp下PSNR 39.21，全面优于传统编码器。
  - StableCodec极低码率（0.03 bpp）但质量指标极差（PSNR 27.56, SSIM 0.572），反映出生成式模型的幻觉问题。

## 4. 资源与算力
- **未明确说明**：论文未提及训练任何LIC模型耗用的GPU型号、数量、训练时长等资源。所有实验均使用各模型的预训练权重，未进行额外训练。
- **仅提及**：VMAF指标需要Docker容器；每个模型的环境隔离；GPU依赖假设（CUDA版本）由原始仓库决定。
- **结论**：本框架不涉及训练资源消耗的测量，主要聚焦于推理和评估。

## 5. 实验数量与充分性
- **实验数量**：仅有一组主实验（表1），在Kodak数据集上对比9个编码器（6个LIC + 3个传统编码器，传统编码器可能有不同实现但不区分）。
- **公平性**：
  - 优点：实现了比特率均衡，LIC与传统编码器在同一比特率下比较；所有LIC模型使用官方预训练权重；传统编码器通过QP搜索逼近目标比特率。
  - 不足：仅一个数据集（Kodak），缺乏对更大或多样性数据集（如CLIC、DIV2K）的测试；没有消融实验验证框架对不同配置的鲁棒性；未比较不同训练数据或超参数下的表现（因为直接使用官方权重）。
- **充分性**：作为框架介绍论文，实验更多是演示功能而非全面benchmark；但对于验证框架可行性，该实验足够。

## 6. 主要结论与发现
- UI-LIC框架成功降低了LIC模型集成和对比的工程成本，通过统一接口和自动化环境管理实现快速复现。
- 通过比特率均衡，揭示了不同LIC模型在不同质量指标上的权衡：例如DCVC-RT在多个指标上优于传统编码器，而StableCodec作为生成式模型在极低比特率下PNSR极低但视觉可能合理（需主观分析）。
- LPIPS分解和误差图叠加提供了深入的空间误差分析能力，有助于理解模型在不同语义区域的优劣（如StableCodec天空区噪声表现好，但物体表征差）。
- 框架支持与传统编码器的对比，将LIC置于更广泛的压缩上下文中。

## 7. 优点
- **统一性与可复现性**：通过隔离环境解决依赖冲突；JSON配置文件集中管理任务；一键设置简化了入门流程。
- **交互性与可视化**：GUI提供直观的侧边比对、误差热图叠加、LPIPS特征层独立开关，支持主观分析。
- **公平对比**：比特率均衡机制确保LIC与传统编码器在同一目标比特率下比较，避免因比特率不同导致的误导。
- **可扩展性**：模块化设计便于集成新LIC模型；支持加载外部重建图像，允许评估未集成的新编码器。
- **开源友好**：MIT许可，提供代码仓库，鼓励社区贡献。

## 8. 不足与局限
- **模型覆盖有限**：仅集成6个LIC模型，且部分模型（如ELIC）使用非官方代码（Wei Jiang的Unofficial ELIC），可能引入偏差。
- **预训练权重依赖**：未涉及模型训练过程；不同预训练检查点可能影响结果，但框架未提供统一训练脚本生成标准化权重。
- **数据集单一**：仅用Kodak 24张图片，样本量小，无法全面评估编码器在复杂场景（如纹理、边缘、人脸、文本）的表现；缺乏主观实验验证。
- **传统编码器配置未充分优化**：仅通过QP搜索实现比特率匹配，未对x264、x265、AV1的编码参数（如preset、profile、tune）进行系统调优，可能限制传统编码器性能。
- **计算效率评测有限**：只报告延迟，但未提供详细的推理时间或内存占用对比；不同模型在不同GPU硬件上的表现未讨论。
- **忽视训练资源需求**：未讨论各LIC模型的训练成本（GPU时长、数据量），而这对实际部署决策很重要。
- **UI/UX限制**：GUI基于Tkinter，功能有限（如图2所示界面较简陋）；缺乏批量处理或高级分析功能。

（完）
