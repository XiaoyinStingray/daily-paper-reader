---
title: Functional Gradient Descent with Adaptive Representations
title_zh: 自适应表示的函数梯度下降
authors: "Daniel Csillag, Rodrigo Schuller, Pedro Dall'Antonia, Leonidas Guibas, Luiz Velho, Tiago Novello"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16926v1"
tags: ["query:neural-arch"]
score: 6.0
evidence: 自适应表示的函数梯度下降优化方法
tldr: 功能优化常通过固定参数表示（如神经网络）求解，导致非凸损失和理论困难。功能梯度下降（FGD）直接在函数空间优化，具有强收敛理论，但无限维梯度难以实现。现有近似方法引入误差且缺乏保证。本文提出自适应表示FGD，将近似显式纳入分析，首次实现具有收敛保证的通用FGD，在光滑损失和PL条件下分别收敛到平稳点和全局最优。在回归、PDE和视觉任务中，该方法在效率和精度上均优于固定近似FGD和神经网络。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16926v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 519, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16926v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1386, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16926v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1458, \"height\": 762, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 401, \"label\": \"Table\"}]"
motivation: 现有FGD因功能梯度无限维而无法直接实现，固定近似引入误差且缺乏收敛保证。
method: 提出自适应调整功能梯度表示的FGD算法，将近似误差显式纳入收敛分析。
result: 首次证明可实现FGD在光滑损失下收敛到平稳点，在PL条件下收敛到全局最小。
conclusion: 在回归、PDE求解和计算机视觉中，方法在效率和精度上均优于固定近似FGD与神经网络基线。
---

## 摘要
函数优化问题通常通过优化固定表示（如神经网络）的参数来解决，导致高度非凸的损失函数，这使训练和理论分析复杂化。一个有趣的替代方法是函数梯度下降（FGD），即直接在函数空间中进行梯度下降，它具有强收敛性结果并拥有简洁的理论。然而，FGD在实践中难以实现，因为函数梯度是无限维的，因此无法完全计算或存储在内存中。现有的实现因此依赖于固定近似，这引入了近似误差。我们提出了一种新的、有理论基础的自适应函数梯度下降算法，该算法在优化过程中调整函数梯度的表示。通过将这种近似显式地纳入分析，我们证明了无论近似如何，算法都能收敛到平稳点（对于光滑损失）和全局最小值（在光滑性和Polyak-Lojasiewicz型条件下）。据我们所知，这是第一种在一般设置下具有如此保证的可实现FGD方法。我们在回归、偏微分方程数值解和现代计算机视觉问题上展示了我们方法的有效性。在各种设置中，我们的方法在效率和准确性方面始终优于具有固定近似的FGD和神经网络基线。

## Abstract
Functional optimization problems are typically solved by optimizing the parameters of a fixed representation, such as a neural network, resulting in highly nonconvex losses that complicate both training and theoretical analysis. An interesting alternative is functional gradient descent (FGD), that is, gradient descent directly in function space, which benefits from strong convergence results and admits a clean theory. However, FGD is difficult to implement in practice because functional gradients are infinite-dimensional, and thus cannot be fully computed nor stored in memory. Existing implementations therefore rely on fixed approximations, which introduce approximation error. We propose a new, theoretically-grounded FGD algorithm that adapts the representation of the functional gradients over the course of optimization. By explicitly incorporating this approximation into the analysis, we establish convergence to a stationary point (for smooth losses) and to a global minimizer (under smoothness + a Polyak-Lojasiewicz-type condition) regardless of our approximations. To the best of our knowledge, this is the first implementable FGD method with such guarantees in a general setting. We demonstrate the effectiveness of our method on regression, numerical solution of PDEs, and modern computer vision. Across settings, our method consistently outperforms both FGD with fixed approximations and neural network baselines in efficiency and accuracy.