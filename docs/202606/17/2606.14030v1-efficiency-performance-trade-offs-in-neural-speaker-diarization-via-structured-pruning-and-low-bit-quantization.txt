Title: Efficiency-Performance Trade-offs in Neural Speaker Diarization via Structured Pruning and Low-Bit Quantization

URL Source: https://arxiv.org/pdf/2606.14030v1

Published Time: Mon, 15 Jun 2026 00:23:09 GMT

Number of Pages: 6

Markdown Content:
# Efficiency-Performance Trade-offs in Neural Speaker Diarization via Structured Pruning and Low-Bit Quantization 

# Rishit Chatterjee ∗∗ , Tahiya Chowdhury 

# Department of Computer Science, Colby College, Waterville, Maine, United States 

rchatt28@colby.edu , tahiya.chowdhury@colby.edu 

# Abstract 

Streaming speaker diarization is crucial for time-critical med-ical dispatch, but deploying it on resource-constrained hard-ware requires smaller, faster models. Using SIMSAMU, a dataset of simulated medical-dispatch conversations, we eval-uate streaming behavior before compressing the segmentation model with pruning and low-bit quantization. We character-ize performance across a range of streaming latency budgets and find that additional buffering is not consistently beneficial, while very low-latency operating points can substantially de-grade performance. Our study shows that model compression trades performance for memory footprint, and we highlight an operating point where FP16 reduces model size by half with es-sentially unchanged real-time factor, at a cost of a 40% relative DER increase against the baseline. This work characterizes the trade-offs for real-time deployment and contributes to speech technology that can enable reliable human communication in time-critical contexts. 

Index Terms : diarization, latency, efficient deep learning, prun-ing, quantization 

# 1. Introduction 

Speaker diarization determines who spoke when in multi-speaker recordings [1] and is a key upstream component for speaker-attributed automatic speech recognition (ASR), meet-ing transcription, and conversational analytics [2, 3]. However, reliability varies sharply across domains (e.g., meetings, broad-casts, call-center audio) due to acoustics, interaction patterns, and domain mismatch [4, 5]. This is particularly consequen-tial in safety-critical settings such as emergency medical dis-patch, where accurate speaker attribution supports downstream processing in a domain- and language-specific context [6]. Ef-ficiency is therefore a practical limitation: streaming systems must produce speaker labels with low latency, and batch sys-tems must operate faster than real time within predictable mem-ory budgets [5, 7]. These constraints are amplified in modern neural diarization, where accuracy gains often rely on scaling transformer encoders, increasing parameters and inference-time memory [8, 9], and in operational audio with channel mismatch and background noise [6]. Finally, because clinical recordings can contain protected health information and speech can act as a biometric voiceprint, public release of domain-specific diariza-tion data is uncommon, limiting systematic evidence about di-arization behavior in this setting [10, 11]. 

> **

indicates the corresponding author. 

# 2. Background 

Efficiency-aware streaming diarization. Diarization is often deployed either online or at scale (requiring high throughput and predictable memory) [12, 13]. A direction is to design mod-els and training strategies for bounded-delay inference. This includes causal segmentation models with configurable look-ahead and streaming diarization models that maintain com-pact speaker states (e.g., buffers/caches) to preserve consistent speaker mapping under small delay budgets [7, 14, 15]. Sev-eral studies explicitly sweep look-ahead and buffering choices to quantify latency–accuracy trade-offs under controlled online inference [7, 14, 15, 16]. Together, these studies motivate treat-ing latency constraints and runtime budgets as explicit experi-mental factors when evaluating diarization systems, rather than as post hoc deployment constraints. However, few works pro-vide an end-to-end characterization of how diarization accu-racy shifts across practical streaming latency budgets under a fixed, reproducible pipeline in a medical conversational setting. 

Pruning for resource-constrained diarization. Compres-sion of self-supervised learning (SSL) speech encoders has been studied in recent speech research because modern diarization pipelines increasingly use large pretrained SSL backbones (e.g., HuBERT/WavLM-style encoders) as their acoustic feature ex-tractors [17, 18, 19]. However, these models are compute- and memory-intensive, motivating pruning. Prior work explores both structured pruning of SSL transformers (e.g., removing layers/heads/feed-forward network components) and unstruc-tured one-shot pruning to induce sparsity [20, 21, 22]. While the results suggest that pruning can be a practical approach for meeting compute budgets, its impact on diarization infer-ence under throughput and memory constraints remains under-explored. 

Low-bit quantization for speech models. Quantization is another compression method that reduces memory footprint and can accelerate inference on integer-optimized hardware. The general foundation for integer-arithmetic-only inference is well established in previous works[23, 24, 25], with tech-niques such as low-bit post-training [26], 8-bit integer [27, 28], and mixed precision [29, 30] found useful. However, fewer works have evaluated quantization’s impact in end-to-end di-arization pipelines. This matters because diarization com-bines frame-level segmentation and speaker-embedding stages, and low-precision arithmetic can affect these components dif-ferently than token-level recognition, motivating diarization-specific quantization studies[31, 26, 27, 32]. As a result, the accuracy–footprint–throughput trade-offs of low-bit quantiza-tion remain less documented for diarization problem than for ASR, especially under realistic streaming constraints. 

> arXiv:2606.14030v1 [cs.SD] 12 Jun 2026

2.1. Contributions 

In this work, we address the aforementioned gaps, and our work makes three contributions: • We characterize diarization accuracy across a wide range of streaming latency budgets, and show that additional buffering is not consistently beneficial, while very low-latency operat-ing points can substantially degrade performance. • We show that choice of pruning structure and level substan-tially influence performance, motivating careful selection of the pruning strategy. • We show a case where pruning followed by quantization reduced the segmentation model size to half the original size—at a cost of a ∼40% increase in error when compared to the baseline model. 

# 3. Data 

Our experiments use SIMSAMU , a public corpus of real-istic medical dispatch phone dialogues in French, annotated for diarization and transcription [6, 33]. The corpus contains call recordings spanning a wide range of emergency scenarios. [6, 34]. This domain is critical for speech technology because dispatch conversations are time-critical, highly structured, and acoustically challenging, yet public diarization resources are uncommon due to privacy constraints [6]. SIMSAMU is well suited to our efficiency-first questions because it provides a pub-lic, standardized diarization recipe and scoring protocol, en-abling controlled streaming and compression sweeps under a fixed pipeline [6, 33]. As a service-domain corpus, it also helps address the lack of end-to-end evidence for diarization under practical deployment constraints in privacy-sensitive settings. For a detailed corpus breakdown and collection protocol, we refer the reader to the SIMSAMU dataset paper [6]. 

# 4. Methods 

4.1. Experimental Setup 

Our experiments follow the diarization setup provided with the SIMSAMU dataset release, i.e., we keep the same preprocess-ing, pipeline structure, and scoring protocol as the dataset paper and accompanying reference implementation [6, 35]. 1 We keep hyperparameters fixed to the defaults, including segmentation post-processing, embedding extraction options, and clustering settings, applying pruning/quantization only to the segmenta-tion model [36]. We keep the same evaluation protocol and re-port diarization error rate (DER) as the evaluation metric. All experiments were run on a single NVIDIA L40S GPU with 28 CPU cores and 64GB RAM, using FP32 inference unless stated otherwise. 

4.2. Evaluation Metric DER. DER is the standard performance metric for speaker diarization [3, 4]. DER measures the fraction of reference                  

> 1We adopt the SIMSAMU pipeline (medkit/simsamu-diarization ): aPyanNet
> speaker-segmentation model initialized from
> pyannote/segmentation-3.0 and paired with apretrained speaker-embedding extractor (pyannote/wespeaker-voxceleb-resnet34-LM )and agglomerative clustering [36, 37, 38]. The segmentation model uses a SincNet front-end followed by a 4-layer bidirectional LSTM (128 hidden units) and a 2-layer post-LSTM linear stack (128 hidden units) [39].

speaker time that is not correctly attributed, decomposed into false alarm, missed speech, and speaker confusion [40, 41]: 

DER = TFA + TMISS + TCONF 

TREF 

. (1) Here, TREF is the total reference speaker time, TFA is hy-pothesized speaker time not attributed to any reference speaker, 

TMISS is reference speaker time not attributed to any hypothe-sized speaker, and TCONF is reference speaker time attributed to the wrong speaker [40, 41]. DER is computed after finding an optimal one-to-one map-ping between reference speakers and hypothesized speaker la-bels (using Hungarian algorithm), so that errors reflect speaker attribution under the best label alignment [40, 41, 42, 43]. We apply a 0.5 s forgiveness collar around reference boundaries (i.e., exclude a ±0.25 s window around each annotated speaker-change from scoring to tolerate boundary imprecision) [41, 42]. 

Real-time factor (RTF). To quantify throughput, we report RTF, defined as the ratio of total wall-clock processing time to the corresponding input audio duration [44]: 

RTF = tproc 

taudio 

, (2) where tproc is the end-to-end elapsed time required to process an utterance and taudio is the duration of the input audio. RTF can be interpreted as the seconds of compute per second of au-dio : where RTF < 1 indicates faster-than-real-time process-ing, RTF ≈ 1 indicates real-time operation, and RTF > 1

indicates the system falls behind live audio [44]. 

Latency. We operate in chunk-wise streaming infer-ence with bounded look-ahead and report algorithmic latency 

(i.e., delay induced by buffering future context), distinct from hardware-dependent computational delay [7]. Let the chunk length (update period) be C seconds and the right-context (look-ahead) be R seconds. At each update, the system buffers an additional R seconds of future audio and emits speaker labels in blocks of length C.We summarize the latency as the effective decision delay in-duced by these two control parameters, which is typically mod-eled as the sum of chunk length and right-context length [7, 45]: Algorithmic latency ≈ C + R. 

Right-context sweep. We fix C = 1 .0 s and sweep R

over log-spaced values to quantify the effect of additional right-context. 

Chunk length sweep. We fix R = 0 and sweep C over log-spaced values to quantify the effect of the chunk length. As linear spacing in seconds would overemphasize the high-latency settings, we use log-spaced sweeps because these latency control parameters are strictly positive and their impact is typically scale-sensitive per standard practice for tuning pos-itive, scale-sensitive hyperparameters [46, 47]. 

4.3. Pruning Baseline. In our experiments, we keep the embed-ding/clustering backend and released pipeline thresholds fixed (Sec. 4.1), and apply all pruning (and any recovery fine-tuning) only to the segmentation model parameters. We apply iterative, magnitude-based structured pruning to the SIMSAMU segmentation model[39, 37]. Our goal is to per-form gradual pruning with recovery fine-tuning to obtain struc-tured throughput and memory savings while preserving accu-racy [48, 49, 50]. We use three disjoint subsets to avoid evalu-ation leakage: train for any recovery fine-tuning after pruning, calib reserved for calibration-only procedures used later (quan-tization), and eval for metric reporting. For each target pruning ratio ρ ∈ { 0.2, 0.4, 0.6, 0.8}, we reload the baseline model and prune gradually over K iterations. In each iteration, we remove an additional fraction ∆ρ = ρ/K of the lowest-importance units, so the model reaches the same final pruning level with better stability than one-shot pruning [48, 49]. Let u denote a prunable unit in the segmentation network. Depending on the pruning structure, u is either (i) a BiLSTM hidden unit (grouped across gates and directions) or (ii) a neu-ron/channel in the post-LSTM linear stack. Let θu be the set of parameters associated with u. We score units by weight magni-tude, 

s(u) = ∥θu∥F , (3) and at each step prune the ∆ρ fraction of remaining units with the smallest s(u) [48, 49]. 

Hidden (BiLSTM hidden size). We treat each BiLSTM hidden unit as a unit u (grouped across the four LSTM gates and both directions). Concretely, for a hidden index c, θu collects the corresponding gate rows/blocks in the recurrent weights (and associated biases). We prune the lowest-scoring hidden units and rebuild the BiLSTM with a smaller hidden size. 

Linear (post-LSTM linear channels). We prune neu-rons/channels in the post-LSTM linear stack. For an interme-diate channel c, we score it by the magnitude of its incident weights in adjacent linear layers (e.g., column in the incoming linear weight and row in the outgoing linear weight) and rebuild narrower linear layers by keeping the top-scoring channels. After each pruning iteration, we run a short supervised fine-tuning phase on train to recover performance, using RTTM-derived speech activity targets with BCEWithLogitsLoss 

and AdamW [20, 48, 49]. Final pruned variants are reported with DER, RTF, parameter count, and model size (Table 1). Pa-rameters are reported as the total number of trainable weights and model size is the on-disk serialized checkpoint size. 

4.4. Quantization 

We evaluate six numeric-precision modes for the segmenta-tion model (Table 1). Quantization is applied to the selected pruned model by lowest DER , and we ensure that evaluation clips never appear in training or calibration [23, 51]. We denote compute precision as W bw Aba, where bw and ba are the weight and activation bit-widths, respectively. For the INT4 setting, we use W4A8 as activations are typically more distribution-shifted and outlier-prone [52, 53]. Keeping them at 8-bit im-proves stability. FP32, floating-point baselines, runs the re-leased pipeline reference without modification. FP16 uses mixed-precision inference with CUDA autocast [54]. All quan-tized variants described below are implemented with PyTorch AO ( torchao ). For post-training quantization (PTQ), we re-place each nn.Linear layer in the segmentation model with a quantized inference module and calibrate activation scales on 

calib only, following standard static PTQ practice [23, 55]. We report INT8 PTQ and INT4 PTQ. Low-bit PTQ can be sensi-tive to activation and weight outliers, so we follow the stan-dard calibration procedure on calib to set quantization ranges [23, 55]. For quantization-aware training (QAT), we replace 

nn.Linear layers with quantized training modules, fine-tune on train split, then convert to real quantized inference operators [23, 51, 56]. After conversion, we run activation calibration on 

calib and evaluate on eval .For each model, we report DER, RTF, and model size, while parameter count remains unchanged. We also report the per-0.02 0.05 0.1 0.2 0.35 0.5 0.75 1 2 4 Baseline Right-context duration (look-ahead, seconds) 

> 10
> 15
> 20
> 25
> 30 DER (%)
> DER

Figure 1: Effect of right-context R (seconds, x-axis) on DER (%, y-axis ) at fixed chunk length. DER is mostly stable for small R and increases sharply at larger right-context values. 0.02 0.05 0.1 0.2 0.35 0.5 0.75 1 2 4 Baseline Chunk duration (update period, seconds) 

> 20
> 40
> 60
> 80 DER (%)
> DER

Figure 2: Effect of chunk length duration C (seconds, x-axis) on DER (%, y-axis) with right-context fixed at R = 0 . Very small chunks degrade DER sharply, while moderate-to-large chunks reaches a stable plateau. 

recording change in DER ( ∆DER) relative to the FP32 baseline, summarized by the median, interquartile range (IQR), and tail statistics (90th percentile and maximum). 

# 5. Results 

5.1. Latency: Does additional delay change diarization ac-curacy? 

With a fixed chunk length ( C = 1 .0 s), does increasing right-context ( R) change performance? Fig. 1 shows that perfor-mance is largely stable for small-to-moderate R. Large right-context values can shift performance noticeably, indicating that increasing R is not consistently beneficial in this pipeline. The sharp change around R≈1 s is consistent with the typical turn-changes in dispatch calls, where buffering that much future au-dio can blur speaker-change boundaries. 

With zero right-context ( R = 0 ), how does the chunk length (C) change performance? Fig. 2 shows that very small chunks (0.02–0.10 s) substantially change error, while C ≳ 0.1 s yields a stable plateau where further increases in C cause only small changes. This suggests that moderate chunk lengths are suffi-cient for stable behavior, with diminishing returns if the length is increased further. 

5.2. How do structured pruning level and type impact ac-curacy and efficiency? 

Table 1 shows a clear accuracy–compression trade-off, but the effect depends strongly on the pruning structure. Hidden-unit pruning substantially reduces parameters and model size, yet accuracy degrades sharply even at low pruning levels and worsens rapidly. In contrast, linear-channel pruning changes model size only marginally and preserves accuracy better at Table 1: Hidden-unit pruning yields large model-size reductions but degrades accuracy sharply, while linear-channel pruning preserves accuracy better at low-to-moderate levels with only modest model size changes. Baseline denotes the released un-pruned FP32 segmentation model. 

> 2

Variant Params (M) Size (MB) RTF DER (%) Baseline 1.47 5.63 0.0232 10.70 Hidden–20 1.01 3.86 0.0225 19.24 Hidden–40 0.69 2.65 0.0221 27.71 Hidden–60 0.47 1.82 0.0224 35.88 Hidden–80 0.33 1.26 0.0224 50.47 Linear–20 1.46 5.59 0.0225 14.94 Linear–40 1.45 5.55 0.0223 12.66 Linear–60 1.45 5.52 0.0222 25.97 Linear–80 1.44 5.50 0.0222 35.86 

Table 2: Quantization sweep on the pruned segmentation model (Linear–40). Lower precision substantially reduces model size, but end-to-end RTF changes are small under this pipeline. FP16 yields the lowest DER among quantized models. 

Mode Precision (W/A) Size (MB) RTF DER (%) FP32 W32A32 5.55 0.0223 12.66 FP16 W16A16 2.78 0.0250 15.02 INT8 PTQ W8A8 1.54 0.0227 23.67 INT8 QAT W8A8 1.54 0.0226 19.02 INT4 PTQ W4A8 0.89 0.0227 47.80 INT4 QAT W4A8 0.89 0.0229 44.93 

low-to-moderate levels, with Linear–40 achieving the lowest DER among pruned models. Across all variants, RTF remains nearly unchanged ( ≈0.022–0.023), indicating that structured pruning provides limited end-to-end RTF gains in this pipeline even when parameter counts drop. This likely occurs because the measured end-to-end runtime is dominated by non-pruned pipeline stages and kernel overheads, so reducing segmentation parameters does not translate into proportional RTF speedups. 

5.3. How do quantization modes impact model size and ac-curacy under model pruning? 

Table 2 summarizes the quantization trade-offs on the selected pruned model (Linear–40). Among lower-precision variants, 

FP16 attains the lowest DER, outperforming both INT8 modes, while INT4 variants substantially degrade accuracy. Across both INT8 and INT4, QAT improves over PTQ since fine-tuning lets the model adapt to quantization noise and reduces rounding error at inference. Relative to the unpruned FP32 baseline, the final Linear–40 + FP16 variant is 2.0× smaller at a 40% relative DER increase, while RTF is essentially unchanged. To assess stability, Fig. 3 reports the distribution of per-recording ∆DER relative to FP32 Linear–40. FP16 is closest to baseline, with a small median increase and a tight interquar-tile range. INT8 variants exhibit modest median degradation but substantially higher variability, including high-∆DER outliers. INT8 QAT reduces this spread compared to INT8 PTQ. In con-−5 0 10 20 30 40 50      

> ΔDER vs FP32 LinearPrune-40 FP16 INT8_QAT INT8_PTQ INT4_QAT INT4_PTQ Quantization mode
> IQR (Q1–Q3) Median P90 Max
> ΔDER =0

Figure 3: Per-recording diarization error change under quan-tization on Linear–40 The x-axis shows ∆DER (percentage points) and the y-axis lists quantization modes. The red dashed line at ∆DER = 0 indicates parity with the FP32 Linear– 40 baseline. Diamonds mark the 90th percentile (P90) and ×

marks the maximum ∆DER across recordings. FP16 is closest to baseline, with the smallest median ∆DER and a tight IQR. 

trast, INT4 (PTQ/QAT) yields a consistently large positive shift across the distribution, indicating systematic accuracy loss. 

# 6. Discussion 

This work is a first step toward making efficiency claims in diarization more interpretable and comparable across studies. Many diarization pipelines are evaluated with papers reporting DER, but omitting the key deployment settings (e.g., chunk length, look-ahead, precision). Our results show that these configuration choices can influence accuracy, so two systems may not be directly comparable even when they use the same high-level architecture. While this study does not introduce a new diarization model, it characterizes settings that drive the accuracy–efficiency trade-off and calls for attention to report them for resource-constrained inference. A key implication is that model compression does not auto-matically translate to end-to-end throughput gains in a complete diarization stack. While low-bit quantization and structured pruning can reduce model storage, the measured RTF changes only marginally, suggesting that runtime may be dominated by non-quantized components (feature extraction, I/O, clustering) or kernel effects. For deployment-oriented speech research, this motivates evaluating efficiency at the pipeline level rather than at checkpoints with faster diarization. 

Practical considerations. These findings suggest several reporting practices for deployment-oriented diarization. Com-pression should be evaluated at the full-pipeline level, since re-ducing the segmentation model size may not improve end-to-end throughput. DER should remain the main operational met-ric, but segmentation-level diagnostics can help explain where errors enter the pipeline. Chunk-wise inference should also be distinguished from fully online diarization, which requires per-sistent speaker-state tracking. Accordingly, conclusions about accuracy–efficiency trade-offs should be tied to the specific pipeline, hardware setting, and quantization backend used. 

Limitations. SIMSAMU is a small, specialized medical-dispatch simulation corpus with two primary roles, so ab-solute DER levels and the observed compression thresholds may not generalize [6]. Our conclusions are based on a spe-cific segmentation–embedding–clustering pipeline; different ar-chitectures could change sensitivity to latency settings (right-context and chunking) and to pruning or reduced-precision arithmetic. Finally, efficiency is measured in one runtime en-vironment, and throughput-memory observations can vary with kernels and hardware generation. 

Future work. Future work can evaluate additional lan-guages and more diverse interaction conditions (e.g., third-party speakers, background speech, and higher overlap), and jointly optimize latency parameters ( C and R) with pruning and quan-tization to map the full accuracy–efficiency trade-off. It can also isolate contributions from the segmentation versus embed-ding and clustering stages, and include robustness analysis un-der higher overlap to quantify degradation under compression. 

# 7. Acknowledgments 

We thank Colby HPC Cluster and especially Stephen Cousins for their support with the compute resources. 

# 8. Generative AI Use Disclosure 

Generative AI tool (ChatGPT) was used for LaTex table format-ting assistance. 

# 9. References 

[1] D. O’Shaughnessy, “Speaker Diarization: A Review of Objectives and Methods,” Applied Sciences , vol. 15, no. 4, p. 2002, 2025, overview of objectives and methods for identifying speaker turns (“who spoke when”). [2] T. J. Park, N. Kanda, D. Dimitriadis, K. J. Han, S. Watanabe, and S. Narayanan, “A Review of Speaker Diarization: Recent Advances With Deep Learning,” Computer Speech & Language ,vol. 72, p. 101317, 2022, survey defining speaker diarization as labeling recordings to identify “who spoke when”. [3] X. Anguera, S. Bozonnet, N. Evans, C. Fredouille, G. Friedland, and O. Vinyals, “Speaker Diarization: A Review of Recent Re-search,” IEEE Transactions on Audio, Speech, and Language Pro-cessing , vol. 20, no. 2, pp. 356–370, 2012. [4] N. Ryant, K. Church, C. Cieri, A. Cristia, J. Du, S. Ganapathy, and M. Liberman, “The Third DIHARD Diarization Challenge,” in Proc. INTERSPEECH 2021 – 22 nd Annual Conference of the International Speech Communication Association , Brno, Czech Republic, Aug. 2021, pp. 3570–3574. [5] I. Medennikov, T. Park, W. Wang, H. Huang, K. Dhawan, J. Wang, J. Balam, and B. Ginsburg, “Streaming Sortformer: Speaker Cache-Based Online Speaker Diarization with Arrival-Time Or-dering,” in Proc. INTERSPEECH 2025 – 26 th Annual Conference of the International Speech Communication Association , Rotter-dam, The Netherlands, Aug. 2025, pp. 5238–5242. [6] A. Nun, O. Birot, G. Guibon, F. Lapostolle, and I. Lerner, “Sim-samu - a french medical dispatch dialog open dataset,” Computer Methods and Programs in Biomedicine , vol. 268, p. 108857, 2025. [7] B. Rahou and H. Bredin, “Multi-latency look-ahead for stream-ing speaker segmentation,” in Proc. INTERSPEECH 2024 – 25 th 

Annual Conference of the International Speech Communication Association , Kos, Greece, Sep. 2024, pp. 1610–1614. [8] A. Baevski, H. Zhou, A. Mohamed, and M. Auli, “wav2vec 2.0: A framework for self-supervised learning of speech representa-tions,” in Advances in Neural Information Processing Systems (NeurIPS) , 2020. [9] J. Han, P. P´ alka, M. Delcroix, F. Landini, J. Rohdin, J. ˇ Cernock´ y, and L. Burget, “Efficient and generalizable speaker diarization via structured pruning of self-supervised models,” 2025. [10] U.S. Department of Health & Human Services, “Guidance regarding methods for de-identification of protected health information in accordance with the hipaa privacy rule,” https://www.hhs.gov/hipaa/for-professionals/special-topics/ de-identification/index.html, 2025, accessed 2026-02-17. [11] D. Wiepert, B. A. Malin, J. R. Duffy, R. L. Utianski, J. L. Stricker, D. T. Jones, and H. Botha, “Reidentification of participants in shared clinical data sets: Experimental study,” JMIR AI , vol. 3, p. e52054, 2024. [12] B. Durmus, B. Munyampirwa, E. Pacheco, A. Orhon, and A. Leonov, “SDBench: A Comprehensive Benchmark Suite for Speaker Diarization,” in Interspeech 2025 , 2025, pp. 1598–1602. [13] F. Kynych, P. Cerva, J. Zdansky, T. Svendsen, and G. Salvi, “A lightweight approach to real-time speaker diarization: from audio toward audio-visual data streams,” EURASIP Journal on Audio, Speech, and Music Processing , vol. 2024, p. 62, 2024. [14] J. Xue, Y. Fujita, S. Horiguchi, N. Kanda, K. Nagamatsu, and S. Watanabe, “Online Neural Diarization with Speaker-Tracing Buffer,” in Proc. INTERSPEECH 2021 – 22 nd Annual Conference of the International Speech Communication Association , Brno, Czech Republic, Aug. 2021, pp. 3370–3374. [15] J. M. Coria, H. Bredin, S. Ghannay, and S. Rosset, “Overlap-Aware Low-Latency Online Speaker Diarization Based on End-to-End Local Segmentation,” in 2021 IEEE Automatic Speech Recognition and Understanding Workshop (ASRU) , 2021, pp. 1139–1146. [16] Y. Xue, S. Horiguchi, Y. Fujita, S. Watanabe, and K. Nagamatsu, “Online End-to-End Neural Diarization with Speaker-Tracing Buffer,” in 2021 IEEE Spoken Language Technology Workshop (SLT) , 2021, pp. 841–848. [17] W.-N. Hsu, B. Bolte, Y.-H. H. Tsai, K. Lakhotia, R. Salakhutdi-nov, and A. Mohamed, “Hubert: Self-supervised speech represen-tation learning by masked prediction of hidden units,” IEEE/ACM Transactions on Audio, Speech, and Language Processing , 2021. [18] S. Chen, C. Wang, Z. Chen, Y. Wu, S. Liu, Z. Chen, J. Li, N. Kanda, T. Yoshioka, X. Xiao et al. , “Wavlm: Large-scale self-supervised pre-training for full stack speech processing,” IEEE Journal of Selected Topics in Signal Processing , vol. 16, no. 6, pp. 1505–1518, 2022. [19] J. Yi et al. , “Empowering speaker segmentation with self-supervised learning,” Electronics Letters , 2025. [20] J. Han, F. Landini, J. Rohdin, A. Silnova, M. Diez, J. ˇ Cernock´ y, and L. Burget, “Fine-tune Before Structured Pruning: Towards Compact and Accurate Self-Supervised Models for Speaker Di-arization,” in Proc. INTERSPEECH 2025 – 26 th Annual Con-ference of the International Speech Communication Association ,Rotterdam, The Netherlands, Aug. 2025, pp. 1583–1587. [21] Y. Peng, Y. Sudo, S. Muhammad, and S. Watanabe, “Dphubert: Joint distillation and pruning of self-supervised speech models,” in Proc. Interspeech 2023 , 2023, pp. 62–66. [22] T. Gu, B. Liu, H. Shao, and Y. Qian, “Sparsewav: Fast and ac-curate one-shot unstructured pruning for large speech foundation models,” in Proc. Interspeech 2024 , 2024, pp. 4498–4502. [23] B. Jacob, S. Kligys, B. Chen, M. Zhu, M. Tang, A. Howard, H. Adam, and D. Kalenichenko, “Quantization and training of neural networks for efficient integer-arithmetic-only inference,” in 

Proceedings of the IEEE Conference on Computer Vision and Pat-tern Recognition (CVPR) , June 2018, pp. 2704–2713. [Online]. Available: https://openaccess.thecvf.com/content cvpr 2018/ html/Jacob Quantization and Training CVPR 2018 paper.html [24] R. Krishnamoorthi, “Quantizing deep convolutional networks for efficient inference: A whitepaper,” 2018. [Online]. Available: https://arxiv.org/abs/1806.08342 [25] S. Kim, A. Gholami, Z. Yao, M. W. Mahoney, and K. Keutzer, “I-bert: Integer-only bert quantization,” in Proceedings of the 38th International Conference on Machine Learning (ICML) ,ser. Proceedings of Machine Learning Research, vol. 139, 2021. [Online]. Available: https://proceedings.mlr.press/v139/kim21d. html [26] T. Gu, B. Liu, H. Wang, and Y. Qian, “Ultra-Low Bit Post-Training Quantization of Large Speech Models via K-Means Clustering and Mixed Precision Allocation,” in Interspeech 2025 ,2025, pp. 1988–1992. [27] D. Wagner, I. Baumann, K. Riedhammer, and T. Bocklet, “Out-lier Reduction with Gated Attention for Improved Post-training Quantization in Large Sequence-to-sequence Speech Foundation Models,” in Interspeech 2024 , 2024, pp. 4623–4627. [28] Y. Hong, H. Han, W. jin Chung, and H.-G. Kang, “StableQuant: Layer Adaptive Post-Training Quantization for Speech Founda-tion Models,” in Proc. IEEE International Conference on Acous-tics, Speech and Signal Processing (ICASSP) , 2025, pp. 1–5. [29] B. J. Kang and H. Kim, “GenPTQ: Green Post-Training Quantization for Large-Scale ASR Models with Mixed-Precision Bit Allocation,” in Findings of the Association for Computational Linguistics: EMNLP 2025 . Association for Computational Linguistics, Nov. 2025, pp. 10 704–10 718. [Online]. Available: https://aclanthology.org/2025.findings-emnlp.566/ [30] E. Fish, U. Michieli, and M. Ozay, “A Model for Every User and Budget: Label-Free and Personalized Mixed-Precision Quantiza-tion,” in Interspeech 2023 , 2023, pp. 3232–3236. [31] R. Aperdannier, S. Schacht, and A. Piazza, “An approach to opti-mize inference of the diart speaker diarization pipeline,” 2024. [32] H. Bredin, “pyannote.audio 2.1 speaker diarization pipeline: prin-ciple, benchmark, and recipe,” in Proc. INTERSPEECH 2023 – 24 th Annual Conference of the International Speech Communica-tion Association , Dublin, Ireland, Aug. 2023, pp. 1983–1987. [33] “medkit/simsamu dataset (hugging face datasets),” https:// huggingface.co/datasets/medkit/simsamu, accessed 2026-02-14. [34] “Large language models for all: Deliverables for WP4 (d4.1 SimSAMU dataset),” https://synalp.gitlabpages.inria.fr/llm4all/ deliverables/WP4 deliverables.pdf, Inria / SYNALP (LLM4All project), Tech. Rep., 2024, accessed 2026-02-14. [35] HeKA Research Team, “Computing metrics on an au-dio dataset,” medkit documentation, 2025, example eval-uation code for SIMSAMU including DER with collar. [Online]. Available: https://medkit.readthedocs.io/en/stable/ cookbook/audio dataset metrics.html [36] “medkit/simsamu-diarization: Simsamu diarization pipeline (hugging face model card),” https://huggingface.co/medkit/ simsamu-diarization, accessed: 2026-02-15. [37] “pyannote/segmentation-3.0 (hugging face model card),” https: //huggingface.co/pyannote/segmentation-3.0, accessed: 2026-02-15. [38] “pyannote/wespeaker-voxceleb-resnet34-lm (hugging face model card),” https://huggingface.co/pyannote/ wespeaker-voxceleb-resnet34-LM, accessed: 2026-02-15. [39] medkit, “Simsamu segmentation model (medkit/simsamu-segmentation),” Hugging Face model repository, 2024, accessed: 2026-02-20. Config file specifies a PyanNet segmentation model (SincNet front-end; 4-layer BiLSTM with 128 hidden units; 2-layer linear stack with 128 hidden units). [Online]. Available: https://huggingface.co/medkit/simsamu-segmentation [40] N. Ryant, K. Church, C. Cieri, A. Cristia, J. Du, S. Ganapathy, and M. Liberman, “Second DIHARD challenge evaluation plan (version 1.1),” PDF, Mar. 2019, dIHARD II official scoring defini-tions for DER/JER. [Online]. Available: https://dihardchallenge. github.io/dihard2/docs/second dihard eval plan v1.1.pdf [41] pyannote contributors, “pyannote.metrics reference documen-tation: Diarization metrics (der/jer) and collar/overlap set-tings,” Documentation, 2025. [Online]. Available: https: //pyannote.github.io/pyannote-metrics/reference.html [42] H. Bredin, “pyannote.metrics: A Toolkit for Reproducible Evalu-ation, Diagnostic, and Error Analysis of Speaker Diarization Sys-tems,” in Proc. INTERSPEECH , 2017, pp. 3587–3591. [43] H. W. Kuhn, “The hungarian method for the assignment problem,” 

Naval Research Logistics Quarterly , vol. 2, no. 1–2, pp. 83–97, 1955. [44] J. Heitkaemper, J. Caroselli, A. Narayanan, and N. Howard, “Tf-Cleanformer: A streaming, array-agnostic, full- and sub-band modeling front-end for robust ASR,” in Proc. INTERSPEECH ,2024. [45] M. Cheng, Y. Lin, and M. Li, “Sequence-to-sequence neural diarization with automatic speaker detection and representation,” arXiv:2411.13849 [eess.AS], 2025, accepted by IEEE/ACM Transactions on Audio, Speech, and Language Processing. [Online]. Available: https://arxiv.org/abs/2411.13849 [46] M. Feurer and F. Hutter, “Hyperparameter optimization,” F. Hut-ter, L. Kotthoff, and J. Vanschoren, Eds. Springer, 2019, pp. 3–38. [47] J. Bergstra and Y. Bengio, “Random search for hyper-parameter optimization,” Journal of Machine Learning Research , vol. 13, pp. 281–305, 2012. [48] S. Han, H. Mao, and W. J. Dally, “Deep compression: Compressing deep neural networks with pruning, trained quantization and huffman coding,” arXiv:1510.00149, 2015. [Online]. Available: https://arxiv.org/abs/1510.00149 [49] M. H. Zhu and S. Gupta, “To prune, or not to prune: Exploring the efficacy of pruning for model compression,” arXiv:1710.01878, 2017. [Online]. Available: https://arxiv.org/abs/1710.01878 [50] H. Li, A. Kadav, I. Durdanovic, H. Samet, and H. P. Graf, “Prun-ing filters for efficient ConvNets,” in International Conference on Learning Representations (ICLR) , 2017, arXiv:1608.08710. [51] PyTorch, “Quantization — pytorch documentation,” Docu-mentation, 2025, accessed: 2026-02-15. [Online]. Available: https://docs.pytorch.org/docs/stable/quantization.html [52] G. Xiao, J. Lin, M. Seznec, H. Wu, J. Demouth, and S. Han, “Smoothquant: Accurate and efficient post-training quantization for large language models,” in Proceedings of the 40th Interna-tional Conference on Machine Learning (ICML) , ser. Proceedings of Machine Learning Research, vol. 202, 2023. [53] T. Dettmers, M. Lewis, Y. Belkada, and L. Zettlemoyer, “Llm.int8(): 8-bit matrix multiplication for transformers at scale,” in Advances in Neural Information Processing Systems (NeurIPS) , 2022. [54] PyTorch, “Automatic mixed precision package — torch.amp ,” Documentation, 2025, accessed: 2026-02-15. [Online]. Available: https://docs.pytorch.org/docs/stable/amp.html [55] ——, “Pytorch 2 export post training quantization,” Doc-umentation tutorial, 2026, accessed: 2026-02-15. [Online]. Available: https://docs.pytorch.org/ao/stable/pt2e quantization/ pt2e quant ptq.html [56] ——, “Torchao qat api reference,” Documentation, 2026, accessed: 2026-02-15. [Online]. Available: https://docs.pytorch. org/ao/stable/api reference/api ref qat.html