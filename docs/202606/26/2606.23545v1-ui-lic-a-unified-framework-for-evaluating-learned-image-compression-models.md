---
title: "UI-LIC: A Unified Framework for Evaluating Learned Image Compression Models"
title_zh: "UI-LIC: 一个用于评估学习图像压缩模型的统一框架"
authors: "Nicholas J. Nolen, Luc Trudeau, Andrew C. Freeman"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23545v1"
tags: ["query:compress"]
score: 9.0
evidence: 统一评估框架用于学习型图像压缩模型，直接匹配图像压缩主题
tldr: 学习型图像压缩模型评估因软件栈、训练条件和评估方法差异而困难。UI-LIC开源框架集成6个高性能LIC模型，提供统一训练、推理与分析平台。通过GUI可等比特率比较传统编码器质量指标，并支持交互式热图分析。该框架降低了LIC研究门槛，促进比较与主观分析。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23545v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 665, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2606.23545v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1744, \"height\": 818}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23545v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 1026}]"
motivation: 解决因异构软件、不同训练和评估方法导致的LIC模型比较困难问题。
method: 集成6个高性能LIC模型，提供集中控制器和GUI，实现等比特率质量指标计算及热图分析。
result: 一键等比特率比较PSNR、SSIM、VMAF、LPIPS指标，并支持交互式质量热图叠加。
conclusion: UI-LIC降低了LIC研究门槛，使模型比较和主观分析更便捷。
---

## 摘要
学习图像压缩（LIC）系统的评估和比较因异构软件栈、不同的训练条件和多样的评估方法而变得复杂。为了解决这些挑战，我们引入了UI-LIC，这是一个用于评估LIC模型的开源软件框架。我们集成了六个高性能LIC模型，并提供了一个集中式控制器，用于执行训练、推理和分析，共享配置参数。我们的GUI程序提供了一个简化的界面，用于评估这些模型以及传统的视频帧内编码器，均衡压缩比特率并计算质量指标，如PSNR、SSIM、VMAF和LPIPS。最后，我们提供了一个交互式图像分析器，具有可配置的质量热图覆盖。我们的框架降低了进一步LIC研究的门槛，通过单个设置命令即可解锁比较指标和主观分析。该开源软件在MIT许可证下发布，可在github.com/BaylorMultimediaLab/UI-LIC获取。

## Abstract
The evaluation and comparison of Learned Image Compression (LIC) systems is complicated by heterogeneous software stacks, varying training conditions, and divergent evaluation methodologies. To address these challenges, we introduce UI-LIC, an open-source software framework for evaluating LIC models. We integrate six high-performance LIC models, and provide a centralized controller for performing training, inference, and analysis with shared configuration parameters. Our GUI program offers a streamlined interface to evaluate these models alongside traditional video intra-frame encoders, equalizing the compressed bitrates and calculating quality metrics such as PSNR, SSIM, VMAF, and LPIPS. Finally, we provide an interactive image analyzer with configurable quality heatmap overlays. Our framework lowers barriers to further LIC research, unlocking comparative metrics and subjective analysis with a single setup command. The open-source software is released under the MIT license and is available at github.com/BaylorMultimediaLab/UI-LIC.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机与背景）
学习图像压缩（LIC）系统在实际评估和比较中面临严重困难：
- **异构软件栈**：不同模型依赖不同Python环境、CUDA版本、配置文件格式，集成多个模型需要大量工程工作。
- **训练条件差异**：训练数据集、损失函数、优化策略、模型架构、检查点选择等均会影响最终性能，导致报告增益的归因不清晰。
- **评估方法不统一**：不同工作使用不同测试集、质量指标、预处理步骤，使得跨论文的比较不可靠。
- **生成式LIC的挑战**：扩散模型等可能产生“幻觉”伪影（视觉合理但语义失真），需要结合客观指标与可视化分析。

因此，UI-LIC开源框架旨在提供一个**统一的训练、评估、比较平台**，降低LIC研究门槛，促进可复现、透明、公平的比较。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
- **模块化解耦**：分离模型训练、推理、可视化，通过集中式接口统一管理。
- **一键设置**：单命令自动创建每个模型所需虚拟环境并下载官方预训练权重，包括VMAF的Docker容器。
- **参数解析接口**：统一构建训练/测试命令，验证参数，在相应Conda环境中执行。
- **作业配置与调度**：用户通过JSON文件指定模型、数据集、作业参数；调度器检查必需参数后按类别执行。

### 关键技术细节
1. **集成模型**：6个LIC模型（ELIC、StableCodec、TCM、HPCM、DCVC-RT intra、RwkvCompress）及传统编码器（AVC/H.264、HEVC/H.265、AV1），通过FFmpeg调用。
2. **模型适配修改**：
   - DCVC-RT：开发专用帧内训练/推理脚本。
   - StableCodec：标准化测试使用ImageFolder；将epochs转换为steps。
   - ELIC/TCM：编写统一评估脚本保存比特流和解码结果。
   - 全部模型：替换硬编码路径、增加输入验证和错误处理。
3. **评估管道**：
   - **指标**：PSNR（加权/YUV）、SSIM、VMAF、LPIPS（含5个特征层分解）、bpp、推理延迟。
   - **误差图**：MSE、块SSIM、归一化梯度、LPIPS各层特征图。
   - **比特率均衡**：对传统编码器进行QP搜索，使压缩比特率尽可能接近LIC模型的比特率，实现公平比较。
4. **GUI交互工具**（Tkinter）：
   - 选择数据集、启用模型、设置QP/权重路径。
   - 运行推理后，侧边栏滑动对比两编解码器的结果，可叠加LPIPS特征图（不同颜色、独立开关），显示整体度量报告。

## 3. 实验设计
### 数据集
- Kodak数据集（True Color Kodak Images），共24张图像，常用于图像压缩基准。

### Benchmark与对比方法
- **LIC模型**：HPCM、LIC-TCM、RwkvCompress、StableCodec、DCVC-RT intra、ELIC（共6个）。
- **传统编码器**：AVC（x264/NVENC）、HEVC（x265/NVENC）、AV1（SVTAV1/NVENC）。
- 所有结果均在**等比特率**条件下比较（传统编码器通过QP搜索达到目标比特率）。

### 主要实验结果（表1）
- 展示每个LIC模型及其对应的传统编码器在相同平均bpp下的PSNR、SSIM、LPIPS、VMAF。
- 示例发现：DCVC-RT 在0.47 bpp下以PSNR 39.21、SSIM 0.917、LPIPS 0.11、VMAF 89.74全面优于传统编码器；而StableCodec在极低比特率（0.03 bpp）下PSNR仅27.56，说明生成式模型在低码率下失真大。

## 4. 资源与算力
论文中**未明确说明**所使用的GPU型号、数量、训练时长等算力信息。仅提及：
- 各模型使用其官方指定的Python/CUDA版本及虚拟环境。
- VMAF计算运行在Docker容器中。
- 所有预训练权重为官方提供，未报告重新训练所需的算力。

## 5. 实验数量与充分性
- **实验数量**：仅展示一组对比（Kodak数据集上的平均指标），无消融实验、无跨数据集（如CLIC、DIV2K）验证。
- **公平性**：采用比特率均衡，传统编码器QP搜索保证比特率匹配；但LIC模型使用官方预训练权重，未针对Kodak微调，且不同LIC模型训练条件本身不同（论文开头即强调此问题，但框架未解决训练差异的公平比较）。
- **充分性判断**：作为框架演示，实验规模较小，但展示了核心功能（统一调用、指标计算、可视化）。对于全面的方法比较，需要更多数据集和消融研究。

## 6. 主要结论与发现
- UI-LIC框架成功降低了整合和比较LIC模型所需工程努力，使研究者能快速在相同条件下评估多个模型。
- 通过交互式可视化（LPIPS热图、误差图），可直观分析不同模型的空间误差模式，尤其适用于检测生成式模型的“幻觉”伪影。
- 示例表明DCVC-RT在Kodak上综合表现优于传统编码器，而StableCodec在极低码率下仍保持视觉合理性但客观指标差，揭示生成式LIC的评估挑战。

## 7. 优点
- **开源与可扩展**：MIT许可证，支持添加新模型（通过继承基础接口）。
- **自动化环境隔离**：一键创建各模型独立虚拟环境，避免依赖冲突。
- **丰富指标与可视化**：集成客观、感知指标及LPIPS层分解、MSE/SSIM误差图，支持半透明叠加分析。
- **比特率均衡机制**：使LIC与传统编码器的比较更公平。
- **GUI降低使用门槛**：图形化操作，适合非工程背景研究者。

## 8. 不足与局限
- **实验覆盖有限**：仅使用Kodak一个数据集，缺乏多样性场景验证。
- **训练差异未统一**：不同LIC模型的预训练权重来自不同出处，框架仅负责调用，无法保证训练数据的公平性（如是否在同一数据集上训练）。
- **无消融研究**：未评估框架中不同组件（如LPIPS层可视化）对分析的实际贡献。
- **算力需求未报告**：缺少运行时性能基准，用户难以评估部署成本。
- **传统编码器配置固定**：仅使用FFmpeg默认/简单调用，未深入优化传统编码器参数（如码率控制模式），可能导致传统编码器性能被低估。
- **生成式LIC的比较难题**：框架建议结合主观分析，但未提供标准化的主观实验工具。

（完）
