---
title: Balancing Image Compression and Generation with Bootstrapped Tokenization
title_zh: 通过自举令牌化平衡图像压缩与生成
authors: "Haozhe Chi, Jinghan Li, Hao Jiang, Wu Sheng, Yi Ma, Jing Wang, Yadong Mu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05552v1"
tags: ["query:compress"]
score: 9.0
evidence: 通过自引导令牌化实现图像压缩与生成
tldr: "图像分词中，标准方法混合不同粒度信息导致冗余且加大生成器负担。本文提出SelfBootTok，通过自引导学习将信息解耦为全局和局部token组，由全局token预测局部细节，使生成器仅需全局token，计算量减少约40%。在重建和生成任务上表现更优，仅用64个token达到gFID 1.56的新纪录。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1019, \"height\": 1016, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1367, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1386, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1021, \"height\": 553, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 671, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 679, \"height\": 413, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05552v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1285, \"height\": 988, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 653, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 374, \"label\": \"Table\"}]"
motivation: 现有图像分词方法混合不同粒度信息，导致token间冗余并增加生成器训练难度。
method: SelfBootTok通过自引导学习将图像信息解耦为全局和局部token组，全局token负责预测局部细节。
result: "生成器仅使用全局token，计算量减少约40%，gFID达1.56（64个token），超越现有方法。"
conclusion: 该范式通过分离信息粒度，提升了tokenizer与generator的效率与性能，且可扩展。
---

## 摘要
尽管图像令牌化取得了进展，但标准方法通过在每个令牌中混合所有粒度来编码冗余信息，因此在令牌之间仍然存在冗余。不同粒度信息的混合也增加了生成器训练的复杂性。本文介绍了SelfBootTok，该方法通过将信息清晰分解为全局和局部令牌组来解决这一问题。通过自举学习，模型仅从全局令牌预测局部细节，将视觉细节的负担从生成器转移到令牌化器。因此，我们的生成器效率大大提高，仅需全局令牌即可减少约40%的计算量，同时实现卓越的重建与生成。此外，该范式优雅地扩展：通过利用更多数据或参数来自监督局部表示学习，SelfBootTok仅使用64个令牌就达到了1.56的新最优gFID分数。

## Abstract
Despite progress in image tokenization, standard methods encode redundant information by mixing all granularities within each token, thus redundancy persists between tokens. The mix of information of different granularity also complicates the training of generators. This paper introduces SelfBootTok, a method that resolves this by cleanly decomposing information into global and local token groups. Through self-bootstrapped learning, the model predicts local details exclusively from global tokens, shifting the burden of visual details from the generator to the tokenizer. Consequently, our generator is far more efficient, requiring only global tokens and reducing computation by approximately 40%, while delivering superior reconstruction and generation. Moreover, this paradigm scales elegantly: by leveraging more data or parameters to self-supervise local representation learning, SelfBootTok achieves a new state-of-the-art gFID score of 1.56 using only 64 tokens.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的图像令牌化（image tokenization）方法（如VQ-GAN、TiTok等）在每个token中混合了不同粒度的信息（全局语义和局部细节），导致token间存在冗余，并且这种混合信息使得下游生成器（如扩散Transformer）训练困难——生成器必须同时处理所有粒度的视觉细节。
- **整体含义**：论文提出一种新的令牌化范式，将信息清晰地解耦为**全局token**（高层语义）和**局部token**（细粒度细节），并通过自举（self-bootstrapped）学习让全局token直接预测局部细节。这相当于将部分生成任务前移到压缩管道中，使得生成器只需要处理少量的全局token，从而降低计算复杂度并提升生成质量。

## 2. 方法论
- **核心思想**：采用**全局-局部分解**（global-local decomposition）和**自监督自举学习**（self-supervised bootstrapped learning）。图像首先由ViT编码器（基于DINOv2）编码为全局token z_G；然后通过两个分支分别预测局部1D token z_L1和局部2D token z_L2。局部1D由简单MLP从全局token映射得到；局部2D由因果Transformer自回归生成。所有token均使用可微的软向量量化（SoftVQ）并分配不同的码本（全局、局部1D、局部2D）。解码阶段，全局和局部令牌融合后经ViT解码器重建图像。
- **关键技术细节**：
  - **自举预测**：公式(8) z_L1 = MLP(z_G)；公式(9) 用因果Transformer建模p_θ(z_L2 | z_G)。
  - **对齐损失**：包括全局对齐（与DINOv2特征）、局部对齐（与I-JEPA/SigLIP等特征）和**最优传输损失**（2D VAE潜变量与1D token序列的对齐，使用Sinkhorn算法求解）。
  - **融合策略**：软残差融合或拼接融合（公式11），并采用交叉注意力注入局部token。
  - **可扩展训练**：两阶段策略——第一阶段训练完整管道（小局部对齐器）；第二阶段冻结全局部分，仅扩大局部2D对齐器并微调，生成器不需重新训练。
- **算法流程**：编码→全局token→自举预测局部1D/2D→软量化→融合→解码。生成时仅需生成全局token，局部由对齐器自动补全。

## 3. 实验设计
- **数据集**：ImageNet-1K，分辨率256×256。
- **基准（Benchmark）**：使用rFID（重建FID）、PSNR、SSIM评估重建质量；使用gFID（生成FID，含/不含CFG）评估生成质量。对比了2D tokenizer（VAE、SD-VAE、VAR-Tok、VQGAN、VA-VAE等）、自回归tokenizer（MaskGIT、LlamaGen、RQ-VAE、MAGE、MAR-H、ViTok等）以及1D tokenizer（TiTok、FlexTok、OneD-piece、HieraTok、GigaTok、MAETok、SoftVQ等）。
- **对比方法**：共列出现有约20种方法，覆盖2D、自回归、1D三大类。
- **实验数量**：重建性能对比（表1）、生成性能对比（表1）、消融实验（表2、表3、图5、图6）覆盖局部对齐器选择（I-JEPA vs DINOv2 vs SigLIP）、设计组件消融（有无局部1D/2D、因果、码本）、融合策略、缩放规模（10M~600M参数）。
- **结论**：SelfBootTok在64 token下rFID 0.66（优于其他1D tokenizer），gFID 1.56（达到当时SOTA），重建和生成均持平甚至优于部分256 token方法。

## 4. 资源与算力
- 文中明确提到：使用DINOv2 ViT-Base作为骨干（173M参数），局部对齐器规模从10M到600M。训练时，小对齐器训练50 epoch，大对齐器额外50~75 epoch。
- **计算效率**：由于生成器只需训练一次，总计算量减少约40%（从4.9B参数、60K GFLOPs降至2.95B、36.6K GFLOPs），训练时间缩短约54%（从28天减至13天）。
- **具体GPU型号、数量未明确说明**，仅提及分布式训练，但未给出详细配置。这是信息不充分之处。

## 5. 实验数量与充分性
- **实验数量**：较为充分。包括主要性能对比（表1）、三种局部编码器消融（表2）、设计组件消融（表3）、渐进式组件分析（图5）、缩放规模实验（图6）。此外有定性结果（图2）。
- **充分性评价**：
  - **优点**：对比了多种主流tokenizer，覆盖全。消融实验覆盖了关键设计（局部分支、码本、因果、融合）。
  - **不足**：仅在一个数据集（ImageNet-1K 256×256）上评估，缺乏零样本跨域、高分辨率、文本到图像等场景的验证。生成模型仅使用了SiT，未测试其他生成架构（如DiT、LlamaGen等）。计算成本对比仅给出相对比例，绝对算力（GPU小时）未详述。

## 6. 主要结论与发现
- **核心发现**：通过全局-局部分解和自举预测，可将视觉细节负担从生成器转移到tokenizer，生成器仅需64个全局token即可达到SOTA生成质量（gFID 1.56），且计算量降低约40%。
- **缩放有效性**：局部对齐器规模从10M扩展到600M，重建和生成性能持续提升，表明该范式可良好扩展。
- **设计消融**：局部1D和2D分支都有帮助；因果建模、独立局部码本、I-JEPA作为局部对齐器效果最优；拼接融合有利于重建但不利生成，存在任务相关权衡。
- **生成器效率**：生成器仅需训练一次，局部对齐器可独立缩放，实现并行优化。

## 7. 优点
- **方法设计新颖**：首次将自举学习引入图像tokenization，实现信息解耦，理论上减少了token间冗余。
- **效率提升显著**：生成器计算量减少约40%，训练时间缩短54%，且生成质量不降反升。
- **可扩展性强**：局部对齐器可独立缩放，生成器无需重训，体现了模块化设计。
- **完全自监督**：局部预测仅需无标注图像，不依赖文本-图像对。
- **结果竞争力**：在64 token下超越了众多256 token的方法，尤其gFID 1.56为当时1D tokenizer最优。

## 8. 不足与局限
- **实验覆盖有限**：仅测试ImageNet-1K 256×256，未涉及更高分辨率、更多样化数据集（如COCO、LSUN）或文本到图像生成任务，泛化性未充分验证。
- **生成架构单一**：仅验证了SiT一种生成器，未与其他流行生成器（如DiT、LlamaGen、VAR）对比，缺少对生成器兼容性的证明。
- **计算资源描述不充分**：未给出具体GPU型号和数量，复现成本难以估算。
- **局部对齐器依赖预训练模型**：使用了DINOv2、I-JEPA、SigLIP等外部编码器，这些模型本身需要大量预训练，可能存在数据泄露或偏差。
- **重建质量仍有提升空间**：rFID 0.66 vs 2D tokenizer的0.27（VAE-LDM），在重建精确度上仍有差距，说明局部细节恢复不够完美。
- **应用限制**：目前仅面向生成式任务，未讨论在判别式下游任务（分类、检测等）中的表现；全局token是否保留足够语义信息尚待验证。

（完）
