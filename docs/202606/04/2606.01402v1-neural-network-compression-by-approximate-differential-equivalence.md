---
title: Neural Network Compression by Approximate Differential Equivalence
title_zh: 基于近似微分等价的神经网络压缩
authors: "Ravi Dhiman, Andrea Passarella, Mirco Tribastone, Lorenzo Valerio"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01402v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 通过聚合动力学相似神经元压缩模型，属于模型优化
tldr: 神经网络压缩通常依赖局部重要性剪枝，但忽略了神经元间的功能相似性。本文提出利用微分等价聚合行为相似的神经元，将网络编码为多项式ODE系统并应用近似前向微分等价识别动态匹配的神经元。在合成数据和回归基准上，该方法实现大幅参数缩减且保持精度，优于幅度剪枝和Wanda。该工作提供了一种基于功能聚集的剪枝替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01402v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1182, \"height\": 239, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01402v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1248, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01402v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 438, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01402v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1286, \"height\": 695, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 618, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 626, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1272, \"height\": 603, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1271, \"height\": 691, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1272, \"height\": 692, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1271, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1263, \"height\": 170, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01402v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1272, \"height\": 960, \"label\": \"Table\"}]"
motivation: 现有剪枝方法独立移除权重，忽略了神经元间行为相似性，导致信息冗余。本文旨在通过聚合功能相似的神经元实现更高效的压缩。
method: 将训练网络转化为多项式ODE系统，利用近似前向微分等价识别动态相似的神经元，通过单一容差参数控制压缩程度。
result: 在合成动态系统和回归基准上，该方法实现显著参数减少且保持准确率，压缩性能一致优于幅度剪枝和Wanda。
conclusion: 基于微分等价的神经元聚合为网络压缩提供了一种原则性且有效的替代方案，超越传统权重中心剪枝。
---

## 摘要
神经网络压缩通常通过基于局部重要性得分（例如基于幅度剪枝）来修剪参数实现。我们提出了一种互补方法，通过聚合具有相似功能行为的神经元来压缩模型，而不是独立移除权重。我们的方法将训练好的网络编码为多项式ODE系统，并应用一种称为近似前向微分等价的聚集方法，以识别具有近似匹配诱导动力学的神经元。单个容差参数ε控制压缩水平，并在模型大小与预测精度之间产生平滑权衡。我们在源自具有已知真实行为的非线性动力系统的合成数据集以及公共回归基准上评估了该方法。在这两种设置下，所提出的方法在保持精度的同时实现了显著的参数减少，并且在相似压缩水平下始终优于基于幅度剪枝和Wanda方法。这些结果表明，基于微分等价的聚合是传统以权重为中心的剪枝的一种有原则且有效的替代方案。

## Abstract
Neural network compression is commonly achieved by pruning parameters based on local importance scores, e.g., magnitude-based pruning. We propose a complementary approach that compresses models by aggregating neurons with similar functional behavior rather than removing weights independently. Our method encodes a trained network as a polynomial ODE system and applies a lumping method called Approximate Forward Differential Equivalence to identify neurons with approximately matching induced dynamics. A single tolerance parameter, $\varepsilon$, controls the compression level and induces a smooth trade-off between model size and predictive accuracy. We evaluate the method on synthetic datasets derived from nonlinear dynamical systems with known ground-truth behavior and on public regression benchmarks. Across both settings, the proposed approach achieves substantial parameter reduction while preserving accuracy, and consistently compares favorably with magnitude-based pruning and Wanda at similar compression levels. These results suggest that differential equivalence-based aggregation is a principled and effective alternative to conventional weight-centric pruning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

传统的神经网络压缩主要依赖基于局部重要性得分的剪枝（如幅度剪枝），即独立移除权重。但这种方法忽略了神经元之间的功能相似性，可能导致冗余保留或有用结构被破坏。本文提出一种互补的压缩思路：**通过聚合功能行为相似的神经元（而非移除权重）来减小模型规模**。这一方法基于微分等价理论，将网络视为动力系统，通过识别动态近似相同的神经元并将其合并，实现压缩。其核心优势在于提供了一种有原则的、可解释的替代方案，并通过单一参数 ε 平滑控制压缩‑精度权衡。

### 2. 论文提出的方法论

- **核心思想**：将训练好的前馈神经网络编码为多项式常微分方程（ODE）系统，然后利用**近似前向微分等价（ε‑FDE）** 找出动态行为接近的神经元，将其聚合成“块和变量”（block-sum variable），从而得到一个参数更少的聚合网络。
- **关键技术细节**：
  1. **编码为多项式 ODE**：对每个神经元输出定义状态变量 \(x_i^{(\ell)}\)，其“导数”由网络的前向计算式（权重乘输入加偏置后经多项式激活函数）给出，得到一个多项式初值问题（PIVP）。
  2. **计算 ε‑FDE 划分**：在多项式 ODE 的系数空间中，若两个神经元对任意块和的导数贡献之差在所有单变量系数上被 ε 界住，则视为近似等价。通过工具 ERODE 计算出最大（最粗）的 ε‑FDE 划分。
  3. **构造参考模型与商系统**：存在一个与原系统系数偏差不超过 ε 的参考模型，使得该划分是精确 FDE，从而块和变量满足封闭的商动力方程。
  4. **聚合网络生成**：直接用商动力方程的参数构造一个聚合后的神经网络 \(N_{agg}\)，其输出近似原网络输出，误差为 O(ε)。
- **算法流程**（文字描述）：
  - 输入：训练好的网络 \(N\)、容差 ε。
  - Step 1：将 \(N\) 编码为多项式 ODE 系统 \(q\)。
  - Step 2：用 ERODE 计算 \(q\) 的 ε‑FDE 划分 \(P\)。
  - Step 3：导出商动力方程 \(g\)。
  - Step 4：根据 \(g\) 构造聚合网络 \(N_{agg}\)。
  - 输出：压缩后的网络 \(N_{agg}\)。

### 3. 实验设计

- **数据集**：
  - **合成动态系统**（10 个种子）：反应网络（RN）、H‑树电路（Electrical Network）、聚合动力学（POL）、糖酵解振荡器（GLY）。每种生成 30000 个样本（70% 训练，20% 验证，10% 测试）。
  - **公共回归基准**（30 个种子）：Abalone、Metro Interstate Traffic Volume、HEPC（家庭电力消耗）、Protein (CASP)。特征标准化处理。
- **网络架构**：三种 MLP 变体（Variant 1 / 2 / 3），宽度和深度不同，使用二次激活函数以保持多项式 ODE 表示。
- **对比方法**：幅度剪枝（MBP）和 Wanda。两种方法在相同压缩水平（GRP%）下进行比较。
- **评价指标**：均方误差（MSE）和全局剩余参数比例（GRP%）。
- **超参数**：通过网格搜索选定（epochs: 200/400, lr: 0.1/0.01/0.005, batch size: 20/30, patience: 20/30/50），并在所有方法间固定。

### 4. 资源与算力

- 文中未明确提及使用的 GPU 型号、数量或训练时长。仅提到使用 Adam 优化器，并在 CPU/通用工作站上进行 ODE 编码与 ERODE 分析。因此无法量化算力消耗。

### 5. 实验数量与充分性

- **实验数量**：合成系统方面，4 个系统 × 3 种变体 × 5~7 个不同 ε/剪枝配置，每种设置重复 10 个随机种子，报告 95% 置信区间。公共数据集方面，4 个数据集 × Variant 1 × 7 个配置，重复 30 个种子。此外，还展示了 ε 的灵敏度分析（图 2 和图 3）。
- **充分性与公平性**：实验覆盖了从无压缩到极端压缩的广泛区间；基线方法在相同 GRP% 下对比，且超参数固定；置信区间较窄说明结果稳定。灵敏度分析表明 ε 是平滑控制参数。整体实验设计较充分、客观、公平。

### 6. 论文的主要结论与发现

1. **一致优越的压缩‑精度权衡**：在几乎所有压缩水平下，提出的方法均保持接近原模型的 MSE，而 MBP 和 Wanda 在相似 GRP% 时 MSE 往往高出几个数量级。
2. **深度不影响压缩效率**：更深的变体（Variant 3）依然能通过聚合实现大幅参数减少，且精度损失极小，说明方法利用的是功能冗余而非浅层结构。
3. **ε 提供可解释控制**：增加 ε 单调提高压缩率、缓慢增加误差，存在一个宽泛的“安全”操作区域。
4. **在真实数据上同样有效**：四个公共基准测试中，本方法在 GRP% 降至 10~20% 时仍能保持合理 MSE，而基线方法早已严重退化。

### 7. 优点

1. **原则性强**：基于动力系统对称性理论，而非启发式重要性评分。
2. **单一参数控制**：ε 直接调节压缩‑精度权衡，无需多阶段微调。
3. **实验设计严谨**：多种数据集、多种架构、多随机种子、置信区间报告，与主流方法公平对比。
4. **稳定性好**：置信区间窄，说明对初始化和数据划分的敏感性低。
5. **可解释性**：聚合的是动态行为相似的神经元，具有一定的物理解释。

### 8. 不足与局限

1. **表示限制**：方法要求网络激活函数是多项式（本文用了二次激活），无法直接应用于 ReLU、Sigmoid 等非多项式激活的网络。
2. **可扩展性**：多项式 ODE 编码及 ERODE 的分区计算在大规模网络（如 ResNet、Transformer）上可能因状态空间庞大而不可行。文中也提到通过分解宽层为小层来缓解，但尚未在巨型模型上验证。
3. **ε 手动调整**：ε 的取值目前依赖人工试探，缺乏自适应策略；不同数据集的最佳 ε 可能差异很大。
4. **实验覆盖不全面**：仅在小型 MLP 与回归任务上测试，未覆盖图像分类、NLP 等广泛应用，也未与更多先进剪枝方法（如 SparseGPT、GraNet）对比。
5. **未考虑实际加速效果**：聚合后的网络参数减少，但缺乏在硬件上的推理速度或能耗测量。
6. **偏差风险**：在合成数据上表现优异，但真实数据的噪声和异构性可能使“功能相似”的定义不够鲁棒。

（完）
