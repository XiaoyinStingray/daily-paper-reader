---
title: Training-Free Continuous Bitrate Control for Scalable Image Coding for Humans and Machines
title_zh: 无训练的可扩展图像编码连续比特率控制用于人类与机器
authors: "Yui Tatsumi, Hiroshi Watanabe"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2606.00158v1"
tags: ["query:compress"]
score: 9.0
evidence: 直接针对图像压缩中量化步长调整实现连续码率控制
tldr: 现有可伸缩图像编码缺乏连续变比特率能力，本文提出无需训练的连续比特率控制框架。通过预测尺度值调整量化步长，实现码率连续可调，同时保留机器层与增强层的高尺度信息。实验证明该方法有效且层间比特分配是关键。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00158v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 903, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00158v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 906, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00158v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.00158v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 377, \"label\": \"Figure\"}]"
motivation: 实际应用需要连续变比特率压缩，但可伸缩图像编码中尚未充分探索。
method: 基于预测的尺度值动态调整量化步长，无需额外训练即可控制码率。
result: 实现了连续比特率控制，机器层和增强层的高尺度信息保持良好。
conclusion: 该方法有效且层间比特率分配对性能至关重要。
---

## 摘要
在现实应用中，连续变比特率压缩需求很高，但在面向人类与机器的可扩展图像编码中仍探索不足。本文提出了一种无训练的变比特率可扩展图像编码框架。通过基于预测尺度值调整量化步长，所提方法在机器层和增强层中保留高尺度信息的同时实现了连续比特率控制。实验结果证明了该方法的有效性，并突出了两层之间比特率分配的重要性。

## Abstract
Continuous variable-rate compression is highly demanded in real-world applications, but remains underexplored in scalable image coding for humans and machines. In this paper, we propose a training-free variable-rate scalable image coding framework. By adjusting quantization steps based on predicted scale values, the proposed method achieves continuous bitrate control while preserving high-scale information in the machine and enhancement layers. Experimental results demonstrate the effectiveness of the proposed method and highlight the importance of bitrate allocation between the two layers.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现实应用（如交通监控）中，图像主要被机器分析（识别任务），仅偶尔由人类查看，因此需要一种既能支持机器识别、又能支持人类视觉重建的**可扩展图像编码**方案。然而，现有基于学习的可扩展编码方法大多不支持**连续比特率控制**（variable-rate），导致在实际部署中缺乏灵活性。
- **背景**：已有的可扩展编码框架（如FR-ICMH）虽能将机器层和增强层解耦，但只能生成固定码率的码流；而变比特率控制方法（如AQVR-ICM）仅适用于单一机器层，未扩展到双层的可扩展结构。因此，需要一种**无需重新训练**、能独立控制两层码率的连续比特率控制方法。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将AQVR-ICM中的**尺度感知自适应量化控制器**（scale-aware adaptive quantization controller）扩展到双层结构（机器层和增强层），通过预测的尺度参数（scale）动态调整每个通道/空间位置的量化步长，实现连续比特率控制，且不改变预训练模型的权重。
- **关键技术细节**：
  - 采用FR-ICMH架构，机器层（machine layer）编码任务无关的特征，增强层（enhancement layer）编码残差信息以恢复人类视觉图像。
  - 用户给定两个独立控制参数 \(d_m\)（机器层）和 \(d_h\)（增强层），分别控制两层码率。
  - 基于通道自回归熵模型（channel-wise autoregressive entropy model），潜变量被划分为N个通道切片（channel slices）。
  - **量化步长确定分两阶段**：
    1. **切片级边界**：为每个切片设定最小和最大量化步长。当 \(0<d<1\) 时，前几个切片获得更细的量化（\(\Delta_{\min}^{(n)}\) 较小）；当 \(d>1\) 时，后几个切片获得更粗的量化（\(\Delta_{\max}^{(n)}\) 较大）。公式(1)(2)。
    2. **空间自适应映射**：在每个切片内，根据该切片中每个通道的尺度值 \(\sigma_{c,h,w}\) 的分布，将量化步长线性插值到 \([\Delta_{\min}^{(n)}, \Delta_{\max}^{(n)}]\) 区间内，如公式(4)。高尺度（重要）的通道获得更细的量化。
- **优势**：由于尺度参数在推理时可由超先验网络直接获取，该方法可直接应用于预训练的FR-ICMH模型，无需任何额外训练。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：COCO-val数据集（Microsoft COCO，2014版验证集），用于识别性能和重建质量的评估。
- **评估指标**：
  - 机器识别性能：mAP@50:95（对YOLOv5和Mask R-CNN的检测/分割任务）。
  - 人类视觉重建性能：PSNR（峰值信噪比）。
- **对比方法**：
  - 均匀量化控制（uniform quantization，即每层所有通道用相同量化步长）。
  - 固定码率可扩展方法FR-ICMH（原论文最高码率点）。
  - 人类视觉专用编码LIC-TCM（混合Transformer-CNN架构的端到端压缩方法）。
- **实验场景**：
  - 仅改变机器层参数 \(d_m\)，观察识别性能随码率变化。
  - 固定 \(d_m = 4\)，改变增强层参数 \(d_h \in \{1,2,4,8,16\}\)，比较重建质量。
  - 分析不同 \(d_m\) 对增强层码率和总码率下PSNR的影响（两层比特分配问题）。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量或训练时长。由于方法**无需训练**（training-free），仅在预训练模型上做推理和量化控制，因此不涉及额外训练算力。但未报告推理时的硬件配置或时间开销。

## 5. 实验数量与充分性
- **实验组数**：共展示了4组结果：
  1. **表I**：不同 \(d_m\)（10, 20, 40, 80, 160）下的bpp和MAP@50:95（三个模型：YOLOv5检测、Mask R-CNN检测、Mask R-CNN分割）。
  2. **图2**：人类视觉重建的定性对比（两张示例图片）。
  3. **图3**：PSNR vs 增强层码率/总码率曲线，与均匀量化、FR-ICMH、LIC-TCM对比（固定 \(d_m=4\)）。
  4. **图4**：不同 \(d_m\) 下增强层码率与总码率分别对应的PSNR曲线。
- **充分性分析**：
  - **优点**：覆盖了机器层变码率、增强层变码率、以及层间分配三个关键维度；对比了均匀量化和现有固定码率方法；使用了标准的COCO数据集和主流识别模型（YOLOv5, Mask R-CNN）。
  - **不足**：
    1. 实验仅用了单个数据集COCO-val，未在ImageNet、Cityscapes等其他数据集上验证泛化性。
    2. 仅测试了识别任务中的检测和分割，未包括分类、跟踪等其他机器视觉任务。
    3. 未对不同预训练骨干（如不同的FR-ICMH变体）进行消融或更多基线的对比（例如与变比特率方法直接对比，但本文是首个无需训练的变比特率可扩展方法，缺乏同类型对标方法）。
    4. 未报告实验的随机种子或多次重复结果，可能存在偶然性。
- **客观公平性**：对比的基线（均匀量化、FR-ICMH、LIC-TCM）均为合理且公开的方法，实验设置清晰。但未对FR-ICMH本身的性能进行复现验证，仅使用作者提供的预训练模型。

## 6. 论文的主要结论与发现
1. **有效性**：所提方法能实现机器层和增强层的独立连续比特率控制，且性能优于均匀量化，证明了尺度感知自适应量化的优势。
2. **比特分配的关键性**：在总码率固定时，适当降低机器层码率（增大 \(d_m\)）可以改善人类视觉重建的RD性能；但在增强层单独视角下，更小的 \(d_m\)（更高机器层质量）有利于增强层效率。因此，最优的层间比特分配取决于目标优先权（识别优先还是重建优先）。
3. **应用潜力**：无需训练的设计使得该方法易于部署到现有预训练模型，适应多种实际场景。

## 7. 优点：方法或实验设计上的亮点
- **无需训练**：完全基于推理时的量化步长调整，复杂度低，可直接应用于已训练的FR-ICMH模型，极具实用性。
- **可解释性**：基于尺度值（scale）的量化策略与信息重要性直观对应（高尺度 → 重要特征 → 更细量化），且公式简洁明确。
- **双层独立控制**：提供两个独立参数 \(d_m\) 和 \(d_h\)，用户可分别调节机器层和增强层码率，灵活性高。
- **发现层间比特分配问题**：通过实验揭示了识别性能与重建RD之间存在的权衡，为后续优化提供了方向。

## 8. 不足与局限
- **实验覆盖不足**：仅使用COCO一个数据集，且只验证了检测和分割任务；未涉及分类、语义分割等其他常见机器视觉任务。人类视觉重建对比仅使用了PSNR指标，未包括MS-SSIM等感知指标。
- **未与现有可变码率SOTA方法直接比较**：论文本身是首个针对可扩展结构的变比特率方法，但未与将单独变比特率方法（如AQVR-ICM、TCM变码率版本）组合到可扩展框架中的替代方案比较。缺少消融实验证明尺度感知比简单阈值更优。
- **计算开销未量化**：虽然无需训练，但推理时每切片/每通道的计算复杂度未给出，可能在高分辨率或实时场景中存在瓶颈。
- **预设模型依赖**：方法依赖FR-ICMH的预训练权重和特定的通道自回归熵模型结构，推广到其他可扩展架构（如不同熵模型或分层结构）需要验证。
- **参数选择缺乏自动化**：用户需手动设定 \(d_m\) 和 \(d_h\)，未提供自动比特分配策略，实际使用中可能需要额外调参。
- **论文篇幅较短**（仅2页），未提供算法伪代码、详细实现细节或更多消融（如不同N值影响、ϵ取值等）。

（完）
