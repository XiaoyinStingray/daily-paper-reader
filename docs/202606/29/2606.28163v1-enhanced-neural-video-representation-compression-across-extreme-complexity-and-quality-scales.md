---
title: Enhanced Neural Video Representation Compression across Extreme Complexity and Quality Scales
title_zh: 极端复杂度和质量尺度下的增强型神经视频表示压缩
authors: "Ho Man Kwan, Tianhao Peng, Fan Zhang, Mike Nilsson, Andrew Gower, David Bull"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28163v1"
tags: ["query:compress"]
score: 9.0
evidence: 神经视频压缩与率失真优化
tldr: 隐式神经表示（INR）在视频压缩中展现出潜力，但现有神经视频编码器难以平衡复杂度和可扩展性。本文提出NVRC++，采用轻量级INR搭配多个高分辨率特征网格，结合优化框架高效过拟合长视频序列，并设计先进熵模型压缩高维网格参数。NVRC++提供从7k到360k MACs/pixel的四个复杂度级别，每个级别覆盖宽广比特率和质量范围，支持实时解码。与最优INR视频编码器NVRC相比，解码速度提升7.6倍，性能相当，实现了统一架构下的复杂度和质量可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28163v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1227, \"height\": 417, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1251, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28163v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 387, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28163v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 556, \"height\": 303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28163v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 646, \"height\": 304, \"label\": \"Table\"}]"
motivation: 现有神经视频编码器在复杂度和可扩展性上存在权衡，轻量模型缩放时性能下降，高性能模型复杂度随质量增加，缺乏统一架构。
method: 采用轻量级INR配合多高分辨率特征网格，提出优化框架避免计算开销，并设计熵模型高效压缩高维网格参数。
result: NVRC++提供四个复杂度级别，每个覆盖宽比特率范围，支持实时解码，解码速度比NVRC快7.6倍，性能相当。
conclusion: NVRC++实现了跨复杂度与质量的高可扩展性，为多样化部署提供了统一架构。
---

## 摘要
隐式神经表示（INR）最近成为视频压缩的一种有前景的方法，提供了具有竞争力的率失真性能以及快速解码。然而，现有的神经视频编解码器难以平衡复杂度和可扩展性。轻量级模型在扩展到不同码率/质量级别时往往会降低压缩性能，而高性能模型的可扩展性有限，因为其模型复杂度通常随质量增加而增加。缺乏能够在广泛码率范围内保持一致复杂度的统一架构严重限制了它们在多样化现实世界中的部署。为了解决这些挑战，我们引入了NVRC++，这是一种基于INR的新型视频编解码器，利用轻量级INR结合多个高分辨率特征网格，在任何给定的复杂度水平下提供高可扩展性。同时配有一个优化框架，能够有效过拟合长视频序列的高分辨率网格，从而利用时空冗余而不会带来过高的计算或内存开销。此外，设计了一种先进的熵模型，用于高效压缩高维网格参数。因此，NVRC++提供了四种复杂度级别（从7kMACs/像素到360kMACs/像素），每个级别覆盖广泛的码率和质量范围，同时支持实时解码。实验结果表明，与最先进的基于INR的视频编解码器NVRC相比，NVRC++提供了更快的解码速度（高达7.6倍），同时提供相当的性能。

## Abstract
Implicit neural representations (INRs) have recently emerged as a promising approach to video compression, delivering competitive rate-distortion performance alongside rapid decoding. However, existing neural video codecs struggle to balance complexity and scalability. Lightweight models often suffer from degraded compression performance when scaled to different bitrate/quality levels, whereas high-performance models exhibit limited scalability, as their model complexity typically increases with quality. This lack of a unified architecture capable of maintaining consistent complexity across a wide range of bitrates severely limits their diverse real-world deployment. To address these challenges, we introduce NVRC++, a novel INR-based video codec that utilizes a lightweight INR with multiple high-resolution feature grids, providing high scalability at any given complexity level. This is paired with an optimization framework that enables efficient overfitting on high-resolution grids for long video sequences, thereby exploiting spatio-temporal redundancies without prohibitive computational or memory overhead. Additionally, an advanced entropy model is designed for efficiently compressing the high-dimensional grid parameters. As a result, NVRC++ provides four complexity levels (from 7kMACs/pixel to 360kMACs/pixel), each spanning wide bitrate and quality ranges while supporting real-time decoding. The experimental results show that NVRC++ offers a much faster decoding speed (up to 7.6x) compared to the SOTA INR-based video codec, NVRC, while delivering comparable performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

当前基于隐式神经表示（INR）的视频压缩方法在率失真性能和解码速度上展现出潜力，但存在关键的“可扩展性”瓶颈：
- **轻量模型**：当扩展到不同码率/质量级别时，压缩性能大幅下降。
- **高性能模型**：模型复杂度随目标质量提高而增加，导致高码率下解码计算量巨大，无法在固定硬件预算下实现宽质量范围。
- **缺乏统一架构**：现有方法要么只支持窄码率范围，要么复杂度随质量变化，阻碍了实际部署（如边缘设备、云服务等需要确定性的计算复杂度）。

该论文旨在解决上述问题，提出一种**跨极端的复杂度和质量尺度**都能保持统一模型配置、恒定解码复杂度，同时兼顾高压缩性能的INR视频压缩框架。

### 2. 论文提出的方法论

**核心思想**：使用轻量级INR网络配合**多个高分辨率特征网格**，将视觉细节的表达负担从神经网络层迁移到特征网格中，从而在保持解码器复杂度不变的前提下实现质量可伸缩。同时设计配套的优化框架和熵模型，使高维网格参数能高效训练和压缩。

**关键技术细节**：

1. **HiNeRV++ – 改进的INR架构**：
   - 采用多尺度高分辨率特征网格（网格分辨率远高于原始HiNeRV）。
   - **多参考输入**：解码时不仅使用插值特征，还直接使用相邻的原始网格切片作为上下文，提升输入信息量。
   - 移除最高分辨率的ConvNeXt块，改用**Pixel Shuffle**层作为输出头，降低计算量。

2. **参数内编码结构 (In-parameter coding structure)**：
   - 将特征网格按时间维度划分为切片，采用**分层B帧结构**组织切片依赖。
   - 使用**非条件量化**（消除传播链）和**随机采样训练**，使在长序列上过拟合高维网格成为可能（仅需24GB显存即可编码600帧1080p视频）。

3. **特征网格掩码 (Feature grid masking)**：
   - 在训练初期对高分辨率网格特征施加类Dropout机制，强制模型优先利用低分辨率网格，防止早期过拟合局部细节，从而提升低码率性能。掩码比率随训练逐步退火至0。

4. **多维网格熵模型**：
   - **尺度先验**：低分辨率网格作为高分辨率网格的条件。
   - **时间先验**：利用已编码的历史切片作为条件。
   - **空间先验**：采用多步掩码（1-4步）模拟自回归上下文，逐步编码当前切片。
   - 统一量化步长 δ<sub>l</sub>，分布参数 μ<sub>l,t,s</sub>, σ<sub>l,t,s</sub> 由轻量网络预测。

5. **联合率失真优化**：
   - 遵循经典 R-D 目标：`argmin R(θ̂) + λ D(V, V_gt)`，端到端优化网格参数、层参数及熵模型参数。

### 3. 实验设计

- **数据集**：
  - **UVG**：7个序列，全高清（1920×1080），每序列300–600帧。
  - **MCL-JCV**：30个序列，全高清，120–150帧。
- **基准方法**：
  - 传统标准编码器：x265 (veryslow)、HM-18.0 (randomaccess)、VTM-20.0 (randomaccess)。
  - 基于自编码器的神经视频编码器：DCVC-FM、DCVC-RT。
  - 基于INR的编码器：HiNeRV、NVRC、C3。
- **评价指标**：
  - PSNR、MS-SSIM，计算BD-rate（以x265为锚）。
  - 解码复杂度：kMACs/像素、FPS。
- **NVRC++配置**：四种复杂度尺度 S1（最小）– S4（最大），每个尺度使用单一模型配置覆盖宽码率范围。

### 4. 资源与算力

论文未明确报告GPU型号、数量和训练时长。仅提及最大模型（含超1亿参数的特征网格）在“consumer GPU with only 24GB memory”上可完成600帧1080p序列的过拟合训练与编码。解码FPS在RTX 4090上测试（如表1脚注所示）。编码速度较慢（S4约0.005 FPS），但属于INR类方法的普遍特点。

### 5. 实验数量与充分性

- **数量**：在2个数据集（共37个序列）上进行了完整率失真评估；对比了7种不同类别的基线方法；给出了4个复杂度变体的详细结果。
- **消融实验**：表2验证了7个组件（低分辨率网格、无掩码、无空间/时间/尺度先验、2D/3D stem）的有效性，每个移除均导致BD-rate上升。
- **额外分析**：表3比较HiNeRV++与HiNeRV的收敛速度和质量；图4展示了NVRC++与NVRC在固定复杂度下的可扩展性对比；图5提供视觉对比。
- **公平性**：基准方法数据大多来自原论文或官方实现，指标（PSNR、MS-SSIM）标准一致；解码速度测量平台有注明差异（如A100 vs RTX 4090），但已在表1中说明。
- **结论**：实验覆盖全面，设计合理，对比充分。

### 6. 论文的主要结论与发现

1. **显著提升可扩展性**：NVRC++的每个复杂度尺度均可覆盖从低到高的宽广码率/质量范围，而NVRC等方法的复杂度随质量上升。
2. **解码速度快**：与SOTA INR方法NVRC相比，解码速度提升**高达7.6倍**（S3 vs NVRC），同时保持相当或更优的压缩性能。
3. **复杂度-性能权衡出色**：如图1所示，NVRC++在所有复杂度级别下的RD曲线均位于或接近Pareto前沿，优于同级其他方法。
4. **HiNeRV++架构优越**：通过将参数从网络层转移到特征网格，HiNeRV++以更低的计算量和参数实现更快收敛和更高质量（比HiNeRV高出2.09 dB PSNR）。
5. **熵模型有效性**：多维度先验（空间/时间/尺度）均独立带来增益，其中尺度先验贡献最大。

### 7. 优点

- **统一架构**：单模型配置可适应任意码率，符合实际部署中固定硬件预算的需求。
- **实时解码潜力**：S3变体达到73.4 FPS（不含熵解码），S1达365 FPS，满足实时交互应用。
- **创新优化策略**：参数内编码结构、随机采样、特征网格掩码等使高维网格训练可行且高效。
- **全面的消融验证**：每个设计选择都有定量依据，结论可靠。
- **广泛对比**：涵盖了传统标准、自编码器类、INR类三种主流路线，对比维度丰富。

### 8. 不足与局限

- **编码慢**：INR方法需逐视频过拟合，编码时间远高于传统编码器（S4编码仅0.005 FPS），影响了实际使用效率。论文未提供编码时间优化对比。
- **实验数据集局限**：仅使用全高清（1080p）数据集，未验证4K或更高分辨率以及低分辨率视频上的表现。
- **解码速度测量不包含熵解码**：表1中FPS括号外为主解码速度，括号内包含熵解码时间，后者较慢（约60–75 FPS），但未与基线统一覆盖。
- **硬件平台不一致**：基线方法的解码FPS来自不同平台（A100 vs RTX 4090），直接比较存在偏差，论文已尽力说明，但仍可能影响公平性。
- **长序列性能未充分探讨**：仅提及最大模型可处理600帧，但未比较不同序列长度对训练稳定性和性能的影响。
- **消融实验基于S2**：未在更大或更小尺度上验证组件增益的一致性。
- **缺少主观质量评估（如MOS）**：仅依赖PSNR/MS-SSIM，虽提供视觉对比，但未进行用户研究。

（完）
