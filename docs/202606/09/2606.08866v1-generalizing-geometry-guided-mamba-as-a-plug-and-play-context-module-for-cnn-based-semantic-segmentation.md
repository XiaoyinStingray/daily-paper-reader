---
title: Generalizing Geometry-Guided Mamba as a Plug-and-Play Context Module for CNN-based Semantic Segmentation
title_zh: 将几何引导的Mamba泛化为CNN语义分割的即插即用上下文模块
authors: "Sheng-Wei Chan, Hsin-Jui Pan, Chun-Po Shen, Chia-Min Lin, Yung-Che Wang, Jen-Shiun Chiang"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08866v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 用于CNN分割的即插即用上下文模块，改进架构设计
tldr: CNN语义分割依赖ASPP等上下文头扩大感受野，但存在计算负担和边界泄漏问题。本文从DGM-Net中提取方向几何Mamba（G-Mamba）作为即插即用上下文模块，通过边界和向心流线索引导选择性扫描，实现高效长程特征聚合。在DeepLabV3+等六个模型上替换原上下文头，Cityscapes数据集一致提升mIoU，仅增加适度计算量。表明几何引导的状态空间模型可成为CNN上下文头的实用增强方案。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08866v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 928, \"height\": 792, \"label\": \"Table\"}]"
motivation: 现有CNN上下文头计算成本高且易引入边界泄漏，需更高效的上下文聚合方法。
method: 将几何引导的状态空间模型G-Mamba作为即插即用模块，利用边界和向心流线索调制选择性扫描过程。
result: 在Cityscapes上替换六个CNN模型的上下文头，均获mIoU提升，仅增加适度GFLOPs。
conclusion: 几何引导的SSM模块可作为CNN上下文头的有效替代或增强。
---

## 摘要
基于CNN的语义分割网络通常依赖ASPP、PPM或注意力模块等上下文头来扩大感受野。这些方法虽然有效，但可能引入高计算量、内存开销或边界泄漏。本文重新审视DGM-Net中的方向几何Mamba（G-Mamba），并将其作为即插即用的上下文聚合模块而非全新的分割架构进行研究。核心思想是将几何引导注入选择性扫描过程，使长距离特征传播能够受边界和向心流线索调节。我们替换了六种代表性CNN分割模型（包括DeepLabV3+、DANet、CCNet、PSPNet、PSANet和OCRNet）的原始上下文头，同时保持ResNet-101骨干网络不变。在Cityscapes上的实验结果显示，在$1024\times1024$分辨率下，仅需适度的额外GFLOPs即可获得一致的mIoU提升，表明几何引导的SSM模块可作为传统CNN上下文头的实用替代或增强方案。

## Abstract
CNN-based semantic segmentation networks usually rely on context heads such as ASPP, PPM, or attention modules to enlarge the receptive field. These heads are effective but may introduce heavy computation, memory cost, or boundary leakage. This paper revisits Directional Geometric Mamba (G-Mamba) from DGM-Net and studies it as a plug-and-play context aggregation module rather than a complete new segmentation architecture. The key idea is to inject geometric guidance into the selective scan process, allowing long-range feature propagation to be modulated by boundary and centripetal-flow cues. We replace the original context heads of six representative CNN segmentation models, including DeepLabV3+, DANet, CCNet, PSPNet, PSANet, and OCRNet, while keeping the ResNet-101 backbone unchanged. Results on Cityscapes show consistent mIoU gains with only moderate extra GFLOPs at $1024\times1024$ resolution, suggesting that geometry-guided SSM modules can serve as practical alternatives or enhancements to conventional CNN context heads.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：CNN语义分割模型通常依赖ASPP、PPM、注意力模块等上下文头来扩大感受野，但这些模块存在计算成本高、内存开销大、边界信息泄漏等问题。
- **背景**：状态空间模型（SSM，如Mamba）能以线性复杂度建模长程依赖，但标准视觉扫描是各向同性的，缺乏几何引导，容易模糊细长结构并导致语义跨边界泄漏。
- **动机**：从DGM-Net中提取方向几何Mamba（G-Mamba），验证其能否作为一种通用的、即插即用的上下文聚合模块，替代传统CNN上下文头，从而在保持低复杂度的情况下提升分割精度和边界感知能力。

## 2. 方法论
- **核心思想**：将几何引导注入Mamba的选择性扫描过程，利用边界和向心流线索调制长程特征传播。
- **关键技术细节**：
  - **几何提示构建**：
    - 从中间特征图 \(F \in \mathbb{R}^{C\times H\times W}\) 预测三个轻量几何先验：
      - 向心势图 \(V\)：描述像素趋向物体中心的倾向。
      - 方向流场 \(\Phi\)：势图的导数，提供扫描感知的方向。
      - 粗略形态边界图 \(D_c\)：强调薄结构或高频结构（如杆、栏杆、物体轮廓）。
    - 通过残差细化分支预测 \(\Delta D\)，得到最终细节提示 \(D = \sigma(D_c + \Delta D)\)（\(\sigma\)为sigmoid）。
    - 对扫描方向 \(s\)，将流响应投影到扫描轴得到 \(\Phi_{s}^{dir}\)，空间提示 \(T_s = 1 + D \cdot \text{ReLU}(\Phi_{s}^{dir})\)。
    - 输入特征经元素级乘法重加权：\(F_s' = F \odot T_s\)。
  - **Cascade G-Mamba**：
    - 第一级：标准多方向Mamba扫描，建立宏观语义上下文（“where”）。
    - 第二级：应用上述几何提示作为精度过滤器（“what”），强化边界敏感区域。
    - 与传统ASPP/PPM不同，有效感受野由 \(D\) 和 \(\Phi\) 动态塑造，无需固定膨胀率或池化箱。
- **即插即用替换协议**：
  - 将六个CNN分割模型的原始上下文头直接替换为G-Mamba（或Cascade G-Mamba），保持ResNet-101骨干、crop大小、输出步长、训练设置不变。

## 3. 实验设计
- **数据集**：Cityscapes（城市街景语义分割数据集）。
- **Benchmark**：Cityscapes验证集mIoU。
- **对比方法**：
  - 六种基线模型：DeepLabV3+（替换ASPP）、DANet（替换双重注意力）、CCNet（替换十字交叉注意力）、PSPNet（替换PPM）、PSANet（替换逐点空间注意力）、OCRNet（替换对象上下文聚合头）。
  - 每个基线比较三种变体：原始基线、+G-Mamba、+Cascade G-Mamba。
- **设置**：所有模型使用ResNet-101骨干，输出步长8，crop尺寸 \(768\times 768\)，batch size 4。
- **补充实验**：在 \(1024\times 1024\) 分辨率下比较GFLOPs。

## 4. 资源与算力
- **GPU**：单张RTX 3080 Ti，显存12GB。
- **训练设置**：batch size 4，crop 768×768。
- **训练时长**：论文未明确说明训练轮数或时长，仅提及采用相同训练协议。
- **推理复杂度**：在1024×1024分辨率下，G-Mamba仅增加33–51 GFLOPs（视主机架构而定）。

## 5. 实验数量与充分性
- **实验数量**：
  - 主要精度对比（表1）：在Cityscapes验证集上对六个架构各三种变体（共18组数据）。
  - 计算复杂度对比（表2）：五个架构（缺少PSANet的GFLOPs数据）各两种设置。
  - 未进行其他数据集（如ADE20K）或轻量骨干的验证。
- **充分性评估**：
  - **充分之处**：覆盖了注意力型（DANet、CCNet）和卷积池化型（DeepLabV3+、PSPNet）等多种上下文头，结果一致提升，证明了通用性。
  - **不足**：
    - 仅使用Cityscapes单一数据集，缺乏跨数据集（如ADE20K、PASCAL Context）的泛化证据。
    - 未提供训练曲线、消融研究（如仅使用边界提示或仅使用流场的影响）、参数数量或延迟对比。
    - 缺少与直接替换为原始DGM-Net完整架构的对比。
    - 仅进行了模块替换，未在相同计算预算下与更高效的上下文头（如轻量ASPP）公平比较。
- **公平性**：保持骨干和训练设置不变，替换仅涉及上下文头，对比相对公平。

## 6. 主要结论与发现
- G-Mamba作为即插即用模块能一致提升六种CNN分割网络的mIoU，且附加计算量适中（+33~51 GFLOPs）。
- Cascade G-Mamba（两级级联）进一步在所有测试架构上提升性能。
- 对于注意力密集型模型（DANet、CCNet）提升尤为明显（mIoU提升超过2个点），表明几何引导的序列建模可有效替代密集注意力。
- 几何引导SSM模块可成为传统CNN上下文头的实用替代或增强方案，降低计算成本和边界泄漏风险。

## 7. 优点
- **模块化设计**：G-Mamba可即插即用嵌入现有CNN模型，无需修改骨干或训练流程。
- **几何感知**：通过边界和向心流动态调节扫描强度，减少各向同性聚合导致的模糊。
- **高效性**：Mamba的线性复杂度使其在高分辨率下仍保持适度计算增量。
- **系统迁移实验**：在六种代表性架构上验证通用性，而非仅针对单一模型。

## 8. 不足与局限
- **实验覆盖狭窄**：
  - 仅评估Cityscapes一个数据集，未在ADE20K、COCO-Stuff等更丰富场景验证。
  - 仅使用ResNet-101骨干，未尝试轻量骨干（如MobileNet、ResNet-18）或Transformer骨干。
  - 未测试多种输入分辨率或输出步长的影响。
- **消融不足**：
  - 未单独分析边界提示、流场、向心势图各自的贡献。
  - 未比较G-Mamba与简化变体（如无几何提示的标准Mamba）。
  - 未报告训练收敛速度、内存占用、推理延迟等关键指标。
- **潜在偏差风险**：
  - Cityscapes场景以城市道路为主，几何先验（边界、向心流）可能过度适配该类场景，在其他场景（如室内、医学图像）泛化性存疑。
  - GFLOPs比较未考虑硬件实测速度，且PSANet的GFLOPs数据缺失。
- **理论局限**：几何提示的依赖轻量预测头，若预测不准可能引入噪声；Cascade设计增加额外计算和工程复杂度。

（完）
