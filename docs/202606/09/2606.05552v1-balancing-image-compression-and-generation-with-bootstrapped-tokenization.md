---
title: Balancing Image Compression and Generation with Bootstrapped Tokenization
title_zh: 使用引导式分词平衡图像压缩与生成
authors: "Haozhe Chi, Jinghan Li, Hao Jiang, Wu Sheng, Yi Ma, Jing Wang, Yadong Mu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05552v1"
tags: ["query:compress"]
score: 9.0
evidence: 端到端图像压缩结合自引导标记化
tldr: "针对图像token化中信息混合导致的冗余和生成器训练困难，本文提出SelfBootTok方法，通过自引导学习将图像信息干净地分解为全局和局部token组，使模型仅用全局token预测局部细节。该范式将视觉细节负担从生成器转移到tokenizer，生成器仅需64个全局token即实现高质量生成，计算量降低约40%。在ImageNet上取得gFID 1.56的新纪录，且随数据或参数扩展性能持续提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1019, \"height\": 1016}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1367, \"height\": 693}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1386, \"height\": 660}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1021, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 671, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2606.05552v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 679, \"height\": 413}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05552v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1285, \"height\": 988}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 653, \"height\": 222}, {\"url\": \"assets/tables/arxiv/2606.05552v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 667, \"height\": 374}]"
motivation: 标准图像token化混合不同粒度信息导致冗余，生成器需处理不必要细节，训练复杂且效率低。
method: SelfBootTok通过自引导学习，令模型从全局token预测局部细节，实现信息解耦，生成器仅接收全局token。
result: "生成器计算量减少40%，64个token在ImageNet上gFID达1.56，刷新最优。"
conclusion: 该范式通过tokenizer承担细节建模，使生成更高效，且随数据/参数增大性能可扩展。
---

## 摘要
尽管图像分词取得了进展，但标准方法通过在每个词元中混合所有粒度的信息来编码冗余信息，因此词元之间仍然存在冗余。不同粒度信息的混合也使生成器的训练变得复杂。本文介绍了SelfBootTok，该方法通过将信息清晰地分解为全局和局部词元组来解决这一问题。通过自引导学习，模型仅从全局词元预测局部细节，从而将视觉细节的负担从生成器转移到分词器。因此，我们的生成器效率更高，仅需全局词元即可减少约40%的计算量，同时提供更优的重建和生成效果。此外，这种范式具有优雅的可扩展性：通过利用更多数据或参数来自监督局部表示学习，SelfBootTok仅使用64个词元就实现了1.56的新最优gFID分数。

## Abstract
Despite progress in image tokenization, standard methods encode redundant information by mixing all granularities within each token, thus redundancy persists between tokens. The mix of information of different granularity also complicates the training of generators. This paper introduces SelfBootTok, a method that resolves this by cleanly decomposing information into global and local token groups. Through self-bootstrapped learning, the model predicts local details exclusively from global tokens, shifting the burden of visual details from the generator to the tokenizer. Consequently, our generator is far more efficient, requiring only global tokens and reducing computation by approximately 40%, while delivering superior reconstruction and generation. Moreover, this paradigm scales elegantly: by leveraging more data or parameters to self-supervise local representation learning, SelfBootTok achieves a new state-of-the-art gFID score of 1.56 using only 64 tokens.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：标准图像分词方法（如VQ-VAE、TiTok等）将不同粒度（全局语义与局部细节）的信息混合在同一个token中，导致token间存在冗余，且生成器需要同时处理所有粒度的信息，训练复杂、效率低。
- **整体含义**：本文旨在通过解耦全局与局部信息，将视觉细节的建模负担从生成器转移到分词器，从而实现更高效、可扩展的图像压缩和生成。

## 2. 方法论
- **核心思想**：提出SelfBootTok，采用**全局-局部分解**和**自引导（self-bootstrapped）学习**。分词器输出三组token：全局token（zG）、局部1D token（zL1）、局部2D token（zL2）。通过自监督方式，仅从全局token预测局部token，生成器只需生成全局token，局部细节由分词器自动补全。
- **关键技术细节**：
  - 全局token由ViT编码器从图像中提取，局部1D token通过MLP从全局token预测，局部2D token通过因果Transformer自回归生成。
  - 使用**软向量量化（SoftVQ）**实现全微分化的量化。
  - 引入**最优传输（Optimal Transport）损失**对齐2D VAE潜在特征与1D token序列，采用Sinkhorn算法求解。
  - 对齐损失包含全局对齐（与DINOv2特征匹配）、局部对齐（与I-JEPA等特征匹配）和最优传输损失。
  - 解码时融合所有token（软残差融合或拼接融合），通过交叉注意力注入解码器。
- **自引导训练流程**：
  1. 固定全局编码器，训练全局token生成。
  2. 使用全局token预测局部token（自引导），仅需无标注图像。
  3. 支持**两阶段缩放**：先训练小规模局部对齐器，然后缩放局部对齐器并微调，冻结全局部分，实现生成器与分词器并行优化。

## 3. 实验设计
- **数据集**：ImageNet-1K 256×256。
- **Benchmark**：生成质量用gFID（含CFG/无CFG），重建质量用rFID、PSNR、SSIM。
- **对比方法**：
  - 2D tokenizer：VAE、SD-VAE、VAR-Tok、VQGAN、VA-VAE等。
  - 自回归tokenizer：MaskGIT、LlamaGen、RQ-VAE、MAGE、MAR-H、ViTok等。
  - 1D tokenizer：TiTok、FlexTok、OneD-piece、HieraTok、GigaTok、MAETok、SoftVQ等。
- **生成器**：使用SiT（扩散/流匹配模型），训练800K次迭代。

## 4. 资源与算力
- 文中未明确说明使用的GPU型号和数量。
- 训练时长：tokenizer训练50 epoch（小对齐器）到75 epoch（大对齐器）；SiT训练800K次迭代。
- 在缩放实验中，采用两阶段策略使总计算量降低约40%（从4.9B参数/60K GFLOPs降至2.95B/36.6K GFLOPs），训练时间从28天缩短至13天（约54%节省）。但未明确具体硬件配置。

## 5. 实验数量与充分性
- **主要实验**：表1对比了17+种方法在rFID和gFID上的表现。
- **消融实验**：
  - 表2：对比不同局部1D对齐器（I-JEPA、DINOv2、SigLIP）。
  - 表3：逐组件消融（全局、局部1D、局部2D、因果Transformer、局部codebook）。
  - 图5：渐进添加局部分支对rFID/PSNR/SSIM的影响。
  - 图6：局部2D对齐器从10M到600M参数的缩放效果。
- **充分性与公平性**：实验覆盖主流1D和2D tokenizer，评估指标全面（rFID、gFID、PSNR、SSIM），消融设计合理，对比条件一致（均使用64 token或256 token，均基于ImageNet）。结论可信。

## 6. 主要结论与发现
- SelfBootTok在仅使用64个token时，gFID达到**1.56**（带CFG），为1D tokenizer的最优结果，接近256 token的2D tokenizer（如MAR-H的1.55）。
- 重建质量rFID为0.66，优于同token数的SoftVQ（0.88）和TiTok（1.70）。
- 通过缩放局部对齐器（从10M到600M参数），生成和重建性能持续提升，且生成器只需训练一次。
- 实现约40%的计算量节省和54%的训练时间缩短。

## 7. 优点
- **创新性**：首次在1D tokenizer中显式解耦全局与局部信息，并采用自引导预测，本质将生成任务中的细节建模转移至分词器，简化生成器。
- **高效性**：生成器仅需处理全局token（64个），计算量减少40%；支持并行优化生成器和局部对齐器，无需重复训练。
- **可扩展性**：局部对齐器可独立缩放，全局编码器和生成器可复用，适用于不同计算预算。
- **实验全面**：包含多类对比、消融、缩放实验，结果客观公正。

## 8. 不足与局限
- **实验覆盖范围**：仅在ImageNet 256×256上进行评估，未在更高分辨率（如512×512）或其他领域（如医学图像、文本图像）验证。
- **算力资源未明确**：未提供GPU型号和数量，影响复现性评估。
- **偏差风险**：生成器使用SiT，仅验证一种生成框架，未测试其他扩散或自回归模型（如DiT、VAR）下的表现。
- **应用限制**：自引导预测依赖预训练视觉编码器（DINOv2、I-JEPA），可能引入特定偏置；文中未讨论对域外数据（如非自然图像）的泛化能力。

（完）
