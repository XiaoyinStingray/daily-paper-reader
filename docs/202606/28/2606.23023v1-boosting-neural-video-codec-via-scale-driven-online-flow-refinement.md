---
title: Boosting Neural Video Codec via Scale-Driven Online Flow Refinement
title_zh: 通过尺度驱动的在线光流细化提升神经视频编码器
authors: "Tiange Zhang, Rongqun Lin, Haocheng Tang, Xiandong Meng, Weijia Jiang, Zhimeng Huang, Siwei Ma"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23023v1"
tags: ["query:compress"]
score: 9.0
evidence: 神经视频编码器改进，采用尺度驱动流细化，与压缩相关
tldr: "现有神经视频编码器在复杂运动模式下泛化能力不足。针对此问题，提出无需训练的尺度驱动在线流细化方法(SOFR)，通过整合粗、细尺度运动信息并按扭曲精度动态融合，有效修正运动估计误差。该方法即插即用，计算开销极低。实验表明，在多个NVC框架上均能显著节省码率，例如为DCVC-FM平均节省2.84%和4.05%码率（PSNR/MS-SSIM），且编码时间几乎不变。SOFR以极低成本提升了NVC的泛化性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23023v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1804, \"height\": 534, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23023v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 727, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23023v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1755, \"height\": 626, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23023v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 321, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23023v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1598, \"height\": 379, \"label\": \"Table\"}]"
motivation: 现有NVC在复杂运动模式下泛化能力不足，在线微调代价高，急需无需训练的高效泛化方法。
method: 提出SOFR，通过粗-细尺度运动信息按扭曲精度动态融合修正估计误差，并引入率感知动态融合策略和可靠性检查。
result: "在USTC-TD数据集上，SOFR为DCVC-FM平均节省2.84%和4.05%码率（PSNR/MS-SSIM），编码时间几乎不变，且适用于多个NVC框架。"
conclusion: SOFR作为即插即用模块，以极低计算代价有效提升了NVC的泛化性能，在多个框架上验证了有效性和通用性。
---

## 摘要
尽管最先进的神经视频编码器（NVC）已经取得了显著性能，但在遇到训练中未见过的复杂运动模式时，它们存在泛化能力有限的问题。为了在不进行昂贵在线微调的情况下弥合这一领域差距，我们提出了一种免训练的尺度驱动在线光流细化（SOFR）方法。作为即插即用模块，SOFR整合了来自粗尺度和细尺度的运动信息，并根据扭曲精度动态融合它们，有效修正运动估计误差，计算开销可忽略不计。此外，我们设计了一种率感知策略，根据码率模式选择不同的动态融合策略，并采用基于扭曲误差的可靠性检查来确保鲁棒性。在USTC-TD数据集上的大量实验验证了SOFR在多种NVC框架（包括DCVC-SDD、DCVC-FM和EHVC）中的有效性和泛化能力。值得注意的是，对于DCVC-FM，SOFR在PSNR和MS-SSIM上分别平均节省了2.84%和4.05%的码率，而编码时间增加可忽略不计。我们的代码可在https://github.com/SunnyMass/SOFR获取。

## Abstract
Although state-of-the-art neural video codecs (NVCs) have achieved remarkable performance, they suffer from limited generalization when encountering complex motion patterns unseen during training. To bridge this domain gap without the expensive cost of online fine-tuning, we propose a Training-Free Scale-Driven Online Flow Refinement (SOFR) method. Serving as a plug-and-play module, SOFR integrates motion information from coarse and fine scales and dynamically fuses them according to warping accuracy, effectively rectifying motion estimation errors with negligible computational overhead. Furthermore, we design a rate-aware strategy that selects different dynamic fusion strategies according to bitrate modes, and employs a reliability check based on warping error to ensure robustness. Extensive experiments on the USTC-TD dataset verify the effectiveness and generalization of SOFR across various NVC frameworks, including DCVC-SDD, DCVC-FM, and EHVC. Notably, it brings an average of 2.84% and 4.05% bitrate savings in terms of PSNR and MS-SSIM, respectively, to DCVC-FM with negligible coding time increase. Our code is available at https://github.com/SunnyMass/SOFR.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义
- **研究动机**：现有的神经视频编码器（NVC）在训练数据集（如Vimeo-90k）上表现出色，但当推理时遇到训练中未见的复杂运动模式（如快速位移、不规则运动）时，其泛化能力严重下降。这是因为预训练的光流网络无法准确估计这些“域外”运动，导致运动向量（MV）不准，进而扭曲上下文特征，损害率失真（R-D）性能。
- **整体含义**：本文旨在弥合训练与推理之间的域差距，但避免使用昂贵的在线微调（需反向传播更新参数）。目标是以“免训练、即插即用”的方式，在推理阶段在线优化光流估计，提升NVC的泛化能力，同时保持极低计算开销。

## 2. 方法论
- **核心思想**：利用多尺度光流的互补性：粗尺度光流捕捉整体运动趋势，结构稳定；细尺度光流保留局部细节，但对噪声和大幅位移敏感。通过动态融合二者，并根据扭曲精度生成软掩膜（soft mask），可有效修正运动估计误差。
- **关键技术细节**：
  1. **双粒度光流生成**：
     - 细粒度流（`v_fine`）：在原始分辨率上用预训练的光流网络计算。
     - 粗粒度流（`v_coarse`）：先将输入帧下采样（因子 `s`），计算光流后再上采样恢复尺寸，并乘以 `s` 补偿缩放。
     - 下采样因子 `s` 由率感知控制模块根据量化参数（Q-Index）动态决定（高码率用 `s=2`，低码率用 `s=4`）。
  2. **率感知融合策略**：
     - 根据Q-Index区分高/低码率模式，调整超参数：下采样尺度 `s` 和决策偏置 `b`（高码率 `b=0.005`，低码率 `b=0.03`）。
     - **可靠性检查**：计算粗、细流各自的扭曲误差的平均绝对值（MAD），若粗流误差不小于细流误差，则退回使用细流（避免粗流劣化）。
     - 生成软掩膜 `M = σ(α · (E_coarse - E_fine - b))`，其中 `E` 为扭曲误差图。最终融合光流：`v_t = M ⊙ v_fine + (1-M) ⊙ v_coarse`。
  - 整个SOFR模块无需训练，仅在前向推理时执行上述操作，作为即插即用模块插入现有NVC中。

## 3. 实验设计
- **数据集**：USTC-TD（2020年代高质量基准），包含10个1920×1080分辨率的视频序列，覆盖多种运动模式（如篮球、羽毛球、舞蹈等）。使用BT.709将YUV转为RGB，推理时进行填充对齐网络下采样。
- **Benchmark**：与三个最先进的NVC基线对比——DCVC-SDD、DCVC-FM、EHVC。
- **对比方法**：
  - 直接比较加入SOFR前后的R-D性能（BD-Rate指标）。
  - 消融实验：对比固定尺度 vs. 动态尺度、有无率感知控制、有无可靠性检查。
- **实验设置**：
  - Intra Period：DCVC-SDD设32，DCVC-FM和EHVC设-1（全P帧）。
  - 每个序列取前96帧，实际编码/解码生成二进制码流，精确计算bpp和PSNR/MS-SSIM。
  - 评估指标：PSNR和MS-SSIM下的BD-Rate（负值表示码率节省）。

## 4. 资源与算力
- 文中明确提到：使用**NVIDIA A100 GPU（40G显存）**进行推理时间评估。
- **训练算力**：未说明。因为SOFR是免训练的，不需要任何训练，只评估其推理时的额外开销。
- **推理复杂度**：SOFR模块使整个编码时间从 `1.0000×` 增加到 `1.0240×`（即仅增加2.4%），解码时间不变。

## 5. 实验数量与充分性
- **主要实验**：在3个NVC框架（DCVC-SDD、DCVC-FM、EHVC）上各测试了PSNR和MS-SSIM下的BD-Rate（共6组结果），且报告了每个序列的详细数据（表I）。
- **消融实验**：基于DCVC-FM进行了5组对照（表II）：固定尺度s=2/4/8、动态尺度+率感知控制、完整SOFR（含可靠性检查）。此外还提供了定性可视化（图3）和复杂度分析。
- **充分性评估**：实验覆盖了多种运动类型的序列，测试了不同码率等级下的效果，消融设计合理，结果公正。但未在更大规模数据集（如UVG、MFQE等）上验证，且仅测试了3个基线。总体而言，实验设计较为充分，结论可信。

## 6. 主要结论与发现
- SOFR作为免训练即插即用模块，能一致提升多个NVC框架的率失真性能：
  - 对DCVC-FM平均节省2.84%（PSNR）和4.05%（MS-SSIM）码率。
  - 对DCVC-SDD平均节省0.97%（PSNR）和0.44%（MS-SSIM）。
  - 对EHVC平均节省0.91%（PSNR）和1.27%（MS-SSIM）。
- 在具有复杂运动的序列（如BasketDrill、BasketPass）上增益最大（最高节省13.31% MS-SSIM）。
- 简单的固定粗尺度（尤其是过大下采样）会损害性能，而动态尺度+可靠性检查是关键。
- 计算开销极低（编码时间仅增加2.4%），适合实际部署。

## 7. 优点
- **免训练**：完全无需反向传播更新任何参数，避免了昂贵的在线微调成本。
- **即插即用**：可直接集成到现有NVC框架中，无需修改网络结构。
- **率感知动态融合**：根据量化参数自适应调整下采样尺度和融合偏置，在不同码率下平衡细节与结构。
- **可靠性检查**：避免粗流质量差时损害性能，确保了融合的鲁棒性。
- **低计算开销**：仅增加2.4%的编码时间，适合实时/近实时应用。
- **实验全面**：在三个SOTA基线、10个序列、两种指标上验证，消融实验清晰验证各组件贡献。

## 8. 不足与局限
- **实验覆盖范围有限**：仅在USTC-TD数据集上测试，该数据集只有10个序列。未在更大规模或多样化的视频测试集（如UVG、MFQE、Xiph等）上验证，泛化性证据不够充分。
- **基线数量较少**：只对比了3个NVC基线，未与传统编码标准（如H.266/VVC）或更多其他NVC方法（如DCVC-DC、FVC等）比较。
- **未讨论失败场景**：论文未分析当粗、细流误差均差时（如极端遮挡、完全运动不连续）的表现，也未展示任何负面案例。
- **依赖预训练光流网络**：SOFR的性能仍受限于所选光流网络的质量。若基础光流估计已很差，双粒度融合可能无法完全恢复。
- **动态尺度选择规则较简单**：仅依赖Q-Index进行二值化划分（s=2或4），可能不够精细。是否存在更优的连续自适应方式未探索。
- **未研究训练**：作为时间较短（2026年）的工作，缺少在训练时联合优化多尺度融合的对比，可能留下进一步改进空间。

（完）
