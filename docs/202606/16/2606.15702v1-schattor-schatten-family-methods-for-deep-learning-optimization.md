---
title: "Schattor: Schatten-family methods for deep learning optimization"
title_zh: "Schattor: 用于深度学习优化的Schatten族方法"
authors: "Bohao Ma, Junyu Zhang, Chuan He"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15702v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 提出Schattor，一种基于Schatten范数的自适应一阶优化方法族
tldr: 针对现代深度学习优化中参数异构、梯度噪声和非凸景观等挑战，现有SGD和自适应优化器各有局限。本文提出Schattor，一个基于Schatten范数的自适应一阶方法族，统一了SGD和矩阵变量自适应优化器Muon。通过新颖的矩阵鞅矩界，建立了随机矩阵优化问题的无维度驻点保证，并开发了自适应平衡块间优化进展的多块扩展，同样获得无维度保证。
source: arxiv
selection_source: fresh_fetch
motivation: 现有深度学习优化器如SGD和自适应方法各有不足，需统一框架以处理异构参数和非凸性。
method: 基于Schatten范数设计自适应一阶方法族Schattor，统一SGD和Muon，并扩展多块自适应版本。
result: 利用矩阵鞅矩界证明随机矩阵优化中无维度驻点保证，多块扩展也保持该性质。
conclusion: Schattor为深度学习优化提供统一框架，具备理论保证和实用扩展性。
---

## 摘要
现代深度学习优化具有异构参数结构、有噪声梯度和高度非凸的景观，给算法设计和理论分析都带来了重大挑战。受SGD的局限性和自适应优化器成功的启发，我们提出了{\it Schattor}，一种基于Schatten范数的自适应一阶方法族。Schattor将SGD和最近提出的矩阵变量自适应优化器Muon统一在一个基于Schatten范数的框架内。我们通过一种新颖的矩阵鞅矩界，为Schattor族中的方法在随机矩阵优化问题上建立了无维度的平稳性保证。我们还开发了多块扩展，能自适应地平衡块间优化进度，并在这一更一般的设置中证明了无维度的平稳性保证。

## Abstract
Modern deep learning optimization features heterogeneous parameter structures, noisy gradients, and highly nonconvex landscapes, posing significant challenges for both algorithm design and theoretical analysis. Motivated by the limitations of SGD and the success of adaptive optimizers, we propose {\it Schattor}, a family of adaptive first-order methods based on Schatten norms. Schattor unifies SGD and the recently proposed matrix-variate adaptive optimizer Muon within a single Schatten-norm-based framework. We establish dimension-free stationarity guarantees for methods in the Schattor family for stochastic matrix optimization problems via a novel matrix martingale moment bound. We also develop multi-block extensions that adaptively balance block-wise optimization progress and prove dimension-free stationarity guarantees in this more general setting.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现代深度学习优化面临三大挑战：**参数结构的异构性**（不同层/块的维度、梯度尺度差异大）、**梯度噪声**（随机采样导致不精确）、以及**高度非凸的损失景观**。现有方法如SGD虽理论成熟但训练现代网络（如Transformer）效果不足，而自适应优化器（如Adam、Muon）虽经验成功但缺乏统一理论框架。
- **整体含义**：作者旨在提出一个**基于Schatten范数的统一自适应一阶优化方法族**，命名为**Schattor**。该框架：
  - 统一了**SGD**（对应Schatten-2范数，即Frobenius几何）和**Muon**（对应Schatten-∞范数，即谱范数）作为两个极端情形。
  - 通过连续变化的Schatten-p范数（p∈[2,∞]）提供介于两者之间的几何插值，从而更灵活地缓解梯度异构性问题。
  - 为单块和多块矩阵优化问题提供**无维度（dimension-free）的平稳性保证**，即收敛率不依赖于矩阵维度。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- 将优化器设计视为**局部搜索的范数几何选择**：使用Schatten-p范数控制更新方向，通过两个基本原语（球oracle和正则化oracle）构建更新规则。
- 引入**动量和小批量梯度估计**稳定随机梯度。
- 扩展到**多块场景**：用加权混合范数（加权ℓα-范数组合块级Schatten-pj范数）耦合不同参数块。

### 关键技术细节
1. **两个Oracle的闭式解**：
   - **球Oracle**：在Schatten-p范数球内最小化线性近似，解为 \( \Delta^b_p(M) = -M (M^T M)^{\frac{q-2}{2}} / \|M\|_{S_q}^{q-1} \)，其中q为p的对偶指数。
   - **正则化Oracle**：最小化加上二次正则项的线性近似，解为 \( \Delta^r_p(M) = \|M\|_{S_q} \cdot \Delta^b_p(M) \)。
   - 多块混合范数版本同理（Lemma 2），通过块级rescaling实现解耦。

2. **矩阵鞅矩界（Lemma 4 & 11）**：
   - 为控制随机噪声在Schatten-q范数下的累积，证明了对于鞅差序列，其和的Sq-范数期望可由迹项和Λ的范数控制，且**与维度无关**。
   - 这是证明无维度收敛定理的关键工具。

3. **算法流程**：
   - **单块算法（Algorithm 1 & 2）**：
     - 每步计算小批量梯度 \( \bar{G}_k \)，经动量平滑得到 \( M_k \)。
     - 使用球oracle（Algorithm 1）更新：\( X_{k+1} = \arg\min_{\|X-X_k\|_{S_p}\leq\eta} \langle M_k, X-X_k \rangle \)。
     - 或使用正则化oracle（Algorithm 2）更新：\( X_{k+1} = \arg\min \langle M_k, X-X_k \rangle + \frac{1}{2\eta}\|X-X_k\|_{S_p}^2 \)。
   - **多块算法（Algorithm 3 & 4）**：将单块的S_p范数替换为混合范数 \( \|\cdot\|_{w,p} \)，解耦后的更新相当于每块独立执行带自适应步长的单块oracle。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **纯理论论文，未包含任何实验**。文章聚焦于数学理论的推导和收敛保证的证明，不涉及实际数据集、基准测试或数值对比。所有结果均为理论收敛界。

## 4. 资源与算力
- **未提及任何计算资源**（如GPU型号、数量、训练时长等）。由于没有实验，自然也无相关描述。

## 5. 实验数量与充分性
- **无实验**。因此无法评价实验的充分性、客观性或公平性。但理论推导严谨，提供了完整的引理和定理证明。

## 6. 论文的主要结论与发现
- **单块情形**：对于球Oracle方法（Algorithm 1），获得了期望Sq-范数梯度范数的收敛率 \( O(\sqrt{\sigma}\Delta^{1/4}K^{-1/4} + \sqrt{\Delta}K^{-1/2} + \sigma^2 \Delta^{-1/2}K^{-1}) \)；对于正则化Oracle方法（Algorithm 2），获得了期望平方Sq-范数梯度范数的收敛率 \( O(\sigma\sqrt{\Delta}K^{-1/2} + \Delta K^{-1}) \)。
  - 这些界是**无维度**的，即仅依赖于梯度的Schatten-q范数的方差代理和Lipschitz常数，而不显式依赖于矩阵的维度m,n。
- **多块情形**：类似地，对于混合范数球Oracle和正则化Oracle，获得了相同形式的无维度收敛保证（Theorem 3 & 4）。
- 关键创新在于利用矩阵鞅矩界替代了欧几里得情形中方便的平方展开性质，从而实现了Schatten范数下的噪声控制。

## 7. 优点
- **统一性**：将SGD、signSGD、Muon等不同方法纳入同一Schatten族框架，提供连续的几何选择。
- **理论深度**：首次为Schatten范数下的自适应优化器（特别是多块混合范数）提供严格的**无维度收敛证明**，弥补了文献空白。
- **技术新颖性**：推导的矩阵鞅矩界（Lemma 4）及混合范数版本（Lemma 11）具有独立的理论价值。
- **灵活性**：球Oracle和正则化Oracle两种范式覆盖了多数线性极小化/正则化更新策略，且闭式解便于实现。

## 8. 不足与局限
- **缺乏实验验证**：作为纯理论论文，未提供任何数值实验或模型训练结果，无法验证所提方法在实际深度学习（如Transformer训练）中的优越性。
- **假设较强**：
  - 要求梯度满足**Schatten-q范数下的Lipschitz连续性**和**矩阵协方差上界**，这些条件在实际非凸深度网络中可能不严格成立。
  - 多块混合范数的权重{L_j}依赖于块级Lipschitz常数，实际中不易精确获得，可能需启发式设定。
- **应用限制**：当前分析限于**矩阵优化**问题（变量为矩阵），未考虑张量参数（如注意力头）或复杂网络结构。多块扩展的加权规则依赖于对偶指数，实现复杂度可能较高。
- **收敛率不是最优**：尽管达到经典O(K^{-1/4})平稳性度量，但常数项较大（如Theorem 1中系数6、8等），且未利用方差约减技术（如SVRG）的加速潜力。

（完）
