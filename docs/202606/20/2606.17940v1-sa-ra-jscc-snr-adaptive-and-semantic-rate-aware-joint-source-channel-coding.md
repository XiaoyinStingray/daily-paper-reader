---
title: "SA-RA-JSCC: SNR-Adaptive and Semantic-Rate-Aware Joint Source-Channel Coding"
title_zh: "SA-RA-JSCC: 自适应信噪比与感知语义速率的联合源信道编码"
authors: "Shitong Zhang, Yaping Sun, Hao Chen, Xiaoyi Li, Bo Gu, Xiaodong Xu, Nan Ma"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17940v1"
tags: ["query:compress"]
score: 9.0
evidence: 端到端联合信源信道编码，带有率失真优化的语义图像传输
tldr: 现有JSCC语义通信中，信道自适应模块逐层独立注入SNR导致全局协调不足，难以学习一致噪声鲁棒表示。为此，提出SA-RA-JSCC模型，将SNR映射为统一语义向量进行一次性全局重加权，并引入语义速率感知模块同步响应信道与速率变化。实验表明，在多个信道和数据集上，PSNR和MS-SSIM等重构指标显著优于已有模型，在广泛SNR范围内表现出更强鲁棒性。该工作为语义通信的信道自适应提供了全局协调的新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17940v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1786, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1823, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17940v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1814, \"height\": 1004, \"label\": \"Figure\"}]"
motivation: 现有信道自适应模块逐层独立注入SNR，缺乏全局协调，难以学习一致噪声鲁棒表示。
method: 将SNR映射为统一语义向量进行全局重加权，并引入语义速率感知模块同步响应信道与速率变化。
result: 在多个信道和数据集上，PSNR和MS-SSIM显著优于现有模型，在广泛SNR范围内鲁棒性更强。
conclusion: SA-RA-JSCC通过全局协调的信道自适应与语义速率感知，有效提升了语义通信鲁棒性。
---

## 摘要
在基于联合源信道编码（JSCC）的语义通信系统中，实现信道约束下稳定可靠的图像语义传输仍是一项关键挑战。在大多数信道自适应模块中，信噪比（SNR）通常以独立逐层的方式注入到信道自适应模型的每一层，这破坏了跨层的全局协调性。因此，整个模型可能无法学习到一致的抗噪表示。为解决此问题，我们提出SA-RA-JSCC，一种新颖的信道自适应JSCC模型。SA-RA-JSCC将SNR映射为特征空间中的统一语义向量，并对编码特征进行一次性全局重加权，从而实现全局一致且可学习的信道自适应。此外，为进一步增强语义信息的抗信道能力，引入了一个语义速率感知模块，使自适应策略能够同时响应信道质量波动和语义速率约束的变化，从而增强全局网络协调性和信道自适应性。在多个信道和数据集上的大量实验结果表明，SA-RA-JSCC在PSNR和MS-SSIM等重建指标上显著优于现有语义通信模型，在广泛的SNR范围内表现出更强的鲁棒性。

## Abstract
In joint source-channel coding (JSCC)-based semantic communication systems, achieving stable and reliable image semantic transmission under channel constraints remains a key challenge. In most channel adaptation modules, the signal-to-noise ratio (SNR) is often injected into each layer of a channel-adaptation model in an independent and layer-wise manner, which undermines global coordination across layers. Therefore, consistent noise-robust representations may fail to be learned throughout the model. To address this problem, we propose SA-RA-JSCC, a novel channel-adaptive JSCC model. SA-RA-JSCC maps SNR into a unified semantic vector in the feature space and then applies a one-shot global reweighting to the encoded features, thereby enabling globally consistent and learnable channel adaptation. Moreover, in order to further enhance the anti-channel capability of semantic information, a semantic-rate-aware module is introduced, enabling the adaptive policy to respond simultaneously to fluctuations in channel quality and changes in semantic-rate constraints, thereby enhancing global network coordination and channel adaptivity. Extensive experiment results across multiple channels and datasets demonstrate that SA-RA-JSCC significantly outperforms existing semantic communication models in terms of reconstruction metrics such as PSNR and MS-SSIM, exhibiting stronger robustness across a broad range of SNR regimes.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：在基于联合源信道编码（JSCC）的语义通信系统中，信道自适应（Channel Adaptation, CA）是实现稳定可靠图像语义传输的关键。然而，现有大多数信道自适应模块将信噪比（SNR）以独立、逐层的方式注入到模型的每一层（如 SwinJSCC w/ CA），这种做法破坏了跨层之间的全局协调性，导致模型难以学习到全局一致的抗噪表示，从而限制了重构性能。
- **背景**：语义通信通过深度学习实现端到端联合编码，相比传统分离式编码（如 BPG+LDPC）在低 SNR 下更具优势。但已有方法（如 SwinJSCC、SNR-EQ-JSCC）存在逐层注入导致表示冲突、或依赖额外反馈链路（如 [5]）增加开销等问题。因此，需要一种全局协调的、轻量高效的信道自适应机制。

## 2. 方法论：核心思想与关键技术细节

- **核心思想**：将 SNR 映射为特征空间中的一个**统一语义向量**，然后对编码后的特征进行一次性的**全局重加权**，从而取代逐层独立注入，实现全局一致且可学习的信道自适应。同时，引入**语义速率感知模块**，使自适应策略能同时响应信道质量变化和语义速率约束，增强全局协调性。
- **关键技术细节**：
  1. **SNR 自适应模块**：
     - 预处理：将 SNR 值 `γ_I` 通过三层全连接网络（ReLU + Sigmoid）映射为权重向量 `S_mi`（公式 (1)）。
     - 重加权：对编码特征 `˜S_I` 进行加权，并通过加性残差连接得到抗 SNR 特征 `˜S_{I,1}`（公式 (2)）：
       ```
       ˜S_{I,1} = ˜S_I + Sigmoid( MLP( S_mi · ˜S_I ) )
       ```
  2. **语义速率感知模块**：
     - 结构类似 SNR 模块，将实际语义速率 `R_I` 通过三层 FC 映射为 `S_mj`（公式 (3)）。
     - 在抗 SNR 特征 `˜S_{I,1}` 基础上再次加性加权，得到 `ˆS_{I,2}`（公式 (4)）。
     - 引入**二进制掩码机制**：对 `ˆS_{I,2}` 按通道维度排序，根据目标语义速率选择 top-C 通道设置为 1，其余为 0，得到掩码向量 `p`，最终输出 `˜S_{I,2} = ⟨ˆS_{I,2} · p⟩`（公式 (5)）。这使模型能自适应地保留最相关的语义特征。
  3. **整体架构**：基于 SwinJSCC 的两阶段 Transformer 编码器（patch embedding + Swin Transformer + patch merging），输出语义特征后进入上述信道自适应模块，对称解码器恢复图像。
  4. **损失函数**：采用 MS-SSIM 和均方误差（MSE，对应 PSNR）的组合，公式 (6)(7) 定义了 MS-SSIM 损失。

## 3. 实验设计

- **数据集**：
  - **DIV2K**：1000 张约 2K 分辨率的 RGB 图像（800 训练、100 验证、100 测试），训练时随机裁剪为 256×256 补丁。
  - **CLIC2020**：用于评估不同压缩率下的泛化性能。
- **信道场景**：加性高斯白噪声（AWGN）信道，SNR 范围从低到高（具体数值见图 2，如 -2 dB 至 20 dB）。
- **对比方法**（baselines）：
  - SNR-EQ-JSCC [3]：将 SNR 嵌入注意力块。
  - SwinJSCC w/o CA [4]：无 SNR 和语义速率模块。
  - SwinJSCC w/ CA [4]：有 SNR 和语义速率模块（逐层注入）。
  - ADJSCC [9]：基于 CNN 加通道注意力。
  - BPG+LDPC [10][11]：传统分离式编码（BPG 压缩 + 5G LDPC 信道编码）。
- **评价指标**：PSNR（像素级）、MS-SSIM（感知级）。
- **计算复杂度**：仅提及“深度为 4 层，Swin Transformer blocks 深度为 [2,2,6,2]，通道数 [128,192,256,320]，窗口大小 8，学习率 0.0001，Adam 优化器”，未系统分析参数量或 FLOPs。

## 4. 资源与算力

- 文中明确说明：“All experiments were trained and tested on **NVIDIA GeForce RTX 5060 Ti**”。
- **未提及** GPU 数量、训练时长（epoch 数或 hours）、以及是否使用多卡并行。因此算力描述不够详细。

## 5. 实验数量与充分性

- **实验数量**：
  - 在 DIV2K 上比较了 2 种压缩率（r=1/32 和 1/8）下 PSNR/MS-SSIM 随 SNR 的变化曲线（图 2 a-d）。
  - 在 CLIC2020 上比较了不同信道带宽比（cbr）下的结果（图 3）。
  - 提供了 4 组可视化对比（图 4，包含不同模型在 10 dB SNR 下的重建图像及 PSNR/MS-SSIM 数值）。
  - 未做严格的消融实验（如去除语义速率模块或仅 SNR 模块的单独效果），但通过对比 SwinJSCC w/o CA、SwinJSCC w/ CA 与 SA-RA-JSCC 间接体现了模块贡献。
- **充分性分析**：
  - 覆盖了多个压缩率、多个 SNR 点、两个数据集（DIV2K 和 CLIC2020），并与 5 种代表性 baseline 对比，较为全面。
  - 评价指标同时使用像素级（PSNR）和感知级（MS-SSIM），较为客观。
  - 但是**仅测试了 AWGN 信道**，未验证瑞利衰落或莱斯衰落等更复杂信道下的表现（论文系统模型虽提到了通用衰落信道，但实验部分明确使用 AWGN）。
  - 对比方法均为开源或公开结果，公平性较好；BPG+LDPC 在不同编码率下进行了优化。

## 6. 主要结论与发现

- SA-RA-JSCC 在所有实验设置下（不同 SNR、压缩率、数据集）均取得了最优的 PSNR 和 MS-SSIM 值。
- 在低压缩率（r=1/32）和低 SNR 下，优势尤为显著，表明全局协调的信道自适应和语义速率感知共同提升了抗干扰与特征保留能力。
- 与传统分离式方案 BPG+LDPC 相比，SA-RA-JSCC 在低中 SNR 区域避免了“悬崖式”性能下降，展现出更平滑的退化曲线和更稳定的语义质量。
- 在较高压缩率（r=1/8）和高 SNR 下，SA-RA-JSCC 仍然领先，说明其能更有效地利用信道资源。
- 可视化结果证实了重建图像在纹理、边缘和局部细节上优于所有 baseline，尤其是在复杂自然场景下。

## 7. 优点

- **方法创新**：提出“一次性全局重加权”替代“逐层注入”，从根本上避免了跨层表示冲突，实现了全局一致的信道自适应。
- **联合感知**：同时考虑 SNR 和语义速率两种约束，能够协同优化，提升整体鲁棒性。
- **结构简洁**：未改变骨干网络（SwinJSCC），仅在外围添加轻量级模块（FC 层和掩码），易于集成。
- **实验充分**：在多个指标、多种压缩率、两个数据集上与多种方法对比，结果置信度高。
- **性能领先**：在几乎所有 SNR 点均超越现有先进 JSCC 方法，且强于传统数字编码方案。

## 8. 不足与局限

- **信道类型单一**：实验仅验证了 AWGN 信道，未涵盖瑞利/莱斯衰落、噪声相关信道等实际无线场景，泛化性有待验证。
- **算力与复杂度描述不足**：未提供训练时间、参数量、推理延迟等关键指标，难以评估实际部署成本。
- **缺乏消融实验**：虽然与 SwinJSCC w/o CA 和 w/ CA 比较可部分反映模块效果，但未单独剥离 SNR 模块或语义速率模块进行定量分析。
- **数据集规模有限**：DIV2K 和 CLIC2020 均为图像压缩/超分领域常用数据，但未在更大规模（如 ImageNet）或多样化场景（如医学、遥感）上测试。
- **未探讨端到端训练稳定性**：未提及多个目标（MS-SSIM 和 MSE）联合训练时的收敛特性和超参数敏感性。
- **未来方向**：论文仅提出扩展到视频/语音的设想，未提供具体方案，应用边界尚不明确。

（完）
