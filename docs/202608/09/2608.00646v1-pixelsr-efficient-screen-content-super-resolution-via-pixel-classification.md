---
title: "PixelSR: Efficient Screen Content Super-Resolution via Pixel Classification"
title_zh: PixelSR：基于像素分类的高效屏幕内容超分辨率
authors: "Zhiheng Li, Lei Chen, Jie Zhou, Jiwen Lu"
date: 2026-08-01
pdf: "https://arxiv.org/pdf/2608.00646v1"
tags: ["query:neural-arch"]
score: 8.0
evidence: 通过像素分类和内容注意力实现高效屏幕内容超分辨率，符合高效神经网络架构设计需求
tldr: 实际上屏幕内容超分常忽视图像中大量重复结构。PixelSR在训练时用像素分箱构建内容注意力，获得内容相关非局部感受野；测试时按自重复性将像素分为独特、重复和背景三类，其中独特像素的网络结果存入查找表，重复像素直接查表，背景像素用最近邻生成。最终该方法在达到SOTA性能的同时大幅降低推理时间，为屏幕内容超分提供了高效新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.00646v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 818, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1876, \"height\": 1007, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1773, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1856, \"height\": 504, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1872, \"height\": 291, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1860, \"height\": 214, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1839, \"height\": 915, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 889, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1877, \"height\": 1084, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.00646v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1863, \"height\": 1300, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.00646v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1487, \"height\": 968, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00646v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1484, \"height\": 831, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00646v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 69, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00646v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1725, \"height\": 1176, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00646v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1499, \"height\": 367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.00646v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1524, \"height\": 277, \"label\": \"Table\"}]"
motivation: 屏幕内容图像含大量重复结构，现有超分方法未充分利用这些特性，导致性能与速度受限。
method: 训练时用像素分箱聚合组内特征形成内容注意力；测试时将像素分为独特、重复和背景三类，独特像素网络处理并缓存，重复像素查表，背景像素最近邻生成。
result: 在屏幕内容超分任务上达到最优性能，且推理时间大幅缩短，验证了像素分类加速策略的有效性。
conclusion: PixelSR利用屏幕内容自重复性，以可忽略的性能代价换取显著加速，为高效超分提供新思路。
---

## 摘要
屏幕内容图像通常由文本和图形组成。与自然图像相比，这些人造图像包含大量清晰但重复的结构。然而，现有的屏幕内容超分辨率工作在利用屏幕内容的特殊特性方面存在不足，这使得模型性能和速度的提升仍有很大空间。在本文中，我们提出PixelSR，一种简单而有效的方法，在提高超分辨率性能的同时实现更快的推理速度。为了提高模型性能，我们在训练阶段通过像素分箱对像素进行分类以计算内容注意力。具体而言，在将像素分入内容相关的组后，内容注意力从每组内的像素特征中聚合，为每个像素引入内容相关的非局部感受野。在测试阶段，我们利用屏幕内容中的自重复性和冗余性来加速推理，且不损失模型性能。我们将目标高分辨率像素分为三种类型：唯一像素、重复像素和背景像素。我们对唯一像素执行常规网络处理，并在即时查找表中缓存其预测结果。对于已在唯一像素中出现过的重复像素，我们直接检索查找表中的预测结果，无需网络处理。对于背景像素，我们使用最近邻算法生成高分辨率像素。即时查找表会被清空，并对下一张测试图像重复上述过程。实验表明，我们的PixelSR在屏幕内容超分辨率中实现了最先进的性能，且推理时间更短。

## Abstract
Screen content images are generally composed of texts and graphics. Compared to natural images, these man-made images contain a large quantity of sharp but repetitive structures. However, existing works in screen content super-resolution underutilize the special characteristics of screen content, leaving a large room to improve model performance and speed up. In this paper, we propose PixelSR, a simple yet effective method to improve super-resolution performance but with faster inference speed. To improve model performance, we classify pixels via pixel binning to compute content attention in the training phase. Specifically, after binning pixels into content-dependent groups, content attention is aggregated from pixel features within each group to introduce a content-dependent and non-local receptive field for every pixel. In the testing phase, we utilize the properties of self-repetitiveness and redundancy in screen content to speed up inference without the loss of model performance. We divide targeted high-resolution pixels into three types, which are unique pixels, repeated pixels, and background pixels for each test image. We conduct conventional network processing on unique pixels and cache their predictions in the on-the-fly lookup table. For repeated pixels which have appeared in unique pixels, we directly retrieve prediction results from the lookup table without network processing. For background pixels, we use the nearest neighbor algorithm to generate high-resolution pixels. The on-the-fly lookup table is cleaned and repeats the procedure above for the next test image. Experiments show our PixelSR achieves state-of-the-art performance with shorter inference time in screen content super-resolution.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：屏幕内容图像（如文本、表格、图表）与自然图像存在显著差异，包含大量“清晰但重复”的结构。现有屏幕内容超分辨率（Screen Content SR）方法（如 LIIF、ITSRN、LTE、BTC）没有充分利用上述特性，导致模型性能仍有提升空间，且推理速度较慢。
- **核心问题**：如何针对屏幕内容的“非自然锐利边缘”“高冗余背景”“高自重复性”三大特性，同时提升超分精度与推理效率。
- **整体含义**：论文提出 PixelSR，在训练阶段通过“像素分箱（pixel binning）”构建内容相关的非局部注意力，在推理阶段通过“像素分类 + 即时查找表”加速重复像素和背景像素的预测，从而在保持甚至提升 PSNR 的同时显著缩短推理时间。

## 2. 论文提出的方法论

### 2.1 训练阶段：像素分箱与内容注意力

- 采用一个轻量级 **内容提取器（content extractor）**，对编码器特征 \(F_{LR}\) 做一次卷积 + ReLU + 归一化，得到每个像素的内容得分图 \(S\)：
  \[
  S = Norm(ReLU(Conv(F_{LR})))
  \]
- 将得分图归一化到 [0, 1]，并设计 **软分箱（soft binning）**：
  - 在 [0, 1] 内均匀设置 \(N\) 个箱中心 \(c_i\)；
  - 对每个得分，通过指数距离 softmax 计算其属于每个箱的软权重，从而保证梯度可传播，避免硬分箱导致的反向传播断裂。
- 将多个箱合并成两个内容组：高中心值组（对应屏幕内容边缘/文本）和低中心值组（对应平坦区域）。
- 对每组像素特征做 **空间平均池化**，得到组级平均特征 \(f_1, f_2\)，再与对应掩码相乘并相加，得到内容注意力图 \(F_{CA}\)，最后与原特征拼接后送入解码器。
- 这样每个像素都获得了“内容相关、非局部、全图范围”的感受野，而不需要昂贵的密集像素相关性计算。

### 2.2 推理阶段：像素分类与即时查找表

- 将目标 HR 像素划分为三类：
  - **背景像素**：最近邻 3×3 LR 块内 9 个像素 RGB 值完全相同；
  - **唯一像素**：3×3 LR 块 RGB 值与相对距离的组合首次出现；
  - **重复像素**：与唯一像素的输入组合相同，但位置不同。
- 推理流程：
  - 背景像素直接用 **最近邻算法** 生成 HR 值，跳过网络；
  - 唯一像素用网络正常处理，并将“输入（3×3 LR 块 RGB + 相对距离）→ 输出 RGB”写入 **即时查找表（on-the-fly lookup table）**；
  - 重复像素直接从查找表检索预测结果，不再经过网络；
  - 每处理完一张测试图像，清空查找表，下张图像重新建立。
- 该方法可即插即用地用于 LIIF、ITSRN、LTE、BTC 等模型，提升推理速度且几乎不损失 PSNR。

### 2.3 网络结构细节

- 内容提取器：输入通道 64、输出通道 1、卷积核 3×3；
- 软分箱总箱数设为 10，其中第 1 个箱为第一组，其余 9 个箱合并为第二组；
- 解码器沿用 BTC 的解码器结构；
- 整体参数量约 1.7M，与对比方法（1.6M–1.9M）相当。

## 3. 实验设计

- **数据集**：
  - SCI1K：训练集 800 张，测试集 200 张；
  - SCID：测试集 40 张；
  - SIQAD：测试集 22 张。
- **Benchmark 设置**：
  - 训练时用双三次下采样，缩放因子范围 1∼4；
  - 输入 48×48 LR patch，对应 HR patch 随机采样 2304 个坐标-RGB 对进行监督；
  - 评估缩放因子包括训练内尺度（×2、×3、×4）以及训练外尺度（×5–×10）；
  - 评价指标以 PSNR 为主。
- **对比方法**：LIIF、ITSRN、LTE、BTC，且分别在两种骨干网络下比较：
  - EDSR-baseline；
  - SwinIR-light。
- **主要结果**：
  - 在 EDSR 骨干下，SCI1K 的 ×3 尺度达到 33.52 dB，比第二名 BTC 高 0.72 dB；
  - 在 SwinIR-light 骨干下，SCI1K 的 ×4 尺度达 30.31 dB，比 BTC 高 0.61 dB；
  - 在 SCID、SIQAD 上基本都在所有尺度上排名第一；
  - 在推理时间方面，PixelSR 在 SCI1K 测试集上的总推理时间在所有尺度下均短于 LIIF、ITSRN、LTE、BTC。
- **其他实验**：
  - 定性可视化比较；
  - 场景文字识别（STR）置信度评估；
  - 即时查找表对 LIIF/ITSRN/LTE/BTC/PixelSR 的加速效果与 PSNR 影响分析；
  - 消融实验：不同箱数（0/2/5/8/10）、内容提取器不同卷积层数（1/2/3）。

## 4. 资源与算力

- 文中明确说明：实验在 **一张 GeForce RTX 3090 GPU** 上进行；
- 训练设置：
  - 优化器：Adam；
  - 初始学习率：0.0001，每 200 epoch 衰减一半；
  - 训练轮数：1000 epoch；
  - Batch size：16；
  - 测试使用最后一个 epoch 的模型。
- 未明确说明实验总时长、单次训练耗时或总 GPU 小时数，也没有给出多卡并行或不同硬件上的完整成本对比。

## 5. 实验数量与充分性

- **实验数量较充分**：
  - 3 个测试数据集（SCI1K、SCID、SIQAD）；
  - 2 种骨干网络（EDSR-baseline、SwinIR-light）；
  - 9 个缩放因子（×2–×10）；
  - 5 种方法对比；
  - 多组消融实验（箱数、层数）与可视化分析；
  - 还包含对即时查找表的单独分析，以及将其应用到其他基线模型的加速实验。
- **公平性**：
  - 所有方法参数量相近（约 1.6M–1.9M），排除了模型容量差异导致的性能偏差；
  - 使用相同的训练集、骨干网络、数据增强和优化策略进行对比；
  - 训练外尺度的评估能反映泛化能力。
- **可以更充分之处**：
  - 仅使用 PSNR 作为定量指标，没有报告 SSIM 或感知指标；
  - 没有在自然图像数据集上做对照实验来验证“屏幕内容专用”设计的边界；
  - 没有报告多次运行的标准差，无法判断统计显著性。

## 6. 论文的主要结论与发现

- 屏幕内容图像具有“锐利边缘”“高冗余背景”“高自重复性”三种可被利用的特性；
- 在训练阶段，通过软分箱得到的 **内容注意力** 可以为每个像素提供内容相关且全图范围的感受野，显著提升 PSNR，尤其是文本和图形边缘的重建质量；
- 在推理阶段，**即时查找表 + 最近邻** 能够大幅减少需要网络计算的像素数量，从而加速推理且不损失 PSNR；
- PixelSR 在各种缩放因子和多个屏幕内容基准上均取得 SOTA 性能，同时推理时间最短；
- 即插即用的即时查找表也能为现有方法带来速度提升，说明该策略具有通用性。

## 7. 优点

- **针对性强**：充分利用了屏幕内容与自然图像的分布差异，而非简单套用自然图像 SR 方法；
- **训练设计巧妙**：软分箱在保持可微分的同时实现了内容分组，避免了硬分箱的梯度断裂；
- **推理设计高效**：将像素分类为唯一/重复/背景三类，用查找表和最近邻替代昂贵网络计算，兼顾精度和速度；
- **即插即用性**：即时查找表不依赖特定网络结构，可以适配多种现有 SR 方法；
- **实验较全面**：包含多数据集、多尺度、多骨干、多基线的对比，以及详尽的消融和可视化，结论可信度较高。

## 8. 不足与局限

- **适用范围受限**：即时查找表依赖屏幕内容的“自重复性”和“平坦背景”，在自然图像上增益有限，论文也承认该局限；
- **背景像素判定条件较强**：仅当 3×3 LR 块内 9 个像素值完全相等才判定为背景，对带渐变、阴影或抗锯齿边缘的背景不适用；
- **评价指标单一**：仅报告 PSNR，未评估 SSIM、LPIPS 或主观感知质量；
- **未报告统计稳定性**：没有多次实验的标准差，无法判断性能提升是否显著；
- **训练成本信息不完整**：未给出总训练时长，硬件资源信息较为简略；
- **理论性质分析较少**：对软分箱中温度系数、箱中心分布等超参的敏感性讨论不够深入。

（完）
