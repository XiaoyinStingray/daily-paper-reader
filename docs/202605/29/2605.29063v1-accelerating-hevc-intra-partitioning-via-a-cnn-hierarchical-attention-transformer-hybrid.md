---
title: Accelerating HEVC Intra Partitioning via a CNN-Hierarchical Attention Transformer Hybrid
title_zh: 通过CNN-分层注意力Transformer混合架构加速HEVC帧内分区
authors: "Krishna Kumar Sharma, Somdyuti Paul"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.29063v1"
tags: ["query:compress"]
score: 8.0
evidence: 使用CNN-Transformer混合加速HEVC帧内分区，涉及率失真优化
tldr: "HEVC帧内编码的递归四叉树分区导致高计算开销，现有深度学习方法中CNN局部视野有限而Transformer全局性好但CPU延迟高。本文提出HFViT混合架构，融合重参数化深度可分卷积和分层注意力Transformer，通过载体令牌实现高效全局信息传播，并采用后训练融合降低延迟。在JCT-VC测试序列上，相比ETH-CNN基线，HFViT在A/B/E类序列上平均VMAF BD-rate惩罚降低2.4/2.6/7.9个百分点，CPU延迟仅增加8%以内，GPU提升40%，具备实时集成可行性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29063v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 217, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 2352, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 1790, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 493, \"height\": 331, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 834, \"height\": 613, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1862, \"height\": 1306, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.29063v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1848, \"height\": 2039, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.29063v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 482, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 913, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 932, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 930, \"height\": 522, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1548, \"height\": 664, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1886, \"height\": 674, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.29063v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 923, \"height\": 152, \"label\": \"Table\"}]"
motivation: HEVC帧内分区递归优化计算量大，现有深度学习方法无法兼顾局部高效与全局上下文，需设计低延迟混合架构。
method: 提出HFViT，融合CNN骨干与分层注意力Transformer，采用载体令牌实现子四次复杂度的全局信息传播，并后融合归一化层。
result: "在JCT-VC序列上，HFViT相比ETH-CNN基线，VMAF BD-rate惩罚降低2.4-7.9个百分点，CPU延迟仅增8%，GPU提升40%。"
conclusion: HFViT以极小延迟代价显著提升编码加速性能，为实时HEVC编码器集成提供实用方案。
---

## 摘要
高效视频编码（HEVC）中的递归四叉树分区带来了显著的计算开销，其中用于CTU分区预测的穷举率失真优化消耗了编码时间的主要部分。尽管通过深度学习进行分区预测已成为一种可行的编码加速器，但架构上的二分法仍未得到充分解决：CNN计算效率高，但由于其局部有效感受野而在空间上短视，无法捕捉长期语义关系和重复纹理；相反，基于Transformer的架构更擅长捕捉全局上下文，但会带来难以承受的CPU延迟，这是阻碍其部署的关键缺陷，因为部署主要受CPU限制。本文介绍了混合快速视觉Transformer（HFViT），这是一种旨在加速HEVC帧内模式分区预测的混合架构。HFViT将重参数化的深度可分离卷积骨干网络与分层注意力Transformer（HAT）机制融合，利用载体令牌方案以次二次复杂度实现高效的全局信息传播。训练后结构融合将批归一化折叠到前一层以进一步减少延迟。全面评估揭示了HFViT在加速不同分辨率下HEVC帧内编码方面的有效性。在标准JCT-VC测试序列上，与竞争的ETH-CNN基线相比，HFViT在A、B和E类上分别将平均VMAF BD-rate惩罚降低了2.4、2.6和7.9个百分点，同时保持CPU推理延迟在CNN基线的8%以内，并且在GPU上超过CNN基线40%，为实时编码器集成建立了实际可行性。

## Abstract
The recursive quad-tree partitioning in High Efficiency Video Coding (HEVC) incurs considerable computational overhead, with exhaustive rate-distortion optimization for CTU partition prediction consuming the dominant share of encoding time. Although partition prediction through deep learning has emerged as a viable encoding accelerator, an architectural dichotomy remains largely unaddressed: CNNs are computationally efficient but spatially myopic due to their localized effective receptive fields, failing to capture long range semantic relationships and repetitive textures; conversely, transformer based architectures are better at capturing global context but incur prohibitive CPU latency, a critical liability that impedes deployment which is predominantly CPU-bound. This paper introduces Hybrid Fast Vision Transformer (HFViT), a hybrid architecture designed to accelerate HEVC intra-mode partition prediction. HFViT fuses a reparameterized depthwise-separable convolutional backbone with a Hierarchical Attention Transformer (HAT) mechanism, leveraging a carrier token scheme to enable efficient global information propagation at sub-quadratic complexity. Post-training structural fusion collapses batch normalization into preceding layers to further reduce latency. Comprehensive evaluation reveals the efficacy of HFViT in accelerating HEVC intra-encoding across resolutions. On standard JCT-VC test sequences, HFViT reduces the average VMAF BD-rate penalty by 2.4, 2.6, and 7.9 percentage points on Classes A, B and E, respectively, as compared to the competing ETH-CNN baseline while maintaining CPU inference latency within 8% of the CNN baseline and surpassing it on GPU by 40%, establishing practical viability for real-time encoder integration.

---

## 论文详细总结（自动生成）

好的，以下是对给定论文的详细中文总结。

## 论文总结：通过CNN-分层注意力Transformer混合架构加速HEVC帧内分区

### 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：HEVC帧内编码中，递归四叉树分区通过穷举率失真优化（RDO）寻找最优分割，其计算开销占编码总时间的60-80%，严重限制了编码效率。
- **现有方法局限**：
    - **传统启发式方法**：基于手工特征的早期终止，速度提升有限（30-45%）且带来BD-rate损失。
    - **深度学习CNN方法**：自动学习层次特征，编码时间可降低50%以上。但CNN本质上是局部模型，其有效感受野远小于理论值，无法捕捉长程空间依赖和重复纹理，导致分区预测精度受限。
    - **Transformer方法**：通过自注意力机制能建模全局上下文，精度更高。但其CPU推理延迟极高（因为不规则内存访问、缺乏优化内核等），而主流视频编码器部署环境多为CPU受限，因此实际应用价值低。
- **研究意义**：本文旨在设计一种混合架构，既能像CNN一样保持低CPU延迟，又能像Transformer一样具备全局上下文建模能力，从而在实时编码流水线中实现更优的率失真性能，弥合精度与效率之间的鸿沟。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：提出**HFViT（混合快速视觉Transformer）**，将重参数化的深度可分离卷积骨干与分层注意力Transformer（HAT）机制相结合，利用载体令牌进行次二次复杂度的全局信息传播，并通过后训练批归一化融合进一步降低推理延迟。
- **整体流程**：输入64×64亮度CTU块 → 卷积嵌入茎（四级下采样） → 两个HAT块（含载体令牌交互） → QP条件分类头 → 输出21维分割概率。
- **关键技术细节**：
    1.  **深度可分离卷积骨干**：使用四阶段渐进下采样，通道数保守增长（8→16→24→32），避免参数冗余和过拟合，适用于低分辨率单通道输入。
    2.  **分层注意力Transformer（HAT）**：
        - **局部窗口自注意力**：将4×4特征图划分为2×2窗口，每个窗口内对空间令牌和对应的载体令牌进行多头自注意力（相对位置偏置）。
        - **载体令牌交互层**：所有窗口的载体令牌通过一个紧凑的自注意力（4×4矩阵）进行全局信息交换，然后将更新后的载体令牌传回各窗口，实现次二次复杂度的全局上下文传播。
    3.  **QP条件预测头**：将归一化的量化参数（QP）与全局平均池化后的特征拼接，输入MLP分类头，使单个模型能适应不同编码质量（QP 22/27/32/37），实现率失真感知预测。
    4.  **后训练BN融合**：在推理阶段将训练时的卷积-BN-GELU结构重参数化为单一的卷积激活单元，以及将线性-BN融合，消除BN的前向开销，显著降低CPU延迟。
    5.  **损失函数**：分层二元交叉熵损失，对各深度级别的有效子块（根据父块是否分割设定掩码）分别计算损失后等权求和。

### 3. 实验设计

- **数据集**：
    - **训练/验证/测试**：CPH-Intra数据集，包含由HM 16.5编码器在QP {22,27,32,37}下生成的CTU分割标签。针对四个分辨率（4928×3264, 2880×1920, 1536×1024, 768×512）分别训练模型（各分辨率模型参数数随样本量调整）。
    - **测试序列**：10个自建序列（覆盖不同空间/时间复杂度）用于分辨率级评估；以及**JCT-VC标准测试集**（Class A/B/E）用于标准化对比。
- **基准与对比方法**：
    - 参考编码器：HM 16.5（未修改，用于生成RD曲线锚点）。
    - 对比方法：**ETH-CNN**（CNN基线，在CPH-Intra上达最优）、**H-FCN**（轻量级分层全卷积架构）。
- **评价指标**：
    - **分区准确率**：L1(64×64)、L2(32×32)、L3(16×16)各层级及整体准确率。
    - **率失真性能**：BD-rate（PSNR, VMAF, MS-SSIM），相对于HM锚点。
    - **编码加速比**：相对于HM 16.5的编码时间节省百分比。
    - **推理延迟**：CPU/GPU上每CTU的推理时间（ms/CTU）。

### 4. 资源与算力

- 文中明确给出的训练配置：
    - **GPU**：NVIDIA A40（46 GB VRAM）—— 1块（但未说明数量，推断为单卡）。
    - **CPU**：2×Intel Xeon Silver 4509Y（32核/64线程）。
    - **训练超参数**：Optimizer AdamW, LR 1e-4, Weight Decay 0.01, Batch Size 64, Max Epochs 10,000, 学习率Scheduler Cosine Annealing。
    - **训练时间**：**未明确说明**实际训练时长或总GPU小时数。
- **推理系统**：AMD Ryzen 5 7235HS CPU, 12 GB DDR4 RAM（用于延迟基准测试）。

### 5. 实验数量与充分性

- **实验数量**：覆盖多维度。
    - **分辨率级评估**：4个分辨率，每个分辨率10个测试序列，报告均值与95%置信区间。
    - **JCT-VC标准评估**：3个类别（A/B/E）共10个序列，报告每个序列的BD-rate及加速比。
    - **消融实验**：1组（在1536×1024分辨率上），对比有无BN融合、有无深度可分离卷积对BD-rate和加速比的影响。
    - **分区可视化**：2个示例帧的分区图定性对比。
- **充分性与公平性**：
    - **充分性**：实验覆盖了多个分辨率、多个序列、多个QP点，以及多种质量指标（PSNR, VMAF, MS-SSIM），并包含消融研究，验证了各组件的贡献。测试序列在空间/时间复杂度上分布广泛，避免了内容偏倚。
    - **公平性**：对比模型ETH-CNN和H-FCN采用相同数据集、相同编码器、相同评价流程。在推理延迟对比中，所有Transformer架构被调整为相似参数量，以公平比较速度。消融实验严格控制变量。

### 6. 主要结论与发现

- **率失真性能**：在JCT-VC测试序列上，HFViT平均VMAF BD-rate分别为3.12%（A类）、3.36%（B类）、6.03%（E类），相比ETH-CNN基线**降低了2.4、2.6、7.9个百分点**。在所有分辨率上，HFViT的VMAF BD-rate均优于CNN基线。
- **推理速度**：
    - **CPU**：HFViT的CPU延迟为0.27 ms/CTU，仅比ETH-CNN（0.25 ms/CTU）**高出8%**，而其他高效Transformer（如EfficientViT 0.50 ms）则慢2倍以上。
    - **GPU**：HFViT的GPU延迟为0.047 ms/CTU，比ETH-CNN（0.078 ms/CTU）**快40%**，兼顾了CPU实用性。
- **编码加速比**：在最高分辨率（4928×3264）上HFViT获得最佳加速比（79.41%），且在高分辨率下比CNN基线更有优势。
- **消融结论**：BN融合是严格无损的运行时优化，带来8.71%的加速收益；深度可分离卷积以微小精度损失换取显著速度提升。

### 7. 优点

- **架构创新**：巧妙融合了CNN的低延迟特性和Transformer的全局建模能力，设计了针对HEVC低分辨率单通道输入定制的轻量化分层注意力机制。
- **实用导向**：高度重视CPU延迟瓶颈，通过载体令牌、深度可分离卷积、后训练BN融合等多重优化，使混合架构在CPU上接近纯CNN速度，而GPU上更快，真正具备实时编码集成可行性。
- **全面评估**：在标准测试集和多个分辨率上进行严格对比，报告了置信区间，且引入VMAF感知指标，评估视角全面。
- **损失函数设计**：采用层次化掩码交叉熵，符合HEVC四叉树的分层条件依赖结构，训练更合理。

### 8. 不足与局限

- **低分辨率性能未全面领先**：在768×512分辨率下，PSNR和MS-SSIM的BD-rate略逊于H-FCN；说明小分辨率下超轻量模型可能更具优势。
- **对特定内容类别的适应不足**：在JCT-VC Class E（含较多对话/头部特写序列）上，所有模型BD-rate显著高于其他类，表明训练数据中该类内容不足，存在内容偏差风险。
- **训练时长未报告**：未提供实际训练所需的GPU小时数，不利于复现和算力预算评估。
- **泛化性验证有限**：实验仅限于HEVC帧内模式；虽然作者提及可扩展至帧间、AV1/VVC等，但未有实际验证。此外，未在更低算力平台（如手机、嵌入式设备）上测试。
- **一定程度的过拟合风险**：在不同分辨率上调整模型参数数（尤其分类头维度）以避免过拟合，表明原始大模型在小数据集上可能过拟合，通用性有待进一步验证。

（完）
