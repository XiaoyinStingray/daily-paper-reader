---
title: "SIGMA: Bridging Structural and Distributional Gaps for Vision Foundation Model Adaptation"
title_zh: SIGMA：弥合视觉基础模型适应中的结构和分布差距
authors: "Lingyu Xiong, Jinjin Shi, Xuran Xu, Cong Luo, Runyu Shi, Ying Huang"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27893v1"
tags: ["query:neural-arch"]
score: 9.0
evidence: SIGMA适配器解决视觉基础模型适应中的结构分布差异，属于架构改进
tldr: "视觉基础模型全微调成本高昂，参数高效微调面临结构性（多尺度信息缺失）和分布性（特征不对齐）两大差距。提出SIGMA方法，通过尺度自适应融合增强多粒度视觉信息提取，并引入语义调制进行全局特征对齐。仅需1.72%可训练参数，在多个密集预测任务和骨干网络上达到最优性能，为VFM高效适应提供统一方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27893v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27893v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1181, \"height\": 1037, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.27893v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 997, \"height\": 1467, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1110, \"height\": 576, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 584, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1295, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.27893v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 647, \"height\": 224, \"label\": \"Table\"}]"
motivation: 现有PEFT在VFM密集预测中存在结构和分布双重差距，需桥接二者以实现高效适应。
method: 提出SIGMA，含尺度自适应融合（增强多粒度信息）和语义调制（全局特征对齐）两模块。
result: "仅用1.72%可训练参数，在多种密集任务和VFM骨干上取得一致领先性能。"
conclusion: SIGMA有效桥接结构性和分布性差距，为VFM适应提供轻量高效方案。
---

## 摘要
视觉基础模型（VFM）展示了令人印象深刻的表现能力。然而，通过全微调将其适应下游任务会带来高昂的计算和存储开销。参数高效微调（PEFT）作为一种有吸引力的替代方案出现，旨在以最小的训练成本达到与全微调相当的性能。尽管如此，由于结构和分布差距的存在，将PEFT应用于VFM进行密集预测任务仍然具有挑战性。为了弥合这些差距，我们提出了尺度集成全局调制适配器（SIGMA），一种新颖的轻量级PEFT方法，它包含两个模块：尺度自适应融合和语义调制。具体来说，尺度自适应融合模块通过增强多粒度视觉信息的提取来弥合结构差距。此外，SIGMA对融合特征引入语义调制以进行全局特征对齐，从而进一步消除分布差距。这种设计促进了统一的空间和分布适应，相对于VFM主干网络仅需要1.72%的可训练参数。在各种下游密集任务和多个VFM主干网络上的全面实验表明，SIGMA在性能上一致优于最先进的PEFT方法。

## Abstract
Vision Foundation Models (VFMs) have demonstrated impressive representational capabilities. However, adapting them to downstream tasks via full fine-tuning incurs prohibitive computational and storage overhead. Parameter-Efficient Fine-Tuning (PEFT) has emerged as a compelling alternative, aiming to achieve performance parity with full fine-tuning at minimal training costs. Nonetheless, applying PEFT to VFMs for dense prediction tasks remains challenging due to the structural and distributional gaps. To bridge these gaps, we propose \textbf{S}cale-\textbf{I}ntegrated \textbf{G}lobal \textbf{M}odulation \textbf{A}dapter (\textbf{SIGMA}), a novel lightweight PEFT method, which consists of two modules: scale-adaptive fusion and semantic modulation. Specifically, the scale-adaptive fusion module is utilized to bridge structural gaps by enhancing the extraction of multi-granularity visual information. Furthermore, SIGMA introduces semantic modulation on the fusion features to perform global feature alignment to further eliminate the distribution gap. This design facilitates unified spatial and distributional adaptation, requiring only 1.72\% trainable parameters relative to the VFM backbone. Comprehensive experiments across various downstream dense tasks and multiple VFM backbones demonstrate that SIGMA achieves consistent and superior performance over state-of-the-art PEFT methods.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究背景**：视觉基础模型（VFM）如 DINOv2、SigLIP2、SAM 在预训练后展现出强大的表征能力，但将其直接应用于下游任务（如目标检测、语义分割、深度估计）时，**全参数微调**的计算和存储成本过高，不切实际。
- **核心问题**：参数高效微调（PEFT）方法虽旨在解决此问题，但在适配 VFM 用于**密集预测任务**时存在两大关键挑战：
    1.  **结构性差距**：现有 PEFT 方法（如 Adapter、LoRA）多源于 NLP，采用线性变换处理 1D 序列，缺乏捕获 2D 空间局部性和多尺度上下文信息的结构归纳偏置，难以满足密集预测对精细空间理解的需求。
    2.  **分布性差距**：VFM 在通用大规模数据集上预训练的特征统计量（均值、方差）与特定下游任务的数据分布存在偏差，而现有适配器缺乏显式机制来校正这种统计不匹配。
- **整体含义**：论文旨在设计一种新的 PEFT 方法，能够**同时弥合结构性差距和分布性差距**，从而实现 VFM 在密集预测任务上的高效、高性能适应。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出 SIGMA（Scale-Integrated Global Modulation Adapter），一个包含**尺度自适应融合**和**语义调制**两大模块的轻量级适配器，协同解决结构性（空间）和分布性（特征）两种差距。
- **关键技术细节**：
    1.  **尺度自适应融合模块**：
        -   **目标**：桥接结构性差距，增强多粒度视觉信息提取。
        -   **过程**：
            -   输入特征经过 LayerNorm 和下投影后，送入三个并行的**深度可分离卷积**（DW Conv），核大小分别为 3×3、5×5、7×7，以捕获不同感受野下的空间特征。
            -   通过一个 1×1 **逐点卷积**（PW Conv）聚合多尺度特征。
            -   引入残差连接以保存特征恒等性并改善梯度流。
    2.  **语义调制模块**：
        -   **目标**：桥接分布性差距，进行全局特征对齐。
        -   **过程**：
            -   在下投影层之后，通过一个线性投影 P 生成四组缩放（γ）和偏移（β）参数。
            -   这些参数分别用于调制上述三个 DW Conv 和一个 PW Conv 的输出特征，显式地调整特征统计量，使其与下游数据分布对齐。
    3.  **总体架构**：
        -   将 SIGMA 模块无缝插入 Transformer 块的**多头注意力（MHA）** 和**前馈网络（FFN）** 之后。
        -   整个过程可形式化为：输入 → LN → 下投影 → 语义调制（生成参数）→ 多尺度 DW Conv（在调制下）→ 逐点 PW Conv（在调制下）→ GeLU → 上投影 → 残差连接 → 输出。
    4.  **参数效率**：通过设置极小的瓶颈维度 r=32，并利用深度可分离卷积的低参数特性，总参数量仅为 1.48M（相对于 VFM 主干的 1.72%）。

### 3. 实验设计
- **数据集与任务**：
    -   **目标检测**：MS-COCO 2017 数据集。
    -   **语义分割**：ADE20K 数据集。
    -   **深度估计**：NYUv2 数据集。
- **Benchmark / 框架**：
    -   检测：采用 DETR 框架。
    -   分割：采用 Mask2Former 框架（在 MMSegmentation 库中实现）。
    -   深度：采用 DPT 框架。
- **对比方法**：包括 Full Tuning（全微调）、Fixed（冻结主干）以及多种 SOTA PEFT 方法：**BitFit**、**Partial-1**、**LoRA**、**Adapter**、**AdaptFormer**、**LoRand**、**VFM-Adapter**。
- **主干网络**：在三种主流 VFM 上验证泛化性：**DINOv2**、**SigLIP2**、**SAM**（均为 ViT-Base 变体）。

### 4. 资源与算力
- **明确规定**：论文中明确给出了 GPU 配置。
- **具体规模**：
    - **目标检测和语义分割**：在 **8 块 NVIDIA Tesla A800 GPU** 上进行。
    - **深度估计**：在 **单块 NVIDIA Tesla A800 GPU** 上进行。
- **未明确说明**：**训练时长**（如具体的小时数）在论文中未明确给出，仅提供了训练迭代数或轮数（如 COCO 30 epochs，ADE20K 160k iterations，NYUv2 20 epochs）。

### 5. 实验数量与充分性
- **实验数量**：论文进行了三组主要实验（三个任务 × 三个主干网络 = 9 个主要场景），并辅以**消融实验**（包括对瓶颈维度大小的分析和模块贡献度分析）。
- **充分性与公平性**：
    - **充分性**：实验覆盖了定位、分类、回归三大类密集预测任务，并在预训练范式差异显著的三个主流 VFM 上进行验证，实验设计较为全面。
    - **客观公平性**：对比了多种代表不同类别的 SOTA PEFT 基线方法，并公开了详细参数设置。在所有比较中，SIGMA 均以更少的参数（1.48M）取得了更优成绩，结果具有说服力。

### 6. 论文的主要结论与发现
- SIGMA 在所有 9 个（3任务×3主干）场景中均**显著且一致地优于所有对比的 PEFT 方法**。
- SIGMA 以极少的参数量（1.48M），**大幅缩小了与全微调之间的性能差距**。例如，在 DINOv2 检测任务中，恢复了 74.5% 的全微调增益；分割任务中差距仅 0.68 mIoU。
- **尺度自适应融合**（解决结构性差距）和**语义调制**（解决分布性差距）两个组件**协同作用**，共同构成了性能提升的关键。移除任一组件都会导致性能显著下降（mAP 分别下降 2.2 和 0.8）。
- 最优的瓶颈维度为 32，证明了所提架构的参数高效性。

### 7. 优点
- **设计巧妙，针对性强**：明确提出并系统性地分析了现有 PEFT 在 VFM 密集预测任务中面临的两大核心差距（结构性与分布性），并为此量身定制了解决方案，而非简单复用 NLP 方法。
- **结构合理，协同高效**：将空间（多尺度卷积）与分布（特征调制）适应机制紧密结合在一个模块内，实现了 1+1 > 2 的效果，且参数效率极高。
- **泛化能力强大**：在**三种不同预训练范式**（自监督、视觉-语言对比、提示式）的 VFM 上均表现最佳，验证了其作为通用适配方法的潜力。
- **性能卓越**：在目标检测、语义分割、深度估计三个任务上都达到了 SOTA 水平，性能提升显著且稳定。

### 8. 不足与局限
- **缺乏训练时长细节**：虽然提供了 GPU 数量和训练轮次，但未报告实际的训练时间，这使得量化其计算效率时缺少一个关键指标。
- **模型泛化性限制**：实验仅基于 ViT-Base 骨干网络，未在更大规模（如 ViT-Large/Huge）上充分验证其方法的扩展性和泛化能力。
- **计算成本分析**：虽然分析了参数效率，但未讨论引入多尺度深度卷积所带来的推理速度和 FLOPs 开销。对于实时性要求高的应用，这部分成本可能很重要。
- **应用限制**：（根据论文内容推断）方法依赖插入特定层（MHA 和 FFN 后），对于非标准 Transformer 结构或 CNN 主干的 VFM，适配可能需要进行修改，这限制了其适用范围的广度。

（完）
