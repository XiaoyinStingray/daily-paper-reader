---
title: "LC3EM: Long-Range Context Extrapolation Enhanced Entropy Model for Coordinate-based Overfitting Image Codecs"
title_zh: LC3EM：面向基于坐标的过拟合图像编解码器的长程上下文外推增强熵模型
authors: "Hao Wang, Junyan Huo, Fei Yang, Shuai Wan, Fuzheng Yang"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.29192v1"
tags: ["query:compress"]
score: 8.0
evidence: 面向学习式过拟合图像编解码器的增强熵模型
tldr: 针对坐标式过拟合图像编解码器，COOL-CHIC 等代表方法受限于熵建模权衡：轻量模型容量不足，复杂模型又需传输图像特定参数增加码率。本文受传统编解码器预测机制启发，提出 LC3EM 熵模型，引入无参数的邻域线性外推模式 NLEM 与微型 MLP 局部预测器互补，并用最小熵启发的连续模式选择自适应融合，仅需额外传输一层线性层参数。还加入轻量迭代隐变量取整精炼，缓解训练松弛与离散量化失配。在 COOL-CHIC 4.0/5.0 上取得一致 BD-rate 增益，验证了长程上下文外推对规则合成图像的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.29192v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 512, \"height\": 768}, {\"url\": \"assets/figures/arxiv/2609.29192v1/fig-002.webp\", \"caption\": \"\", \"page\": 5, \"index\": 2, \"width\": 800, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2609.29192v1/fig-003.webp\", \"caption\": \"\", \"page\": 5, \"index\": 3, \"width\": 800, \"height\": 500}, {\"url\": \"assets/figures/arxiv/2609.29192v1/fig-004.webp\", \"caption\": \"\", \"page\": 10, \"index\": 4, \"width\": 1716, \"height\": 1275}]"
motivation: COOL-CHIC 等坐标式过拟合编解码器存在熵建模权衡：轻量预测器容量有限，强表达模型需传图像特定参数，导致码率开销。
method: 提出 LC3EM，以无参数邻域线性外推 NLEM 补充微型 MLP 局部预测器，并用最小熵启发的连续模式选择自适应融合，仅传额外线性层参数；另加迭代隐变量取整精炼。
result: "多基准实验一致改进；在 SIQAD 和 API 上，集成 COOL-CHIC 4.0 取得 -3.43% 与 -7.69% BD-rate 增益；集成 5.0 则分别为 -2.88% 与 -3.15%，规则合成图像提升尤明显。"
conclusion: LC3EM 可即插即用地增强坐标式过拟合编解码器，以长程上下文外推和软模式选择提升压缩率，为低解码复杂度图像编码提供新熵建模思路。
---

## 摘要
基于坐标的过拟合图像编解码器因其低解码复杂度和不依赖跨图像泛化而受到越来越多的关注。然而，代表性方法如 COOL-CHIC 面临固有的熵建模权衡：轻量级模型容量有限，而表达能力更强的模型则会因传输图像特定参数而带来额外码率开销。受传统编解码器中预测机制的启发，我们提出了一种新的熵建模策略，引入具有区域自适应软模式选择的互补预测模式，而不是依赖单一学习到的预测器来建模多种类型的冗余。基于这一概念，我们开发了长程上下文外推增强熵模型（LC3EM），它可以集成到基于坐标的过拟合编解码器中。具体而言，一个无参数的基于邻域的线性外推模式（NLEM）补充了基于微型 MLP 的局部预测器，以利用长程上下文冗余和强方向性结构。设计了一种受最小熵启发的连续模式选择策略，以自适应融合这两种互补模式，同时仅需传输单个额外线性层的参数。此外，为缓解训练时松弛量化与实际离散量化之间的不匹配，我们引入了一个轻量级迭代潜在舍入细化阶段，以提高压缩性能。实验表明，在不同基准上均有一致提升，尤其是在高度规则的计算机生成图像上。当与 COOL-CHIC 4.0 集成时，所提方法在 SIQAD 和 API 数据集上分别实现了 -3.43% 和 -7.69% 的 BD-rate 增益。以 COOL-CHIC 5.0 作为骨干时，相应的增益分别为 -2.88% 和 -3.15%。代码将很快公开。

## Abstract
Coordinate-based overfitting image codecs have attracted increasing attention for their low decoding complexity and independence from cross-image generalization. However, representative approaches such as COOL-CHIC face an inherent entropy-modeling trade-off: lightweight models have limited capacity, while more expressive ones incur additional bitrate overhead from transmitting image-specific parameters. Inspired by the prediction mechanism in traditional codecs, we propose a new entropy-modeling strategy that introduces complementary prediction modes with region-adaptive soft mode selection, rather than relying on a single learned predictor to model diverse types of redundancy. Based on this concept, we develop a Long-Range Context Extrapolation Enhanced Entropy Model (LC3EM), which can be integrated into coordinate-based overfitting codecs. Specifically, a parameter-free Neighborhood-based Linear Extrapolation Mode (NLEM) complements the tiny MLP-based local predictor to exploit long-range contextual redundancy and strongly directional structures. A Minimum-Entropy-Inspired Continuous Mode Selection strategy is designed to adaptively fuse these two complementary modes, while requiring the transmission of only the parameters of a single additional linear layer. Moreover, to alleviate the mismatch between training-time relaxed and actual discrete quantization, we introduce a lightweight iterative latent rounding refinement stage to improve compression performance. Experiments demonstrate consistent improvements across diverse benchmarks, particularly on highly regular computer-generated images. When integrated with COOL-CHIC 4.0, the proposed method achieves BD-rate gains of -3.43\% and -7.69\% on the SIQAD and API datasets, respectively. With COOL-CHIC 5.0 as the backbone, the corresponding gains are -2.88\% and -3.15\%, respectively. The code will be made publicly available soon.

---

## 论文详细总结（自动生成）

# 论文总结：LC3EM：面向基于坐标的过拟合图像编解码器的长程上下文外推增强熵模型

## 1. 核心问题与整体含义
- **研究背景**：基于坐标的过拟合图像编解码器（如 COOL-CHIC 系列）因解码复杂度低、不依赖跨图像泛化而受到关注。其核心思路是为每张图像单独优化一个紧凑模型，并将模型参数和隐变量一起编码传输。
- **核心问题**：COOL-CHIC 等代表性方法面临固有熵建模权衡：
  - 轻量级熵模型容量有限，难以捕捉长程上下文冗余和强方向性结构；
  - 更强大的熵模型虽然能更好压缩隐变量，但需要传输更多图像特定参数，反而增加总码率。
- **整体含义**：论文试图回答“能否在几乎不增加参数传输开销的前提下增强熵模型能力”，并受传统编解码器中多假设预测与 RDO 模式选择机制启发，提出引入互补预测模式与区域自适应软模式选择的新熵建模策略。

## 2. 方法论
### 2.1 核心思想
- 提出 **LC3EM（Long-Range Context Extrapolation Enhanced Entropy Model）**，可即插即用地集成到基于坐标的过拟合编解码器中。
- 在原有微型 MLP 局部预测器之外，引入 **无参数的基于邻域线性外推模式 NLEM**，用于捕捉长程上下文和方向性结构冗余。
- 通过 **最小熵启发的连续模式选择 MECMS** 在分布层面自适应融合两种预测模式，仅需额外传输一个线性层参数。
- 增加 **迭代隐变量取整精炼阶段**，缓解训练时噪声松弛量化与实际离散取整之间的不匹配。

### 2.2 NLEM：基于邻域的线性外推模式
- 对目标位置 \(p\)，利用已解码邻域特征向量 \(x_p\) 和线性系数 \(a\) 预测：
  \[
  \hat I(p)=x_p^T a
  \]
- 具体分三步：
  1. **长程上下文样本构造**：收集 \(s\) 个已解码上下文位置，构造观测矩阵 \(X\) 和观测向量 \(y\)。文中取 \(s=40\)、\(c=4\)，上下文数量多于 MLP 局部模式。
  2. **外推系数估计**：基于局部模板距离计算相似度权重 \(S_k\)，使用加权最小二乘（WLS），并加入自适应岭正则：
     \[
     a^*=(X^TSX+\eta I_c)^{-1}X^TSy
     \]
     其中 \(\eta=\epsilon \cdot \frac{1}{c}\mathrm{tr}(X^TSX)\)，\(\epsilon=0.01\)。
  3. **特征预测**：用估计系数对目标位置进行点预测。
- NLEM 为手工设计模式，不需要额外参数传输，擅长边缘、线状纹理和规则方向结构。

### 2.3 MECMS：最小熵启发的连续模式选择
- 目标：在传统离散模式选择不适用于逐点熵建模且不可微的背景下，实现可微、连续、分布级的模式融合。
- 设 MLP 局部模式预测为 \(Z_1\sim L(\mu_1,b_1)\)，NLEM 预测为 \(Z_2\)，考虑融合随机变量：
  \[
  Z^*=wZ_1+(1-w)Z_2
  \]
- 从最小方差角度推导，理想权重为：
  \[
  w^*=\frac{\sigma_2^2}{\sigma_1^2+\sigma_2^2}
  \]
  融合后方差降低，对应熵降低。
- 实际参数化中，使用可学习权重 \(w\) 同时控制均值插值和尺度调制：
  \[
  \mu^*=w\mu_1+(1-w)\mu_2,\quad (b^*)^2=w b_1^2
  \]
- \(w\) 由轻量辅助网络预测，仅含一个全连接层加 tanh：
  \[
  w=(1.5\tanh(g_w(h)))_+
  \]
  即权重范围扩展至 \([0,1.5]\)。当 \(w\in(1,1.5]\) 时，尺度更新为：
  \[
  (b^*)^2=(2w^2-2w+1)b_1^2
  \]
- 仅需传输该额外线性层参数，几乎不增加网络码率。

### 2.4 迭代隐变量取整精炼
- 动机：训练时常用噪声松弛替代离散量化，而实际熵编码使用确定性取整；NLEM 对已解码隐变量敏感，小扰动可能影响后续最小二乘估计。
- 采用两类 SGA 与 SSL 松弛框架，冻结网络参数，仅更新隐变量网格。
- 对每个隐变量元素，考虑向下取整 \(I^-\) 和向上取整 \(I^+\) 两个候选，基于距离定义分数：
  \[
  \ell_r(p)=-\log\left[1+\left(\frac{d_r(p)}{1-d_r(p)}\right)^\alpha\right]
  \]
  加入 Gumbel 噪声后通过 softmax 得到软分配概率，并得到松弛隐变量。
- 两阶段率估计：
  - 前 1/4 迭代在松弛隐变量上评估率；
  - 后 3/4 迭代使用两个整数候选的期望编码代价。
- 实际执行 2000 次 Adam 更新，仅约占总优化迭代的 2%；温度从 0.3 退火至 0.08，学习率从 \(5\times10^{-4}\) 余弦退火至 \(1\times10^{-5}\)，最终硬取整。

## 3. 实验设计
- **骨干模型**：COOL-CHIC 4.0 与最新 COOL-CHIC 5.0。
- **编码配置**：采用 “hop” 解码配置和 “slow 100k” 编码配置；训练超参数与基线保持一致，基线与所提方法均重新训练以获得完整 RD 曲线。
- **数据集**：
  - **Kodak**：24 张自然 RGB 图像，768×512。
  - **SCID**：40 张高质量屏幕内容图像，1280×720，包含文本、界面、游戏等。
  - **SIQAD**：20 张高质量屏幕内容图像，分辨率不一，混合文本与自然内容。
  - **API**：动漫图像数据集，主实验使用按原始顺序的前 10 张 RGB 图像。
- **对比方法**：
  - 过拟合式编解码器：COOL-CHIC 4.0、COOL-CHIC 5.0、C3、LotteryCodec、LANCE。
  - 自编码器式学习编解码器：ELIC、TCM、DICA、基于超像素的方法。
  - 传统编解码器：VTM-18.2，以及面向屏幕内容的 VTM-18.2-SCC。
- **评价指标**：BD-rate、RD 曲线、PSNR、bpp、\(D+\lambda R\)、解码复杂度 MACs/pixel。

## 4. 资源与算力
- 论文**未明确说明使用的 GPU 型号、数量、训练时长或总能耗**。
- 文中仅提到：
  - 采用 COOL-CHIC 的 “slow 100k” 编码配置；
  - 取整精炼阶段执行 2000 次 Adam 更新，约占整体优化迭代的 2%；
  - 训练超参数与基线保持一致，基线和所提方法均重新训练。
- 因此，无法从文中判断具体算力规模和训练成本，这是实验可复现性方面的一个信息缺口。

## 5. 实验数量与充分性
- 实验覆盖较广：
  - 在 **4 个数据集**上评估 RD 性能，覆盖自然图像、屏幕内容、动漫图像；
  - 在 **2 个 COOL-CHIC 骨干**上验证通用性；
  - 与多类先进方法对比，包括过拟合式、自编码器式和传统编解码器；
  - 消融实验覆盖 NLEM、MECMS、取整精炼三个组件，在 Kodak 和 API 上进行；
  - 额外分析激活函数对权重范围的影响；
  - 分析比特流组成，对比扩大上下文但增加网络参数的变体；
  - 分析解码复杂度，并提出两种剪枝策略及组合策略。
- **充分性**：
  - 实验设计整体较充分，尤其是消融、比特流组成和复杂度分析，能较好支撑核心论点。
  - 结果一致显示所提方法在多个骨干和数据集上有效。
- **公平性与客观性**：
  - 基线和所提方法均重新训练，训练超参数保持一致，比较相对公平。
  - 但论文未报告随机种子、多次运行方差或统计显著性检验。
  - 部分数据集并非所有方法都参与比较，API 仅使用前 10 张图像，SCID 上 COOL-CHIC 4.0 的完整结果似乎未展示。
  - 硬件与训练时长未披露，影响复现与能耗公平比较。

## 6. 主要结论与发现
- LC3EM 可即插即用地增强 COOL-CHIC 系列，在不同数据集和骨干上取得一致的 BD-rate 增益。
- 以 **COOL-CHIC 5.0** 为骨干：
  - SI
