---
title: Distributed Image Compression with Multimodal Side Information at Extremely Low Bitrates
title_zh: 极低码率下基于多模态边信息的分布式图像压缩
authors: "Guojun Xu, Mingyang Zhang, Jianwen Xiang, Cheng Tan, Yanchao Yang, Junwei Zhou"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22061v1"
tags: ["query:compress"]
score: 9.0
evidence: 极低码率下利用多模态边信息的神经网络分布式图像压缩
tldr: 分布式图像压缩在极低码率下难以有效利用边信息，导致重建模糊和细节丢失。本文提出MDIC框架，首次以多模态方式引入边信息，通过文本引导扩散解码器捕获全局语义，并设计特征掩码生成器增强细节提取。在KITTI和Cityscapes数据集上，MDIC实现了极低码率下领先的感知质量，为多视传输提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22061v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 846, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1787, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1682, \"height\": 990, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 781, \"height\": 700, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1792, \"height\": 746, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1789, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1820, \"height\": 625, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22061v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 842, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22061v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22061v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 834, \"height\": 279, \"label\": \"Table\"}]"
motivation: 现有分布式压缩方法无法充分利用边信息的全局语义和对象级细节，导致极低码率下局部模糊和细节丢失。
method: 提出MDIC框架，结合文本到图像扩散解码器利用文本边信息，以及多模态对齐的特征掩码生成器增强视觉边信息的细节提取。
result: 在KITTI Stereo和Cityscapes数据集上，MDIC在低于0.1 bpp的极低码率下达到最先进的感知质量。
conclusion: MDIC首次将多模态边信息融入分布式图像压缩，有效保留了精细细节并提升了全局感知质量，为极低码率多视传输开辟了新方向。
---

## 摘要
分布式图像压缩对于多视图传输至关重要，尤其是在极低码率（< 0.1 bpp）条件下。其核心挑战在于，在严格的码率预算下如何有效利用边信息以实现高质量重建。然而，现有的分布式图像压缩方法难以从边信息中利用全局上下文和目标级别的细节，导致重建结果出现局部模糊和细节丢失。为解决这些局限性，我们提出了一种多模态分布式图像压缩框架（MDIC），首次将边信息以多模态方式引入分布式图像压缩范式，有效保留重建图像中的细粒度局部细节，并提升全局感知质量。具体而言，我们引入了一种基于文本到图像扩散的解码器，该解码器以从相关图像中提取的文本语义边信息为条件，以捕捉共享的全局语义。此外，我们设计了一个由多模态细粒度对齐任务监督的特征掩码生成器，以增强对视觉边信息的利用。生成的掩码具有双重作用：首先，它引导从无损传输的边信息中提取细粒度细节，以保持重建细节的语义一致性；其次，它调控从量化后的VQ-VAE嵌入中提取聚类特征表示，补偿主图像在极端压缩下丢失的类别信息。在广泛使用的KITTI立体和Cityscapes数据集上的大量实验表明，MDIC在极低码率下实现了最先进的感知质量。

## Abstract
Distributed Image Compression (DIC) is crucial for multi-view transmission, especially when operating at extremely low bitrates (< 0.1 bpp). Its core challenge is effectively utilizing side information to achieve high-quality reconstruction under strict bitrate budgets. However, existing DIC approaches struggle to exploit global context and object-level details from side information, leading to local blurring and the loss of fine details in the reconstruction. To address these limitations, we propose a Multimodal DIC framework (MDIC), which, for the first time, leverages side information in a multimodal manner into the DIC paradigm, effectively preserving fine-grained local details and enhancing global perceptual quality in reconstructed images. Specifically, we introduce a text-to-image diffusion-based decoder conditioned on textual side information extracted from correlated images to capture shared global semantics. Moreover, we design a feature-mask generator, supervised by a multimodal fine-grained alignment task, to strengthen the exploitation of visual side information. The generated mask serves two purposes: first, it guides the extraction of fine-grained details from losslessly transmitted side information to preserve the semantic consistency of reconstructed details; second, it regulates the extraction of clustered feature representations from the quantized VQ-VAE embeddings, compensating for category information lost under the extreme compression of the primary image. Extensive experiments on the widely used KITTI Stereo and Cityscapes datasets demonstrate that MDIC achieves state-of-the-art perceptual quality at extremely low bitrates.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与研究动机

- **背景**：分布式图像压缩（DIC）在多相机监控、三维场景重建等应用中至关重要，中心站接收一路无损视图作为边信息，仅对另一路或多路视图进行极低码率压缩（<0.1 bpp），从而在无需编码器间交互的条件下逼近联合编码效率。
- **核心挑战**：在码率预算极度受限时，如何有效利用边信息辅助解码器重建高质量图像，同时避免引入冗余或干扰信息。
- **现有方法的不足**：
  - 主流VAE架构下的DIC方法（如NDIC、ATN、LDMIC）通过全局交叉注意力融合边信息，但在极低码率下传输特征高度退化，注意力难以捕捉对象级细节，导致重建出现局部模糊和纹理缺失。
  - 这些方法侧重像素级保真度（PSNR、MS-SSIM），忽视感知质量，易在细节丢失时采用平均化弥补，造成过度平滑。
  - 感知导向的扩散模型单图压缩方法（如Perco、DiffEIC）虽能生成逼真纹理，但因缺少专用边信息建模，在多视图分布式场景下产生与原始图像不一致的语义和局部失真。

## 2. 方法论：多模态分布式图像压缩框架（MDIC）

### 2.1 整体架构
- **编码端**：
  - 使用冻结的预训练VAE编码器将主视图 \(I_x\) 和边信息视图 \(I_y\) 映射到隐空间。
  - 通过“HyperAlign”模块（卷积+线性注意力混合）进一步压缩并提取深度语义特征。
  - 对主视图隐变量 \(z_x\) 进行VQ-VAE量化，得到离散表示 \(\hat{z}_x\)，用于压缩和类别特征聚类。
  - 利用基于Transformer的自回归熵模型估计比特率。
  - 用BLIP-2模型从\(I_y\)生成文本描述，经CLIP文本编码器得到文本边信息 \(z_{\text{text}}\)。
  - 边信息视图的视觉特征直接无损传输（或部分量化作为补充）。

- **解码端**：
  - **视觉掩码生成模块（VMGM-TS）**：在文本监督下，利用主视图和边视图的量化特征（及其差分和乘积）生成区域感知的视觉掩码 \(m_v\)，该掩码通过与对象级文本语义对齐的训练，学会突出关键目标区域。
  - **掩码门控边信息补充模块（MG-SISM）**：
    - 门控 \(G_1\)：将掩码应用于量化的边信息 \(\hat{z}_y\)，通过视觉Transformer与 \(\hat{z}_x\) 交互，生成富含类别语义的特征 \(\hat{z}_{xc}\)，补偿主视图在极端压缩中丢失的类别信息。
    - 门控 \(G_2\)：将掩码应用于无损传输的边信息 \(z_y\)，提取细粒度对象细节，形成细节增强特征 \(z_d\)，同时通过门控抑制冗余或视角不一致信息。
  - **扩散解码器**：
    - 以文本边信息 \(z_{\text{text}}\)、类别特征 \(\hat{z}_{xc}\) 和细节特征 \(z_d\) 作为多模态条件。
    - 对初始隐变量 \(z_{L_x}\) 进行前向扩散加噪，再通过U-Net网络基于条件预测噪声，逐步去噪得到干净隐变量 \(\hat{z}_0\)。
    - 最后使用冻结的VAE解码器重建图像。

### 2.2 关键公式与技术细节
- **掩码生成**（式4-8）：利用特征差分 \(F_{\text{diff}}\) 和乘积 \(F_{\text{prod}}\) 捕捉差异与相似性，通过卷积模块输出logits，结合Gumbel噪声和硬阈值采样生成二值掩码。
- **文本监督的掩码对齐**（式9-12）：从图像描述中构建含“mask”标记的文本，通过自回归Transformer预测被屏蔽的词汇，使掩码生成器学会关注与文本语义相关的视觉区域，提升对象级语义一致性。
- **掩码门控机制**（式13、14）：\(\hat{z}_{xc} = G_1(\hat{z}_x, \hat{z}_y \odot m_v)\)，\(z_d = z_y + G_2(z_y) \odot F_m(m_v)\)，实现类别信息补充和细节增强。
- **扩散与去噪**（式15-17）：采用\(\hat{\alpha}_t\)调度，条件\(Z_{\text{cond}} = \{z_d, \hat{z}_{xc}, z_{\text{text}}\}\)，网络预测噪声\(\epsilon_\theta\)。
- **损失函数**：包含扩散噪声预测损失 \(L_{\text{diff}}\)、掩码交叉熵辅助损失 \(L_{\text{mask}}\)、VQ-VAE量化损失 \(L_{VQ}\)、率失真权衡损失 \(L_{RD}\)，加权组合训练。

## 3. 实验设计

### 3.1 数据集与预处理
- **KITTI Stereo**：训练集1576对，验证/测试集790对，图像中心裁切至370×740后下采样至128×256。
- **Cityscapes**：训练集2975对，验证集500对，测试集1525对，直接缩放至128×256。

### 3.2 评估指标
- **感知质量**（主要）：LPIPS、FID、DISTS、KID、NIQE。
- **失真指标**：PSNR、MS-SSIM（传统DIC指标），以及对象级语义一致性指标mIoU。
- 由于极低码率下低层失真指标的意义有限，论文以感知质量作为主要评价准则。

### 3.3 对比方法
- **DIC方法**：NDIC（2022）、ATN（2023）、LDMIC（2023）。
- **联合编码（SIC）方法**：SASIC、ECSIC、BiSIC、CAMSIC。
- **扩散基感知压缩（LIC）方法**：Perco、DiffEIC、RDEIC。
均在与MDIC相近的极低码率区间进行对比。

## 4. 资源与算力
- 硬件：4块NVIDIA L40 GPU。
- 训练配置：batch size 8，优化器AdamW，学习率采用warmup后恒定策略（前10000次迭代线性增至8×10⁻⁵），每个数据点平均训练约200个epoch。
- 推理：扩散步数设为10，硬采样阈值\(\theta=0.2\)，词汇表大小14（两个数据集最频繁的名词）。

## 5. 实验充分性与客观性
- **主实验**：在KITTI和Cityscapes上分别绘制了多个感知指标和失真指标随bpp变化的曲线，覆盖0.02~0.10 bpp典型区间，对比近10种SOTA方法，图表充分。
- **消融实验**：
  - 分别移除侧信息组件\(\hat{z}_{xc}\)和\(z_d\)，比较BD-Quality表明两者均对重建质量有显著贡献，尤其是类别特征缺失时影响更大（表1）。
  - 移除门控机制\(G_1\)和\(G_2\)，验证掩码门控的有效性（表2），并给出可视化对比。
- **可视化对比**：展示不同方法在真实样本上的重建效果，直观呈现MDIC在细节保留和语义一致性方面的优势。
- **公平性**：所有对比方法均基于相同数据集和分辨率重新训练或评估，码率区间对齐，指标选择合理，消融实验控制了变量，整体实验设计客观且较充分。

## 6. 主要结论与发现
- MDIC首次将多模态边信息（文本+视觉）引入分布式图像压缩框架，在极低码率下显著提升了重建图像的感知质量，同时保持了良好的对象级语义一致性（mIoU）。
- 通过文本描述提供全局语义先验，并通过特征掩码门控机制自适应地利用视觉边信息中的类别线索和细粒度细节，有效解决了传统方法因像素平均导致的模糊和扩散方法易产生语义不一致的问题。
- 在KITTI和Cityscapes数据集上，MDIC在LPIPS、FID、DISTS等感知指标上全面超越现有DIC、SIC及感知导向LIC方法，且在极低码率下失真指标（MS-SSIM、mIoU）也具备竞争力。

## 7. 方法优点
- **创新性强**：首次将多模态边信息（文本+视觉）与扩散模型结合用于DIC，开辟了极低码率下重建的新范式。
- **精细的边信息利用**：设计文本监督的掩码生成和双门控机制，既提取全局语义，又捕捉对象级细节和类别信息，兼顾了感知真实性与语义一致性。
- **性能优势明显**：在多个感知和语义指标上大幅领先同类方法，且在极低码率区间具有实际应用价值。
- **模块化设计**：依赖冻结的预训练模型（VAE、CLIP、BLIP-2），减少了多视图数据有限时从头训练的难度。

## 8. 不足与局限
- **数据依赖**：训练需要成对的多视图图像及其文本描述，文本描述质量会影响掩码监督效果，实际部署中可能需额外标注或生成字幕。
- **场景泛化性**：实验仅在自动驾驶场景（KITTI、Cityscapes）上验证，对其他多视图应用（如室内、遥感）的泛化能力未经验证。
- **计算复杂度**：扩散模型解码需多步去噪（文中推理用10步），相比单步VAE解码器，实时性可能受限。
- **没有讨论码率冗余**：边信息视图虽无损传输，但其数据量未计入总码率，实际存储/传输成本可能较高。
- **潜在偏差**：基于预训练生成模型，可能引入生成式偏差（如幻觉细节），在要求严格保真的场景下需谨慎评估。

（完）
